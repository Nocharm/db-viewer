# Service DB stack: PostgreSQL 16 + SQLAlchemy 2.0 + Alembic (정지점 2 승인)
# psycopg[binary]: PG 드라이버, 로컬 빌드 없이 설치 / PG driver without local build
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pydantic-settings==2.7.0
fastapi==0.115.6
uvicorn==0.32.1
# Phase 2 뷰 DDL 파싱 — 계획 명시 (dialect=tsql) / mandated by the plan for view parsing
sqlglot==25.34.0
# 인증 — Keycloak JWT 검증 + AD 동기화 (bpm 패턴·버전 미러링)
pyjwt[crypto]==2.13.0
ldap3==2.9.1
