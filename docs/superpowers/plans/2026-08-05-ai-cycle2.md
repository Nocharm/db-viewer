# AI 후속 사이클(1~5) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** reason 영속·기각 이력(1·2), 잡 기반 비동기(5), 임베딩 의미 검색+폴백(3), 스키마 Q&A 챗(4)을 스펙(2026-08-05-ai-cycle2-design.md)대로 구현한다.

**Architecture:** 마이그레이션 0009 하나(relations.reason + ai_embeddings + ai_jobs). judge 인터페이스를 수용/기각 판정 전체를 반환하는 `RelationJudgement`로 확장해 두 상태 모두 relations에 적재. suggest·임베딩 인덱싱은 scan_jobs 패턴(202+BackgroundTasks+폴링) 재사용. 임베딩은 같은 LLM 서버의 `/embeddings` + 순수 Python 코사인 — 실패·미설정 시 키워드 프리필터 자동 폴백. 챗은 검색 top-8 테이블 메타데이터를 컨텍스트로 한 동기 호출 + 전역 플로팅 패널.

**Tech Stack:** Python/FastAPI/pydantic-settings/stdlib urllib (의존성 0) / Next.js/React (신규 패키지 없음) / pytest·vitest

**Spec:** `docs/superpowers/specs/2026-08-05-ai-cycle2-design.md`

## Global Constraints

- **랜딩·푸시 순서 (사용자 지시):** Group A(파트 1·2, Tasks 1–4) 완료 후 `git push origin main` → Group B(파트 5, Tasks 5–6) 후 push → Group C(파트 3·4, Tasks 7–12) 후 push. 그룹 내 각 태스크는 개별 커밋.
- 신규 의존성 금지 (백엔드 stdlib, 프론트 기존 패키지만).
- 프롬프트 입력은 메타데이터만 — 원본 데이터 값 금지.
- 임베딩 인덱싱: 잡 1회 상한 `AI_EMBED_JOB_CAP`(기본 1000, **2000 초과 금지** — 사용자 부하 제약), 호출당 `AI_EMBED_BATCH`(32), 호출 간 `AI_EMBED_SLEEP_MS`(500ms).
- 응답 계약: 기존 키 불변 + additive만. **예외(승인됨):** `POST /api/ai/suggest-relations`는 202+폴링으로 전환.
- 검색은 임베딩 문제로 절대 502가 되지 않는다(자동 키워드 폴백). 동기 LLM 경로(재랭크·요약·해석·챗) 502 규약 유지. 잡 실패는 `ai_jobs.error`.
- 타입 힌트 전체 시그니처, `X | None`·`list[str]`, 주석 한/영 병기 WHY 위주, 기존 파일 스타일 준수.
- 커밋: `type(scope): English summary — 한국어 요약`. 커밋 직전 PROGRESS.md의 "AI 후속 사이클(1~5) 설계 확정" 항목 끝 진행 표기 "(구현 중 — Task N/12)" 갱신·동반 스테이징.
- 전체 그린 + 린트 클린으로만 커밋: `cd backend && .venv/bin/python -m pytest tests -q && .venv/bin/ruff check app alembic tests`. 프론트 변경 태스크는 `cd frontend && npm test`(vitest)와 `npx next lint`가 있으면 lint까지.
- main 직커밋(저장소 관행).

---

### Task 1: 마이그레이션 0009 + 모델

**Files:**
- Create: `backend/alembic/versions/0009_ai_cycle2.py`
- Modify: `backend/app/models/relations.py` (Relation.reason + AiEmbedding), `backend/app/models/jobs.py` (AiJob), `backend/app/models/__init__.py`
- Test: 기존 `backend/tests/test_migrations.py`가 자동 검증(테이블 전수·모델 diff) — 신규 테스트 파일 불필요.

**Interfaces:**
- Produces: `Relation.reason: Mapped[str | None]`, `AiEmbedding(object_qname unique, model, vector, source_hash, updated_at)`, `AiJob(kind, status, progress_done, progress_total, result, error, triggered_by, created_at, started_at, finished_at)` — 이후 전 태스크가 사용.

- [ ] **Step 1: 모델 추가** — `relations.py`의 `Relation`에 `created_at` 필드 위에:

```python
    # LLM 판정 근거 — 수용/기각 공용, 규칙·사용자 기원은 NULL / judge reason, AI rows only
    reason: Mapped[str | None] = mapped_column(Text)
```

같은 파일 `AiSummary` 아래에:

```python
class AiEmbedding(Base):
    """Table embedding for semantic search. / 의미 검색용 테이블 임베딩 (사이클2 §3)."""

    __tablename__ = "ai_embeddings"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    object_qname: Mapped[str] = mapped_column(String(261))
    model: Mapped[str] = mapped_column(String(128))
    # JSON 배열 텍스트 — pgvector 없이 파이썬 코사인 (2,342×768차원 ≈ 수십 ms)
    vector: Mapped[str] = mapped_column(Text)
    # 원문 해시 — 변경분만 재임베딩 / re-embed only when source text changes
    source_hash: Mapped[str] = mapped_column(String(64))
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))

    __table_args__ = (
        UniqueConstraint("object_qname", name="uq_ai_embeddings_qname"),
    )
```

`jobs.py`의 `ScanResult` 아래에:

```python
class AiJob(Base):
    """AI background jobs — suggest judging, embedding indexing. / AI 백그라운드 잡 (사이클2 §5)."""

    __tablename__ = "ai_jobs"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    kind: Mapped[str] = mapped_column(String(16))
    status: Mapped[str] = mapped_column(String(16))
    progress_done: Mapped[int] = mapped_column(Integer)
    progress_total: Mapped[int] = mapped_column(Integer)
    # 완료 결과 JSON — suggest: {suggested, created, rejected, items} / embed: {indexed, skipped, remaining}
    result: Mapped[str | None] = mapped_column(Text)
    error: Mapped[str | None] = mapped_column(Text)
    triggered_by: Mapped[str] = mapped_column(String(64))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))
    started_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    finished_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))

    __table_args__ = (
        CheckConstraint("kind IN ('suggest', 'embed_index')", name="ck_ai_jobs_kind"),
        CheckConstraint(
            "status IN ('queued', 'running', 'done', 'failed')", name="ck_ai_jobs_status"
        ),
    )
```

`models/__init__.py`에 `AiEmbedding`(relations), `AiJob`(jobs) import·`__all__` 추가(알파벳 순 기존 배치 따름).

- [ ] **Step 2: 마이그레이션 작성** — `0009_ai_cycle2.py` (0008 파일 헤더 형식 그대로, `revision="0009"`, `down_revision="0008"`):

```python
def upgrade() -> None:
    op.add_column("relations", sa.Column("reason", sa.Text(), nullable=True))
    op.create_table(
        "ai_embeddings",
        sa.Column("id", sa.Integer(), primary_key=True),
        sa.Column("object_qname", sa.String(261), nullable=False),
        sa.Column("model", sa.String(128), nullable=False),
        sa.Column("vector", sa.Text(), nullable=False),
        sa.Column("source_hash", sa.String(64), nullable=False),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
        sa.UniqueConstraint("object_qname", name="uq_ai_embeddings_qname"),
    )
    op.create_table(
        "ai_jobs",
        sa.Column("id", sa.Integer(), primary_key=True),
        sa.Column("kind", sa.String(16), nullable=False),
        sa.Column("status", sa.String(16), nullable=False),
        sa.Column("progress_done", sa.Integer(), nullable=False),
        sa.Column("progress_total", sa.Integer(), nullable=False),
        sa.Column("result", sa.Text(), nullable=True),
        sa.Column("error", sa.Text(), nullable=True),
        sa.Column("triggered_by", sa.String(64), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.Column("started_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column("finished_at", sa.DateTime(timezone=True), nullable=True),
        sa.CheckConstraint("kind IN ('suggest', 'embed_index')", name="ck_ai_jobs_kind"),
        sa.CheckConstraint(
            "status IN ('queued', 'running', 'done', 'failed')", name="ck_ai_jobs_status"
        ),
    )


def downgrade() -> None:
    op.drop_table("ai_jobs")
    op.drop_table("ai_embeddings")
    op.drop_column("relations", "reason")
```

- [ ] **Step 3: 검증** — `cd backend && .venv/bin/python -m pytest tests/test_migrations.py -q` (테이블 전수·모델 diff 테스트가 신규 스키마를 자동 커버) 후 전체 스위트+ruff.

- [ ] **Step 4: 커밋** — `feat(ai): migration 0009 — relation reason, embeddings, ai jobs — 사이클2 스키마`

---

### Task 2: judge 판정 전체 반환 — RelationJudgement 전환

**Files:**
- Modify: `backend/app/adapters/ai.py`, `backend/app/adapters/llm_ai.py`
- Test: `backend/tests/test_ai.py`, `backend/tests/test_llm_ai.py`

**Interfaces:**
- Produces: `RelationJudgement(src_object, src_column, tgt_object, tgt_column, accepted: bool, reason: str)` (frozen dataclass, adapters/ai.py). Protocol·Fake·Llm 모두 `judge_relations(candidates: list[CandidatePair]) -> list[RelationJudgement]` — **판정된 전부**(수용+기각)를 반환, LLM이 index를 누락한 페어는 미판정(반환 없음 → 기록 없음 → 다음 실행 재등장).
- `AiRelationSuggestion`은 이 전환으로 고아가 되면 제거(참조 grep 후).

- [ ] **Step 1: adapters/ai.py** — `AiRelationSuggestion` 자리에(또는 아래) 추가:

```python
@dataclass(frozen=True)
class RelationJudgement:
    """수용/기각 판정 — 둘 다 relations에 적재된다 (사이클2 §1·2). / full verdict."""

    src_object: str
    src_column: str
    tgt_object: str
    tgt_column: str
    accepted: bool
    reason: str
```

Protocol과 `FakeAiClient.judge_relations`를 판정 전체 반환으로 교체:

```python
    def judge_relations(self, candidates: list[CandidatePair]) -> list[RelationJudgement]:
        # 뷰 JOIN 증거 또는 명명 유사면 수용 — 결정론 판정 / deterministic verdicts
        return [
            RelationJudgement(
                src_object=c.src_object, src_column=c.src_column,
                tgt_object=c.tgt_object, tgt_column=c.tgt_column,
                accepted="view_join" in c.signals
                         or _normalize(c.src_column) == _normalize(c.tgt_column),
                reason=f"signals: {', '.join(c.signals)}",
            )
            for c in candidates
        ]
```

- [ ] **Step 2: llm_ai.py judge_relations** — 매핑을 판정 전체로 교체 (가드 유지·확장):

```python
    def judge_relations(self, candidates: list[CandidatePair]) -> list[RelationJudgement]:
        if not candidates:
            return []
        data = self._chat(build_judge_prompt(candidates))
        judgements = data.get("judgements", [])
        if not isinstance(judgements, list):
            raise AiUnavailableError("llm returned malformed judgements",
                                     {"data": str(data)[:200]})
        seen: set[int] = set()
        verdicts = []
        for j in judgements:
            if not isinstance(j, dict):
                continue
            idx = j.get("index")
            # 모델이 지어낸 인덱스·타입은 버린다 / drop hallucinated or mistyped indices
            if not isinstance(idx, int) or isinstance(idx, bool) or not 0 <= idx < len(candidates):
                continue
            accepted = j.get("accept")
            if not isinstance(accepted, bool):
                continue  # 불량 accept는 미판정 처리 — 다음 실행에 재등장
            if idx in seen:
                continue
            seen.add(idx)
            c = candidates[idx]
            fallback = "LLM accepted" if accepted else "LLM rejected"
            verdicts.append(RelationJudgement(
                src_object=c.src_object, src_column=c.src_column,
                tgt_object=c.tgt_object, tgt_column=c.tgt_column,
                accepted=accepted, reason=str(j.get("reason") or fallback),
            ))
        return verdicts
```

import를 `RelationJudgement`로 교체. `AiRelationSuggestion` 참조를 전수 grep(`grep -rn AiRelationSuggestion backend`)해 남는 곳이 없으면 정의 삭제.

- [ ] **Step 3: 테스트 갱신** — test_llm_ai.py의 judge 테스트들: 수용만 세던 단언을 판정 전체 기준으로 갱신(기각도 반환됨 — `accepted=False`·reason "무관" 확인, `"false"` 문자열 accept는 미판정=미반환 유지, reason 폴백은 accepted/rejected 각각). test_ai.py의 Fake judge 테스트: 신호 없는 페어가 이제 `accepted=False`로 **반환됨**을 단언(빈 리스트 아님).

- [ ] **Step 4: 전체 검증** — 이 시점 api/ai.py 엔드포인트는 아직 구 반환형을 기대하므로 **Task 3까지 한 커밋으로 묶는다. 커밋하지 말고 Task 3로 진행.**

---

### Task 3: 적재·그래프·상세 — reason 영속 + rejected 이력 (Task 2와 한 커밋)

**Files:**
- Modify: `backend/app/api/ai.py` (suggest 엔드포인트 적재부), `backend/app/api/objects.py` (`_load_relation_edges`의 ai_suggested payload + detail relations payload)
- Test: `backend/tests/test_ai.py`

**Interfaces:**
- Produces: suggest 응답 `{snapshot_id, suggested, created, rejected, items}` (rejected additive). graph의 ai_suggested 엣지에 `reason` 키. `/api/objects/{id}/detail`의 relations 항목에 `reason` 키.

- [ ] **Step 1: 적재부 교체** — api/ai.py suggest 엔드포인트의 judgements 처리:

```python
    judgements = ai.judge_relations(pairs_meta)

    now = datetime.now(UTC)
    created = []
    rejected_count = 0
    for j in judgements:
        db.add(Relation(
            src_object=j.src_object, src_column=j.src_column,
            tgt_object=j.tgt_object, tgt_column=j.tgt_column,
            status="candidate" if j.accepted else "rejected",
            origin="ai", reason=j.reason, created_at=now,
        ))
        if j.accepted:
            created.append({**key_as_dict((j.src_object, j.src_column,
                                           j.tgt_object, j.tgt_column)),
                            "reason": j.reason})
        else:
            rejected_count += 1
    return {"snapshot_id": snapshot.id, "suggested": len(pairs_meta),
            "created": len(created), "rejected": rejected_count,
            "items": created[:100]}
```

기각도 relations에 남으므로 기존 dedupe(전 relations 양방향)가 자동으로 재실행에서 제외한다 — **추가 로직 금지**.

- [ ] **Step 2: objects.py** — `_load_relation_edges`의 `edges.append({...})`에 ai_suggested일 때만 `"reason": rel.reason` 추가(다른 kind는 키 생략 또는 None — 파일 스타일에 맞게 통일). rejected는 기존 where 필터가 이미 제외함 — 변경 금지. detail 엔드포인트(라인 ~164)의 relations 항목 dict에 `"reason": rel.reason` 추가.

- [ ] **Step 3: 테스트 추가** — test_ai.py:

```python
def test_judgements_persist_reason_and_rejections(client, migrated_engine, load_fixture):
    """수용은 candidate+reason, 기각은 rejected+reason — 재실행 자동 제외 (사이클2 §1·2)."""
    from app.api.ai import get_ai_client
    from app.adapters.ai import RelationJudgement

    _seed(client, load_fixture)

    class _SplitAi:
        def judge_relations(self, candidates):
            out = []
            for i, c in enumerate(candidates):
                out.append(RelationJudgement(
                    src_object=c.src_object, src_column=c.src_column,
                    tgt_object=c.tgt_object, tgt_column=c.tgt_column,
                    accepted=(i % 2 == 0), reason=f"근거 {i}",
                ))
            return out

    client.app.dependency_overrides[get_ai_client] = lambda: _SplitAi()
    try:
        first = client.post("/api/ai/suggest-relations").json()
        second = client.post("/api/ai/suggest-relations").json()
    finally:
        client.app.dependency_overrides.pop(get_ai_client)

    assert first["created"] > 0 and first["rejected"] > 0

    with migrated_engine.connect() as conn:
        rel_t = Base.metadata.tables["relations"]
        rows = conn.execute(sa.select(rel_t).where(rel_t.c.origin == "ai")).all()
    assert all(r.reason for r in rows)
    statuses = {r.status for r in rows}
    assert statuses == {"candidate", "rejected"}
    # 기각분이 dedupe에 걸려 두 번째 실행은 같은 페어를 재판정하지 않는다
    first_keys = {(r.src_object, r.src_column, r.tgt_object, r.tgt_column) for r in rows}
    assert len(first_keys) == len(rows)  # 중복 적재 없음
    assert second["suggested"] == 0 or second["created"] + second["rejected"] > 0  # 다음 창으로 전진


def test_rejected_relations_never_render_as_edges(client, migrated_engine, load_fixture):
    _seed(client, load_fixture)
    client.post("/api/ai/suggest-relations")  # Fake — 수용/기각 혼재 가능
    items = client.get("/api/objects", params={"limit": 1}).json()["items"]
    graph = client.get(f"/api/objects/{items[0]['id']}/graph?depth=3").json()
    with migrated_engine.connect() as conn:
        rel_t = Base.metadata.tables["relations"]
        rejected = conn.execute(
            sa.select(rel_t).where(rel_t.c.status == "rejected")).all()
    rejected_ids = {f"rel-{r.id}" for r in rejected}
    assert all(e["id"] not in rejected_ids for e in graph["edges"])


def test_ai_suggested_edges_carry_reason(client, load_fixture):
    _seed(client, load_fixture)
    body = client.post("/api/ai/suggest-relations").json()
    assert body["created"] > 0
    target = body["items"][0]
    _, table = target["src_object"].split(".", 1)
    anchor = client.get("/api/objects", params={"q": table}).json()["items"][0]
    graph = client.get(f"/api/objects/{anchor['id']}/graph").json()
    ai_edges = [e for e in graph["edges"] if e["kind"] == "ai_suggested"]
    assert ai_edges and all(e.get("reason") for e in ai_edges)
```

주의: 기존 paging 테스트(`test_suggest_relations_pages_without_dupes_then_exhausts`)는 Fake가 이제 기각도 반환하므로 `total_created` 단언이 달라질 수 있다 — Fake는 픽스처 후보 대부분을 수용하므로 그대로 통과가 기대값이나, 실패 시 단언을 `total_created + total_rejected > 40`으로 조정(계약 의미 동일 — 페이징 전진).

- [ ] **Step 4: 전체 검증 + 커밋** — 전체 스위트+ruff 그린 후:
`feat(ai): persist judge verdicts with reasons — 판정 근거 영속·기각 이력 (rejected 자동 제외)`

---

### Task 4: 프론트 — reason 표시 + Group A 푸시

**Files:**
- Modify: `frontend/src/lib/types.ts` (GraphEdge.reason, DetailRelation.reason), `frontend/src/components/browser/TableDetail.tsx`, `frontend/src/components/erd/ErdCanvas.tsx`, `frontend/src/lib/i18n.ts`

**Interfaces:**
- Consumes: Task 3의 graph 엣지 `reason`, detail relations `reason`.

- [ ] **Step 1: types.ts** — `GraphEdge`에 `reason?: string | null;` 추가. detail relations 항목 인터페이스(파일에서 `other`/`confidence` 가진 타입 검색)에 `reason?: string | null;` 추가.

- [ ] **Step 2: TableDetail** — 관계 행(라인 ~418 `detail.relations.map`)의 confidence 스팬 뒤에:

```tsx
{relation.reason && (
  <span className="block truncate text-xs" style={{ color: "var(--muted)" }}
        title={relation.reason}
        data-testid={`TableDetail-relationReason-${index}`}>
    {relation.reason}
  </span>
)}
```

- [ ] **Step 3: ErdCanvas hover reason 카드** — 기존 엣지 hover 강조 핸들러(onEdgeMouseEnter/Leave에서 `buildEdgeEmphasis` 호출하는 지점)에 얹는다:
  - state 추가: `const [edgeReason, setEdgeReason] = useState<string | null>(null);`
  - enter 핸들러에서 해당 edge가 `kind === "ai_suggested"`이고 `reason`이 있으면 `setEdgeReason(edge.reason)`, leave에서 `setEdgeReason(null)`.
  - 캔버스 루트(부유 카드들과 같은 컨테이너)에 렌더:

```tsx
{edgeReason && (
  <div className="absolute bottom-3 left-3 z-20 max-w-md rounded-lg border px-3 py-2 text-xs"
       style={{ borderColor: "var(--hairline)", background: "var(--surface-card)",
                color: "var(--body-text)" }}
       data-testid="ErdCanvas-edgeReasonCard">
    <span className="font-semibold" style={{ color: "var(--stat-ink)" }}>AI</span> {edgeReason}
  </div>
)}
```

  기존 파일의 부유 카드·hover 코드 관용을 따르되 레이아웃 재계산을 유발하지 않는 렌더 데코레이션만 추가한다.

- [ ] **Step 4: 검증 + 커밋 + 푸시** — `cd frontend && npm test` + 백엔드 스위트 무변경 확인. 커밋:
`feat(frontend): show AI judge reasons on edges and detail — 판정 근거 표시`
그 후 **`git push origin main`** (Group A 체크포인트).

---

### Task 5: ai_jobs 서비스 + suggest 202 전환

**Files:**
- Create: `backend/app/services/ai_jobs.py`
- Modify: `backend/app/api/ai.py`
- Test: `backend/tests/test_ai.py`

**Interfaces:**
- Produces:
  - `run_ai_job(session_factory: sessionmaker, job_id: int, ai: AiClient, settings: Settings) -> None` — kind 분기 실행기(embed_index 분기는 Task 8이 채움 — 이 태스크에서는 `raise ValueError(f"unknown kind: {job.kind}")` 폴백).
  - `run_suggest(db: Session, ai: AiClient, settings: Settings, snapshot_id: int | None) -> dict` — 기존 동기 suggest 본문을 함수로 추출(반환 dict = 기존 응답 그대로).
  - `POST /api/ai/suggest-relations` → **202** `{"job_id", "status"}`; 같은 kind의 queued/running 존재 시 **409**.
  - `GET /api/ai/jobs/{job_id}` → `{"job_id", "kind", "status", "progress_done", "progress_total", "result", "error"}` (result는 done일 때 파싱된 JSON, 아니면 None).

- [ ] **Step 1: 서비스 작성** — `ai_jobs.py`:

```python
"""AI background jobs — suggest judging, embedding indexing. / AI 잡 실행기 (사이클2 §5)."""

import json
import logging
from datetime import UTC, datetime

from sqlalchemy import select
from sqlalchemy.orm import Session, sessionmaker

from app.adapters.ai import AiClient
from app.config import Settings
from app.models import AiJob

logger = logging.getLogger(__name__)


def run_suggest(db: Session, ai: AiClient, settings: Settings,
                snapshot_id: int | None) -> dict:
    """기존 동기 suggest 로직 — api/ai.py에서 이동, 반환은 기존 응답 dict 그대로."""
    # Task 3까지의 suggest_relations 본문을 그대로 이 함수로 옮긴다
    # (resolve_snapshot → 후보 선별 → dedupe → judge → 적재 → dict 반환).
    ...


def run_ai_job(session_factory: sessionmaker, job_id: int,
               ai: AiClient, settings: Settings) -> None:
    """백그라운드 진입점 — 잡 상태 전이·에러 기록. / job lifecycle wrapper."""
    with session_factory() as db:
        job = db.get(AiJob, job_id)
        if job is None or job.status != "queued":
            return
        job.status = "running"
        job.started_at = datetime.now(UTC)
        db.commit()
        try:
            if job.kind == "suggest":
                result = run_suggest(db, ai, settings, snapshot_id=None)
            else:
                raise ValueError(f"unknown kind: {job.kind}")
            job.result = json.dumps(result, ensure_ascii=False)
            job.status = "done"
        except Exception as e:  # 잡 실패는 502 규약 대상 아님 — error 컬럼 기록
            logger.exception("ai job failed", extra={"job_id": job_id, "kind": job.kind})
            db.rollback()
            job = db.get(AiJob, job_id)
            job.status = "failed"
            job.error = str(e)
        job.finished_at = datetime.now(UTC)
        db.commit()


def has_active_job(db: Session, kind: str) -> bool:
    """kind별 동시 실행 1개 가드 / one active job per kind."""
    return db.execute(
        select(AiJob.id).where(AiJob.kind == kind,
                               AiJob.status.in_(["queued", "running"]))
    ).first() is not None
```

`run_suggest`의 `...`는 실제 이동한 본문으로 채운다 — **플레이스홀더로 커밋 금지**. suggest 본문 이동 시 진행 갱신 추가: judge 호출 전 `job` 접근이 없으므로 진행은 (총 페어 수를 알기 어려운 LLM 1콜 구조상) `progress_total=1, done=0→1`로 단순화 — scan처럼 세밀할 필요 없음(계약은 status가 핵심).

- [ ] **Step 2: API 전환** — api/ai.py: suggest 엔드포인트를 202로 교체(scan.py의 start_scan 형식 준용):

```python
@router.post("/suggest-relations", status_code=202)
def start_suggest_job(
    background: BackgroundTasks,
    db: Session = Depends(get_db),
    ai: AiClient = Depends(get_ai_client),
    session_factory: sessionmaker = Depends(get_ai_session_factory),
) -> dict:
    """AI 관계 제안 — 202 + ai_jobs 폴링 (사이클2 §5). / async suggest."""
    if has_active_job(db, "suggest"):
        raise HTTPException(409, {"message": "suggest job already running", "context": {}})
    job = AiJob(kind="suggest", status="queued", progress_done=0, progress_total=1,
                triggered_by="ui", created_at=datetime.now(UTC))
    db.add(job)
    db.flush()
    settings = get_settings()
    background.add_task(run_ai_job, session_factory, job.id, ai, settings)
    return {"job_id": job.id, "status": job.status}


@router.get("/jobs/{job_id}")
def get_ai_job(job_id: int, db: Session = Depends(get_db)) -> dict:
    job = db.get(AiJob, job_id)
    if job is None:
        raise HTTPException(404, {"message": "job not found", "context": {"job_id": job_id}})
    return {"job_id": job.id, "kind": job.kind, "status": job.status,
            "progress_done": job.progress_done, "progress_total": job.progress_total,
            "result": json.loads(job.result) if job.result else None,
            "error": job.error}
```

`get_ai_session_factory`는 scan.py의 `get_scan_session_factory`와 동일 형태로 신설(테스트 DI 지점). import 정리(`sessionmaker`, `BackgroundTasks`, `AiJob`, `run_ai_job`, `has_active_job`, `json`).

- [ ] **Step 3: 테스트 전환** — test_ai.py의 suggest 관련 테스트들을 202+폴링으로 갱신. conftest의 client는 TestClient — BackgroundTasks는 응답 후 동기 실행되므로:

```python
def _run_suggest_job(client) -> dict:
    """202 시작 → 완료 폴링 헬퍼 / start then poll to completion."""
    start = client.post("/api/ai/suggest-relations")
    assert start.status_code == 202
    job = client.get(f"/api/ai/jobs/{start.json()['job_id']}").json()
    assert job["status"] == "done", job.get("error")
    return job["result"]
```

기존 `client.post("/api/ai/suggest-relations").json()` 호출부를 `_run_suggest_job(client)`로 교체(반환 dict 계약은 동일 — suggested/created/rejected/items). 409 테스트 추가: queued 상태 AiJob을 직접 insert 후 시작 → 409. 세션 팩토리 DI: conftest client fixture에 `get_ai_session_factory` override가 필요하면 scan 테스트의 기존 처리 방식을 그대로 따른다(`grep -n get_scan_session_factory backend/tests`).

- [ ] **Step 4: 전체 검증 + 커밋** — `feat(ai): async suggest via ai_jobs with 202 polling — 관계 제안 잡 전환`

---

### Task 6: 프론트 잡 폴링 전환 + Group B 푸시

**Files:**
- Modify: `frontend/src/lib/api.ts`, `frontend/src/app/erd/page.tsx`

- [ ] **Step 1: api.ts** — `suggestRelationsAi`를 교체:

```ts
export interface AiJobStatus {
  job_id: number;
  kind: string;
  status: "queued" | "running" | "done" | "failed";
  progress_done: number;
  progress_total: number;
  result: { suggested: number; created: number; rejected: number } | null;
  error: string | null;
}

export function startAiSuggest(): Promise<{ job_id: number; status: string }> {
  return postJson("/api/ai/suggest-relations", {});
}

export function fetchAiJob(jobId: number): Promise<AiJobStatus> {
  return getJson(`/api/ai/jobs/${jobId}`);
}
```

- [ ] **Step 2: erd/page.tsx** — 버튼 onClick을 시작+폴링으로 교체 (ColumnPanel의 스캔 폴링 관용):

```tsx
const [aiJobId, setAiJobId] = useState<number | null>(null);

useEffect(() => {
  if (aiJobId === null) return;
  const timer = setInterval(() => {
    fetchAiJob(aiJobId)
      .then((job) => {
        if (job.status === "done" && job.result) {
          setAiNotice(t("erd.aiNotice")
            .replace("{s}", String(job.result.suggested))
            .replace("{n}", String(job.result.created)));
          setAiJobId(null);
          setAiBusy(false);
        } else if (job.status === "failed") {
          setAiNotice(job.error ?? t("ai.failed"));
          setAiJobId(null);
          setAiBusy(false);
        }
      })
      .catch((e) => {
        setAiNotice(e.message);
        setAiJobId(null);
        setAiBusy(false);
      });
  }, 1500);
  return () => clearInterval(timer);
}, [aiJobId, t]);
```

onClick: `setAiBusy(true); void startAiSuggest().then((res) => setAiJobId(res.job_id)).catch((e) => { setAiNotice(e.message); setAiBusy(false); });`
import 교체(`startAiSuggest, fetchAiJob`). i18n에 `ai.failed` 키가 없으면 추가(ko: "AI 작업 실패", en: "AI job failed" — 기존 사전 구조 따름).

- [ ] **Step 3: 검증 + 커밋 + 푸시** — `npm test` + 백엔드 스위트. 커밋:
`feat(frontend): poll the async AI suggest job — 제안 버튼 잡 폴링 전환`
그 후 **`git push origin main`** (Group B 체크포인트).

---

### Task 7: 임베딩 설정 + 클라이언트 + 코사인

**Files:**
- Modify: `backend/app/config.py`, `.env.example`, `docker-compose.yml`, `backend/app/adapters/llm_ai.py`
- Test: `backend/tests/test_llm_ai.py`

**Interfaces:**
- Produces: Settings `ai_embed_model: str = ""`, `ai_embed_batch: int = 32`, `ai_embed_job_cap: int = 1000`, `ai_embed_sleep_ms: int = 500`. `embed_texts(base_url: str, model: str, api_key: str, timeout: int, texts: list[str]) -> list[list[float]]`, `cosine_similarity(a: list[float], b: list[float]) -> float` (llm_ai.py 모듈 함수).

- [ ] **Step 1: Settings** — `ai_suggest_max_pairs` 아래 4필드(스펙 표 그대로, Environment/Tuning 주석 — job_cap 주석에 "2000 초과 금지" 명시). `.env.example` AI 블록에 4항목 추가. compose backend env에 `AI_EMBED_MODEL: ${AI_EMBED_MODEL}` + int 3종은 `${VAR:-기본값}` 폴백(구 .env 부팅 안전 관행).

- [ ] **Step 2: llm_ai.py** — 추가:

```python
def embed_texts(base_url: str, model: str, api_key: str, timeout: int,
                texts: list[str]) -> list[list[float]]:
    """OpenAI 호환 /embeddings 1회 호출 — 배치 입력 / one embeddings call, batched input."""
    url = f"{base_url.rstrip('/')}/embeddings"
    headers = {"Content-Type": "application/json"}
    if api_key:
        headers["Authorization"] = f"Bearer {api_key}"
    request = urllib.request.Request(
        url, data=json.dumps({"model": model, "input": texts},
                             ensure_ascii=False).encode(),
        headers=headers, method="POST",
    )
    last_error: Exception | None = None
    for attempt in range(RETRY_COUNT + 1):
        try:
            with urllib.request.urlopen(request, timeout=timeout) as response:
                payload = json.loads(response.read().decode())
            data = payload["data"]
            if not isinstance(data, list) or len(data) != len(texts):
                raise KeyError("embeddings count mismatch")
            # index 순 정렬 — 서버가 순서를 보장하지 않을 수 있다
            data = sorted(data, key=lambda d: d["index"])
            return [[float(x) for x in d["embedding"]] for d in data]
        except (URLError, TimeoutError, KeyError, IndexError, TypeError,
                ValueError, json.JSONDecodeError) as e:
            last_error = e
            logger.warning("embeddings attempt failed",
                           extra={"url": url, "model": model, "attempt": attempt})
    raise AiUnavailableError("embeddings request failed after retries",
                             {"url": url, "model": model, "cause": str(last_error)})


def cosine_similarity(a: list[float], b: list[float]) -> float:
    """순수 파이썬 코사인 — 의존성 0 / dependency-free cosine."""
    if len(a) != len(b) or not a:
        return 0.0
    dot = sum(x * y for x, y in zip(a, b))
    norm_a = math.sqrt(sum(x * x for x in a))
    norm_b = math.sqrt(sum(y * y for y in b))
    if norm_a == 0.0 or norm_b == 0.0:
        return 0.0
    return dot / (norm_a * norm_b)
```

- [ ] **Step 3: 테스트** — Settings 기본값 4종; `cosine_similarity`(동일=1.0, 직교=0.0, 역=−1.0, 길이 불일치·영벡터=0.0); `embed_texts` 성공 매핑(index 역순 응답 → 정렬 확인)·개수 불일치 raise·재시도 후 raise (`captured` 확장 또는 전용 fake_urlopen).

- [ ] **Step 4: 전체 검증 + 커밋** — `feat(ai): embeddings client and cosine — 임베딩 클라이언트·코사인`

---

### Task 8: 임베딩 인덱싱 잡 + 관리 콘솔 버튼

**Files:**
- Create: `backend/app/services/ai_embeddings.py`
- Modify: `backend/app/services/ai_jobs.py` (embed_index 분기), `backend/app/api/ai.py` (시작 엔드포인트), `frontend/src/app/admin/page.tsx`, `frontend/src/lib/api.ts`, `frontend/src/lib/i18n.ts`
- Test: `backend/tests/test_ai.py` (또는 신규 `test_ai_embeddings.py`)

**Interfaces:**
- Produces: `build_embedding_text(qname: str, column_names: list[str], summary: str | None) -> str`, `compute_source_hash(text: str, model: str) -> str`, `run_embed_index(db: Session, job: AiJob, settings: Settings) -> dict` (반환 {"indexed", "skipped", "remaining"}). `POST /api/ai/embed-index` → 202 (admin.py와 동일한 sysadmin 게이트 의존성 — `grep -n "def require" backend/app/auth.py`로 이름 확인 후 사용), kind별 409.

- [ ] **Step 1: 서비스** — `ai_embeddings.py`:

```python
"""Embedding index builder — capped, batched, throttled. / 임베딩 인덱싱 (사이클2 §3).

부하 제약(사용자 지시): 잡 1회 상한 AI_EMBED_JOB_CAP(2000 초과 금지),
호출당 AI_EMBED_BATCH, 호출 간 AI_EMBED_SLEEP_MS 대기. 남은 분량은
재실행이 이어간다 — 제안 페이징과 같은 문법.
"""

import hashlib
import json
import time
from datetime import UTC, datetime

from sqlalchemy import select
from sqlalchemy.orm import Session

from app.adapters.llm_ai import embed_texts
from app.config import Settings
from app.models import AiEmbedding, AiJob, AiSummary, CatalogColumn, CatalogObject, Snapshot


def build_embedding_text(qname: str, column_names: list[str],
                         summary: str | None) -> str:
    parts = [qname, " ".join(column_names)]
    if summary:
        parts.append(summary)
    return "\n".join(parts)


def compute_source_hash(text: str, model: str) -> str:
    return hashlib.sha256(f"{model}\n{text}".encode()).hexdigest()


def run_embed_index(db: Session, job: AiJob, settings: Settings) -> dict:
    snapshot = db.execute(
        select(Snapshot).where(Snapshot.status == "ready")
        .order_by(Snapshot.id.desc()).limit(1)
    ).scalar_one_or_none()
    if snapshot is None:
        raise RuntimeError("no ready snapshot to index")

    columns_by_obj: dict[int, list[str]] = {}
    for object_id, name in db.execute(
        select(CatalogColumn.object_id, CatalogColumn.name)
        .join(CatalogObject, CatalogColumn.object_id == CatalogObject.id)
        .where(CatalogObject.snapshot_id == snapshot.id)
        .order_by(CatalogColumn.object_id, CatalogColumn.ordinal)
    ):
        columns_by_obj.setdefault(object_id, []).append(name)
    summaries = {s.object_qname: s.summary
                 for s in db.execute(select(AiSummary)).scalars()}
    existing = {e.object_qname: e
                for e in db.execute(select(AiEmbedding)).scalars()}

    pending: list[tuple[str, str, str]] = []  # (qname, text, hash)
    skipped = 0
    for obj in db.execute(
        select(CatalogObject)
        .where(CatalogObject.snapshot_id == snapshot.id,
               CatalogObject.type == "table")
        .order_by(CatalogObject.schema, CatalogObject.name)
    ).scalars():
        qname = f"{obj.schema}.{obj.name}"
        text = build_embedding_text(qname, columns_by_obj.get(obj.id, []),
                                    summaries.get(qname))
        source_hash = compute_source_hash(text, settings.ai_embed_model)
        row = existing.get(qname)
        if row is not None and row.source_hash == source_hash:
            skipped += 1
            continue
        pending.append((qname, text, source_hash))

    total_pending = len(pending)
    batch_input = pending[:settings.ai_embed_job_cap]
    job.progress_total = len(batch_input)
    db.commit()

    indexed = 0
    batch = settings.ai_embed_batch
    for start in range(0, len(batch_input), batch):
        chunk = batch_input[start:start + batch]
        vectors = embed_texts(settings.ai_base_url, settings.ai_embed_model,
                              settings.ai_api_key, settings.ai_timeout,
                              [text for _, text, _ in chunk])
        now = datetime.now(UTC)
        for (qname, _, source_hash), vector in zip(chunk, vectors):
            row = existing.get(qname)
            if row is None:
                db.add(AiEmbedding(object_qname=qname,
                                   model=settings.ai_embed_model,
                                   vector=json.dumps(vector),
                                   source_hash=source_hash, updated_at=now))
            else:
                row.model = settings.ai_embed_model
                row.vector = json.dumps(vector)
                row.source_hash = source_hash
                row.updated_at = now
        indexed += len(chunk)
        job.progress_done = indexed
        db.commit()  # 부분 진행 보존 — 실패해도 인덱싱분 유지
        if start + batch < len(batch_input) and settings.ai_embed_sleep_ms > 0:
            time.sleep(settings.ai_embed_sleep_ms / 1000)

    return {"indexed": indexed, "skipped": skipped,
            "remaining": total_pending - indexed}
```

- [ ] **Step 2: 잡 분기 + API** — ai_jobs.py `run_ai_job`의 else 분기 앞에 `elif job.kind == "embed_index": result = run_embed_index(db, db.get(AiJob, job_id), settings)` — 아니, job 객체 재사용: `result = run_embed_index(db, job, settings)`. embed_index는 `ai` 클라이언트 불필요(모듈 함수 직접 호출). api/ai.py에 시작 엔드포인트:

```python
@router.post("/embed-index", status_code=202)
def start_embed_index_job(
    background: BackgroundTasks,
    db: Session = Depends(get_db),
    session_factory: sessionmaker = Depends(get_ai_session_factory),
) -> dict:
    """임베딩 인덱싱 — 관리 작업, 상한·배치·대기로 부하 관리 (사이클2 §3)."""
    settings = get_settings()
    if not settings.ai_base_url or not settings.ai_embed_model:
        raise HTTPException(400, {"message": "embedding is not configured",
                                  "context": {"ai_embed_model": settings.ai_embed_model}})
    if has_active_job(db, "embed_index"):
        raise HTTPException(409, {"message": "embed index job already running", "context": {}})
    job = AiJob(kind="embed_index", status="queued", progress_done=0, progress_total=0,
                triggered_by="admin", created_at=datetime.now(UTC))
    db.add(job)
    db.flush()
    background.add_task(run_ai_job, session_factory, job.id, create_ai_client(), settings)
    return {"job_id": job.id, "status": job.status}
```

라우터 게이트: 이 엔드포인트만 sysadmin 제한 — admin.py가 쓰는 의존성 이름을 확인해 `dependencies=[Depends(<그 이름>)]`을 데코레이터에 부여.

- [ ] **Step 3: 백엔드 테스트** — fake embed_texts를 monkeypatch(`app.services.ai_embeddings.embed_texts`)로: ① cap 준수(cap=2로 설정 시 progress_total=2·remaining>0), ② source_hash 스킵(2회차 skipped 증가·호출 없음), ③ 배치 분할(batch=1이면 호출 수=대상 수), ④ sleep은 `ai_embed_sleep_ms=0`로 무력화, ⑤ 미설정 400·중복 409. Settings 주입은 `Settings(_env_file=None, ai_base_url="http://llm/v1", ai_embed_model="e", ai_embed_job_cap=2, ...)` 객체를 run_embed_index에 직접 전달.

- [ ] **Step 4: 관리 콘솔** — api.ts에 `startEmbedIndex(): Promise<{job_id, status}>` (postJson `/api/ai/embed-index`) 추가 — 폴링은 Task 6의 `fetchAiJob` 재사용. admin/page.tsx의 수집 패널 아래에 같은 카드 관용으로 "AI 임베딩 인덱싱" 섹션: 시작 버튼(진행 중 비활성) + 진행 `done/total` + 완료 시 `indexed/skipped/remaining` 표시 + 실패 시 error. i18n 키(admin.embedIndex*) ko/en 추가. `data-testid="AdminPage-embedIndexButton"` 등 identifiers 규칙 준수.

- [ ] **Step 5: 전체 검증 + 커밋** — `feat(ai): capped embedding index job with admin panel — 임베딩 인덱싱 잡 (상한·배치·대기)`

---

### Task 9: 검색 임베딩 경로 + 자동 폴백

**Files:**
- Create: `backend/app/services/ai_search.py`
- Modify: `backend/app/adapters/llm_ai.py` (`rerank_tables` 분리), `backend/app/api/ai.py` (search 엔드포인트)
- Test: `backend/tests/test_llm_ai.py`, `backend/tests/test_ai.py`

**Interfaces:**
- Produces: `LlmAiClient.rerank_tables(query: str, candidates: list[TableMeta]) -> list[AiTableHit]` (기존 search_tables의 재랭크 부분 추출 — search_tables = 키워드 프리필터 + rerank_tables). `search_tables_smart(db: Session, query: str, tables: list[TableMeta], ai: AiClient, settings: Settings) -> tuple[str, list[AiTableHit]]` — mode("embedding"|"keyword")와 히트 반환. search 응답에 `"mode"` additive.

- [ ] **Step 1: rerank 분리** — LlmAiClient.search_tables 내부의 "LLM 재랭크 + 환각/점수 가드 + 정렬 + 상한" 블록을 `rerank_tables(self, query, candidates)`로 추출, search_tables는 `candidates = filter_search_candidates(query, tables); return [] if not candidates else self.rerank_tables(query, candidates)`. 기존 테스트 무수정 통과 확인.

- [ ] **Step 2: 서비스** — `ai_search.py`:

```python
"""Two-stage table search — embedding first, keyword fallback. / 의미 검색 + 폴백 (사이클2 §3)."""

import json
import logging

from sqlalchemy import select
from sqlalchemy.orm import Session

from app.adapters.ai import AiClient, AiTableHit, TableMeta
from app.adapters.llm_ai import (
    AiUnavailableError,
    LlmAiClient,
    cosine_similarity,
    embed_texts,
)
from app.config import Settings
from app.models import AiEmbedding

logger = logging.getLogger(__name__)

# 임베딩 프리필터 상한 — 키워드 경로의 50과 동일한 재랭크 입력 크기
EMBED_TOP_K = 50


def rank_by_cosine(query_vec: list[float], rows: list[tuple[str, list[float]]],
                   top_k: int) -> list[str]:
    """(qname, vector) 목록을 코사인 내림차순 상위 top_k qname으로 / pure ranking."""
    scored = [(cosine_similarity(query_vec, vec), qname) for qname, vec in rows]
    scored.sort(key=lambda pair: (-pair[0], pair[1]))
    return [qname for _, qname in scored[:top_k]]


def search_tables_smart(db: Session, query: str, tables: list[TableMeta],
                        ai: AiClient, settings: Settings) -> tuple[str, list[AiTableHit]]:
    """임베딩 가용이면 의미 검색, 아니면(또는 실패하면) 키워드 폴백.

    폴백은 에러가 아니다 — 검색이 임베딩 문제로 502가 되지 않는 것이 계약.
    """
    if settings.ai_embed_model and isinstance(ai, LlmAiClient):
        try:
            rows = [
                (e.object_qname, json.loads(e.vector))
                for e in db.execute(
                    select(AiEmbedding)
                    .where(AiEmbedding.model == settings.ai_embed_model)
                ).scalars()
            ]
            if rows:
                query_vec = embed_texts(
                    settings.ai_base_url, settings.ai_embed_model,
                    settings.ai_api_key, settings.ai_timeout, [query],
                )[0]
                by_qname = {t.qname: t for t in tables}
                candidates = [
                    by_qname[qname]
                    for qname in rank_by_cosine(query_vec, rows, EMBED_TOP_K)
                    if qname in by_qname
                ]
                if candidates:
                    return "embedding", ai.rerank_tables(query, candidates)
        except AiUnavailableError as e:
            # 임베딩 실패는 폴백 신호 — warning 로그 후 키워드로
            logger.warning("embedding search unavailable, falling back",
                           extra={"cause": str(e)})
    return "keyword", ai.search_tables(query, tables)
```

- [ ] **Step 3: 엔드포인트** — api/ai.py search_tables: `hits = ai.search_tables(...)` 를 `mode, hits = search_tables_smart(db, q, _load_table_meta(db, snapshot.id), ai, get_settings())`로 교체, 응답에 `"mode": mode` 추가(기존 키 불변).

- [ ] **Step 4: 테스트** — ① `rank_by_cosine` 순수 단위. ② 폴백 3경로: 모델 미설정(Fake라도 keyword), embed_texts 실패(monkeypatch로 AiUnavailableError → keyword + 로그), 인덱스 빈 경우(keyword). ③ 임베딩 경로: AiEmbedding 2행 시드 + embed_texts·rerank 스텁 → mode=="embedding"·코사인 상위가 재랭크 입력이 됨. ④ 엔드포인트 응답에 mode 존재(Fake 경로 "keyword").

- [ ] **Step 5: 전체 검증 + 커밋** — `feat(ai): semantic search with automatic keyword fallback — 임베딩 검색·자동 폴백`

---

### Task 10: Q&A 챗 백엔드

**Files:**
- Create: `backend/app/services/ai_chat.py`
- Modify: `backend/app/adapters/ai.py` (ChatTableContext·ChatContext·Protocol·Fake), `backend/app/adapters/llm_ai.py` (build_chat_prompt + answer_question), `backend/app/api/ai.py` (POST /chat)
- Test: `backend/tests/test_ai.py`, `backend/tests/test_llm_ai.py`

**Interfaces:**
- Produces: `ChatTableContext(qname, columns: list[ColumnMeta], summary: str | None, relations: list[str], base_tables: list[str])`, `ChatContext(tables: list[ChatTableContext])` (frozen dataclass, adapters/ai.py). Protocol 6번째 메서드 `answer_question(question: str, history: list[tuple[str, str]], context: ChatContext) -> str`. `build_chat_context(db: Session, snapshot_id: int, query: str, tables: list[TableMeta], ai: AiClient, settings: Settings) -> ChatContext` (services/ai_chat.py — search_tables_smart 재사용, top-8; snapshot_id는 lineage 역추적용). `POST /api/ai/chat` → `{"answer", "tables", "mock"}`.

- [ ] **Step 1: adapters/ai.py** — dataclass 2종 + Protocol 메서드 + Fake:

```python
    def answer_question(self, question: str, history: list[tuple[str, str]],
                        context: ChatContext) -> str:
        names = ", ".join(t.qname for t in context.tables[:5]) or "관련 테이블 없음"
        return f"[목업] 관련 테이블: {names}. 질문: {question}"
```

- [ ] **Step 2: llm_ai.py** —

```python
CHAT_HISTORY_LIMIT = 6  # 프롬프트에 싣는 최근 턴 수 / recent turns kept in prompt


def build_chat_prompt(question: str, history: list[tuple[str, str]],
                      context: ChatContext) -> str:
    payload = {
        "tables": [{
            "qname": t.qname,
            "columns": [{"name": c.name, "type": c.data_type, "pk": c.is_pk}
                        for c in t.columns],
            "summary": t.summary,
            "relations": t.relations,
            "base_tables": t.base_tables,
        } for t in context.tables],
    }
    lines = [f"{role}: {content}" for role, content in history[-CHAT_HISTORY_LIMIT:]]
    history_block = ("이전 대화:\n" + "\n".join(lines) + "\n\n") if lines else ""
    return (
        "다음 스키마 컨텍스트만 근거로 사용자의 질문에 답하라. "
        "컨텍스트에 없는 테이블·컬럼·관계는 추측하지 말고 모른다고 답하라.\n"
        '출력 스키마: {"text": "<한국어 답변, 3~6문장>"}\n\n'
        f"{history_block}질문: {question}\n\n스키마 컨텍스트:\n"
        f"{json.dumps(payload, ensure_ascii=False)}"
    )


    def answer_question(self, question: str, history: list[tuple[str, str]],
                        context: ChatContext) -> str:
        return _require_text(self._chat(build_chat_prompt(question, history, context)))
```

- [ ] **Step 3: 서비스** — `ai_chat.py`: `build_chat_context(db, snapshot_id, query, tables, ai, settings)` — `search_tables_smart(db, query, tables, ai, settings)`로 관련 테이블 top-8 qname 선정(히트 없으면 빈 컨텍스트), qname별로: columns(tables의 TableMeta에서), summary(AiSummary), relations(해당 qname이 src/tgt인 validated·confirmed를 `"src.col → tgt.col (status)"` 문자열로 최대 10건), base_tables(ViewLineageFlat 역추적 — api/ai.py summarize의 기존 쿼리 관용 재사용) → ChatContext.

- [ ] **Step 4: 엔드포인트** — api/ai.py:

```python
class ChatTurn(BaseModel):
    role: Literal["user", "assistant"]
    content: str = Field(min_length=1, max_length=2000)


class ChatRequest(BaseModel):
    question: str = Field(min_length=2, max_length=500)
    history: list[ChatTurn] = Field(default_factory=list, max_length=6)


@router.post("/chat")
def chat(
    req: ChatRequest,
    snapshot_id: int | None = None,
    db: Session = Depends(get_db),
    ai: AiClient = Depends(get_ai_client),
) -> dict:
    """스키마 Q&A — 검색 컨텍스트 기반 동기 응답 (사이클2 §4). / schema Q&A chat."""
    snapshot = resolve_snapshot(db, snapshot_id)
    tables = _load_table_meta(db, snapshot.id)
    context = build_chat_context(db, req.question, tables, ai, get_settings())
    answer = ai.answer_question(
        req.question, [(t.role, t.content) for t in req.history], context)
    return {"answer": answer,
            "tables": [t.qname for t in context.tables],
            "mock": isinstance(ai, FakeAiClient)}
```

(pydantic `BaseModel`/`Field`/`Literal` import — 파일 상단 그룹에 맞춰.)

- [ ] **Step 5: 테스트** — ① Fake 경로 엔드포인트: answer 비어있지 않음·mock True·tables가 픽스처 qname 부분집합. ② history 검증: 7턴 → 422. ③ Llm 경로(스텁 _chat): 프롬프트에 컨텍스트 테이블·이전 대화·질문 포함, `{"text": ...}` 반환 매핑. ④ build_chat_context: 관계 문자열 형식·top-8 상한.

- [ ] **Step 6: 전체 검증 + 커밋** — `feat(ai): schema Q&A chat endpoint with search-driven context — 스키마 챗 백엔드`

---

### Task 11: 챗 프론트 — 플로팅 패널

**Files:**
- Create: `frontend/src/components/ChatPanel.tsx`, `frontend/src/lib/chat-utils.ts`, `frontend/src/lib/chat-utils.test.ts`
- Modify: `frontend/src/components/AppHeader.tsx` (버튼+패널 마운트), `frontend/src/lib/api.ts`, `frontend/src/lib/i18n.ts`

**Interfaces:**
- Consumes: `POST /api/ai/chat` `{question, history}` → `{answer, tables, mock}`.
- Produces: `chatAi(question: string, history: ChatTurn[]): Promise<ChatResponse>` (api.ts), `buildChatHistory(messages: ChatMessage[]): ChatTurn[]` (chat-utils — 마지막 6턴만).

- [ ] **Step 1: api.ts**

```ts
export interface ChatTurn { role: "user" | "assistant"; content: string }
export interface ChatResponse { answer: string; tables: string[]; mock: boolean }

export function chatAi(question: string, history: ChatTurn[]): Promise<ChatResponse> {
  return postJson("/api/ai/chat", { question, history });
}
```

- [ ] **Step 2: chat-utils.ts** (+ vitest):

```ts
/** 챗 이력 정리 — 서버 상한(6턴)에 맞춰 최근 턴만 전송 / trim to the server cap. */

import type { ChatTurn } from "./api";

export const CHAT_HISTORY_LIMIT = 6;

export interface ChatMessage extends ChatTurn { tables?: string[] }

export function buildChatHistory(messages: ChatMessage[]): ChatTurn[] {
  return messages.slice(-CHAT_HISTORY_LIMIT)
    .map(({ role, content }) => ({ role, content }));
}
```

테스트: 7개 메시지 → 마지막 6개, tables 필드 제거 확인.

- [ ] **Step 3: ChatPanel.tsx** — 전역 플로팅 패널 (기존 부유 패널·카드 스타일 변수 관용):

```tsx
"use client";

/** 스키마 Q&A 플로팅 챗 — 세션 메모리만, 전 페이지 공용 (사이클2 §4). */

import Link from "next/link";
import { useRef, useState } from "react";

import { useI18n } from "@/components/i18n";
import { chatAi } from "@/lib/api";
import { buildChatHistory, type ChatMessage } from "@/lib/chat-utils";

export function ChatPanel() {
  const { t } = useI18n();
  const [open, setOpen] = useState(false);
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [input, setInput] = useState("");
  const [busy, setBusy] = useState(false);
  const [mock, setMock] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const listRef = useRef<HTMLDivElement | null>(null);

  const send = () => {
    const question = input.trim();
    if (question.length < 2 || busy) return;
    const next: ChatMessage[] = [...messages, { role: "user", content: question }];
    setMessages(next);
    setInput("");
    setBusy(true);
    setError(null);
    chatAi(question, buildChatHistory(messages))
      .then((res) => {
        setMock(res.mock);
        setMessages([...next,
          { role: "assistant", content: res.answer, tables: res.tables }]);
        // 새 답변으로 스크롤 / scroll to the newest answer
        requestAnimationFrame(() =>
          listRef.current?.scrollTo({ top: listRef.current.scrollHeight }));
      })
      .catch((e) => setError(e.message))
      .finally(() => setBusy(false));
  };

  return (
    <>
      <button className="icon-button" onClick={() => setOpen((cur) => !cur)}
              title={t("chat.title")} data-testid="ChatPanel-toggleButton">
        💬
      </button>
      {open && (
        <div className="fixed bottom-4 right-4 z-50 flex h-[480px] w-96 flex-col rounded-lg border shadow-lg"
             style={{ borderColor: "var(--hairline)", background: "var(--surface-card)" }}
             data-testid="ChatPanel-root">
          <div className="flex items-center gap-2 border-b px-3 py-2"
               style={{ borderColor: "var(--hairline)" }}>
            <span className="text-sm font-semibold">{t("chat.title")}</span>
            {mock && (
              <span className="badge badge--muted" data-testid="ChatPanel-mockBadge">
                {t("chat.mockBadge")}
              </span>
            )}
            <button className="icon-button ml-auto" onClick={() => setOpen(false)}
                    data-testid="ChatPanel-closeButton">✕</button>
          </div>
          <div ref={listRef} className="min-h-0 flex-1 overflow-y-auto px-3 py-2"
               data-testid="ChatPanel-messages">
            {messages.length === 0 && (
              <p className="text-xs" style={{ color: "var(--muted)" }}>
                {t("chat.emptyHint")}
              </p>
            )}
            {messages.map((message, index) => (
              <div key={index} className="mb-2">
                <div className="text-xs font-semibold"
                     style={{ color: message.role === "user" ? "var(--stat-ink)" : "var(--slate)" }}>
                  {message.role === "user" ? t("chat.you") : "AI"}
                </div>
                <div className="whitespace-pre-wrap text-sm">{message.content}</div>
                {message.tables && message.tables.length > 0 && (
                  <div className="mt-1 flex flex-wrap gap-1">
                    {message.tables.map((qname) => (
                      <Link key={qname} href={`/erd?anchor=${encodeURIComponent(qname)}`}
                            className="badge badge--muted"
                            data-testid={`ChatPanel-tableChip-${qname}`}>
                        {qname}
                      </Link>
                    ))}
                  </div>
                )}
              </div>
            ))}
            {busy && <p className="text-xs" style={{ color: "var(--muted)" }}>{t("ai.working")}</p>}
            {error && <p className="text-xs" style={{ color: "var(--danger, #c00)" }}
                         data-testid="ChatPanel-error">{error}</p>}
          </div>
          <div className="flex gap-2 border-t px-3 py-2" style={{ borderColor: "var(--hairline)" }}>
            <input className="min-w-0 flex-1 rounded border px-2 py-1 text-sm"
                   style={{ borderColor: "var(--hairline)", background: "transparent" }}
                   value={input}
                   placeholder={t("chat.placeholder")}
                   onChange={(e) => setInput(e.target.value)}
                   onKeyDown={(e) => { if (e.key === "Enter") send(); }}
                   data-testid="ChatPanel-input" />
            <button className="btn-secondary !py-1 text-xs" onClick={send}
                    disabled={busy || input.trim().length < 2}
                    data-testid="ChatPanel-sendButton">
              {t("chat.send")}
            </button>
          </div>
        </div>
      )}
    </>
  );
}
```

`/erd?anchor=` 딥링크가 qname 형태를 받는지 erd/page.tsx의 searchParams 처리에서 확인 — 이름만 받으면 `qname.split(".")[1]`로 맞춘다.

- [ ] **Step 4: AppHeader 마운트** — `<div className="ml-auto ...">` 안 `{children}` 뒤에 `<ChatPanel />` 추가(import 포함). 패널은 fixed라 헤더 밖에 그려진다.

- [ ] **Step 5: i18n** — ko/en 사전에 `chat.title`("스키마 Q&A"/"Schema Q&A"), `chat.placeholder`("스키마에 대해 질문…"/"Ask about the schema…"), `chat.send`("전송"/"Send"), `chat.mockBadge`("AI 미연결 — 목업 응답"/"AI offline — mock reply"), `chat.emptyHint`("예: 수주와 출하를 잇는 테이블은?"/"e.g. which tables link orders to shipping?"), `chat.you`("나"/"You") — 기존 사전 구조 그대로.

- [ ] **Step 6: 검증 + 커밋** — `npm test`(chat-utils 테스트 포함) + 백엔드 스위트 무변경. 커밋:
`feat(frontend): global floating schema chat panel — 전역 챗 패널`

---

### Task 12: 문서 + 최종 검증 + Group C 푸시

**Files:**
- Modify: `docs/connect.md` (§9 확장), `PROGRESS.md` (압축), 확인만: `README.md`

- [ ] **Step 1: connect.md §9 확장** — 기존 §9 끝에 추가:

```markdown
### 임베딩 의미 검색 켜기 (선택)

`.env`에 `AI_EMBED_MODEL=<임베딩 모델명>` 추가 → backend 재기동 → 관리 콘솔 →
**AI 임베딩 인덱싱** 실행. 잡 1회 최대 `AI_EMBED_JOB_CAP`(기본 1,000)개만 처리한다
— 2,342 테이블이면 **3회 실행** (남은 개수가 화면에 표시된다). 서버 부하는
호출당 `AI_EMBED_BATCH`(32)·호출 간 `AI_EMBED_SLEEP_MS`(500ms)로 조절.

- ✅ 확인: `?` 검색이 이름에 없는 의미 질의에도 결과를 낸다. 임베딩이 죽어도
  검색은 키워드 방식으로 자동 폴백된다(에러가 아님).
- 챗: 헤더 💬 버튼 — AI 미연결이면 "목업 응답" 배지가 뜬다. 대화는 새로고침 시 사라진다.
- AI 관계 제안 버튼은 이제 백그라운드 잡이다 — 진행 표시 후 "N건 판정, M건 생성".
  기각된 후보도 기록되어 재실행마다 항상 새 후보로 전진한다.
```

- [ ] **Step 2: README 확인** — `grep -n -i "ai\|chat" README.md` — 기능 서술 변경 필요 시만 갱신(env 개별 나열 없음이 관행).

- [ ] **Step 3: PROGRESS 압축** — "AI 후속 사이클(1~5) 설계 확정" 항목(진행 표기 포함)을 완료 요약 1건으로 교체: 판정 근거·기각 이력(rejected 재사용, 페이징 전진), suggest·임베딩 ai_jobs 202 전환, 임베딩 검색+키워드 폴백(잡 상한 1,000·배치 32·대기 500ms), 전역 챗 패널(세션 메모리), 테스트 수 변화, 스펙·계획 링크.

- [ ] **Step 4: 최종 전체 검증** — `cd backend && .venv/bin/python -m pytest tests -q && .venv/bin/ruff check app alembic tests && cd ../frontend && npm test`

- [ ] **Step 5: 커밋 + 푸시** — `docs(connect): embedding, chat and async suggest runbook — 사이클2 런북` 후 **`git push origin main`** (Group C 체크포인트).

---

## 남는 리스크 (실행자 참고)

- ERD hover reason 카드와 챗 패널은 기존 파일의 훅·스타일 관용에 의존 — 구현자는 대상 파일을 먼저 읽고 앵커를 확인할 것 (이 계획의 스니펫은 스타일 변수 기준).
- `/erd?anchor=` 딥링크 파라미터 형태(qname vs name)는 erd/page.tsx에서 확인 후 칩 href를 맞출 것.
- 실 임베딩 품질·차원·인덱싱 소요는 서버 연결 후에만 확인 가능 — connect.md 체크리스트가 수동 검증 경로.
- 챗 컨텍스트 top-8·관계 10건 상한은 소형 LLM 컨텍스트 창 보호용 — 부족하면 튜닝 후보(별도 사이클).
