{
  "name": "W0 recon queries",
  "nodes": [
    {
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "parameters": {}
    },
    {
      "name": "recon 01 fk count",
      "type": "n8n-nodes-base.microsoftSql",
      "typeVersion": 1.1,
      "position": [
        220,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "-- [1] FK 개수 / foreign key count — FK가 적을수록 추론(Phase 3~4) 의존도가 커진다\nSELECT COUNT(*) AS fk_count FROM sys.foreign_keys;\n"
      },
      "credentials": {
        "microsoftSql": {
          "id": "REPLACE_ME",
          "name": "MSSQL readonly"
        }
      }
    },
    {
      "name": "recon 02 object scale",
      "type": "n8n-nodes-base.microsoftSql",
      "typeVersion": 1.1,
      "position": [
        440,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "-- [2] 객체 규모 / object scale — 계획 전제(테이블 409)와의 차이 확인\nSELECT type_desc, COUNT(*) AS cnt\nFROM sys.objects\nWHERE type IN ('U', 'V')\nGROUP BY type_desc;\n"
      },
      "credentials": {
        "microsoftSql": {
          "id": "REPLACE_ME",
          "name": "MSSQL readonly"
        }
      }
    },
    {
      "name": "recon 03 view definition permission",
      "type": "n8n-nodes-base.microsoftSql",
      "typeVersion": 1.1,
      "position": [
        660,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "-- [3] 뷰 DDL 권한 / VIEW DEFINITION permission  ★ 최우선\n-- blocked > 0 이면 권한 부족 — 에러가 아니라 조용히 NULL이라 놓치기 쉽다\n-- blocked > 0 means missing permission; fails silently as NULL, not an error\nSELECT COUNT(*) AS total,\n       SUM(CASE WHEN m.definition IS NULL THEN 1 ELSE 0 END) AS blocked\nFROM sys.views v\nLEFT JOIN sys.sql_modules m ON v.object_id = m.object_id;\n"
      },
      "credentials": {
        "microsoftSql": {
          "id": "REPLACE_ME",
          "name": "MSSQL readonly"
        }
      }
    },
    {
      "name": "recon 04 cross database refs",
      "type": "n8n-nodes-base.microsoftSql",
      "typeVersion": 1.1,
      "position": [
        880,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "-- [4] 크로스 DB 참조 / cross-database references — 카탈로그가 못 잡는 유일한 경로(Phase 2)\nSELECT DISTINCT referenced_database_name\nFROM sys.sql_expression_dependencies\nWHERE referenced_database_name IS NOT NULL;\n"
      },
      "credentials": {
        "microsoftSql": {
          "id": "REPLACE_ME",
          "name": "MSSQL readonly"
        }
      }
    },
    {
      "name": "recon 05 nested views",
      "type": "n8n-nodes-base.microsoftSql",
      "typeVersion": 1.1,
      "position": [
        1100,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "-- [5] 중첩 뷰 / nested views — lineage 재귀 깊이·규모 가늠\nSELECT COUNT(*) AS view_on_view\nFROM sys.sql_expression_dependencies d\nJOIN sys.objects o ON d.referencing_id = o.object_id AND o.type = 'V'\nJOIN sys.objects r ON d.referenced_id  = r.object_id AND r.type = 'V';\n"
      },
      "credentials": {
        "microsoftSql": {
          "id": "REPLACE_ME",
          "name": "MSSQL readonly"
        }
      }
    },
    {
      "name": "recon 06 lineage dmv smoke",
      "type": "n8n-nodes-base.microsoftSql",
      "typeVersion": 1.1,
      "position": [
        1320,
        0
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "-- [6] lineage DMV 스모크 테스트 / dm_sql_referenced_entities smoke test\n-- 대상 뷰 1개를 골라 즉시 호출 — 권한·호환성 문제를 미리 드러낸다\n-- pick one view and call the DMV inline; surfaces permission issues early\nDECLARE @qname nvarchar(517);\nSELECT TOP 1 @qname = QUOTENAME(s.name) + N'.' + QUOTENAME(v.name)\nFROM sys.views v\nJOIN sys.schemas s ON v.schema_id = s.schema_id\nORDER BY v.object_id;\n\nIF @qname IS NULL\n    SELECT CAST(NULL AS nvarchar(517)) AS smoke_view, 'no views found' AS note;\nELSE\nBEGIN TRY\n    SELECT @qname AS smoke_view,\n           COUNT(*) AS referenced_rows,\n           CAST(NULL AS nvarchar(400)) AS error\n    FROM sys.dm_sql_referenced_entities(@qname, 'OBJECT');\nEND TRY\nBEGIN CATCH\n    SELECT @qname AS smoke_view,\n           CAST(NULL AS int) AS referenced_rows,\n           LEFT(ERROR_MESSAGE(), 400) AS error;\nEND CATCH;\n"
      },
      "credentials": {
        "microsoftSql": {
          "id": "REPLACE_ME",
          "name": "MSSQL readonly"
        }
      }
    },
    {
      "name": "Recon report",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1540,
        0
      ],
      "parameters": {
        "jsCode": "// 정찰 결과 종합 — 판단은 사람이 한다 (결과 보고 → 정지점 16)\n// assemble the recon report; humans judge the results\nconst report = {};\nreport.fk_count = $('recon 01 fk count').all().map(i => i.json);\nreport.object_scale = $('recon 02 object scale').all().map(i => i.json);\nreport.view_definition_permission = $('recon 03 view definition permission').all().map(i => i.json);\nreport.cross_database_refs = $('recon 04 cross database refs').all().map(i => i.json);\nreport.nested_views = $('recon 05 nested views').all().map(i => i.json);\nreport.lineage_dmv_smoke = $('recon 06 lineage dmv smoke').all().map(i => i.json);\nreport.warnings = [];\nconst perm = report.view_definition_permission[0];\nif (perm && Number(perm.blocked) > 0) {\n  report.warnings.push(`VIEW DEFINITION blocked on ${perm.blocked}/${perm.total} views — 권한 필요 (최우선)`);\n}\nconst smoke = report.lineage_dmv_smoke[0];\nif (smoke && smoke.error) {\n  report.warnings.push(`dm_sql_referenced_entities failed on ${smoke.smoke_view}: ${smoke.error}`);\n}\nreturn [{ json: report }];\n"
      }
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "recon 01 fk count",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "recon 01 fk count": {
      "main": [
        [
          {
            "node": "recon 02 object scale",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "recon 02 object scale": {
      "main": [
        [
          {
            "node": "recon 03 view definition permission",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "recon 03 view definition permission": {
      "main": [
        [
          {
            "node": "recon 04 cross database refs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "recon 04 cross database refs": {
      "main": [
        [
          {
            "node": "recon 05 nested views",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "recon 05 nested views": {
      "main": [
        [
          {
            "node": "recon 06 lineage dmv smoke",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "recon 06 lineage dmv smoke": {
      "main": [
        [
          {
            "node": "Recon report",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "notes": "연결 단계 정지점 16 — 실행 결과(Recon report)를 보고 후 W1 진행 여부를 결정한다. [3] blocked > 0 이면 VIEW DEFINITION 권한부터 해결할 것. credentials를 배포 환경에 맞게 설정."
  }
}
