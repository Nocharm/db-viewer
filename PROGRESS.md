# Progress


## Aug 7, 2026
- Task 3: 타입 패밀리 판정. TDD로 `get_type_family(data_type: str) -> str` 구현 완료 — 테스트 9/9 통과

프로젝트 진행 현황 로그. 커밋 직전 갱신한다 (`rules/common/git.md` 규칙).

## 2026-08-26

- **LDAP 로그인 병행 + 개발 서버 배포 설계 확정** (`docs/superpowers/specs/2026-08-26-ldap-login-dev-deploy-design.md`). 지금 로그인은 Keycloak 전용이고 LDAP은 서비스 계정으로 사용자 목록만 읽는 동기화 용도다. 세 갈림길 결정: ① 폴백은 **병행**(사용자가 고름) ② 바인드는 **백엔드 직결**(Keycloak에 의존하면 폴백이 성립하지 않는다) ③ **12시간 단일 토큰, 갱신 없음**. 핵심은 `get_current_user`가 `iss`로 라우팅하되 **두 경로 모두 알고리즘을 고정**하는 것 — 고정하지 않으면 `iss`를 주장하는 다른 알고리즘 토큰이 통과한다. 하류 게이트(화이트리스트·sysadmin)는 손대지 않아도 자동 적용된다.
- **설계 자체 검토에서 결함 2건 정정** — (1) IP축 잠금을 뺐다: 백엔드가 프론트 프록시 뒤라 peer 주소가 항상 프록시 컨테이너다. 두면 한 명의 오타가 전원을 잠그거나 `X-Forwarded-For` 위조로 우회된다. 축은 `login_id` 하나. (2) 개발 스택에서 `NEXT_PUBLIC_KEYCLOAK_*`를 비우는 안이 `makeManager()`의 issuer 존재 가정과 충돌 — 조건부 마운트 확인을 구현 단계 요구사항으로 명시. 그리고 `.env.dev`는 커밋하지 않는다(`.env.dev.example` + gitignore) — 이름으로 커밋 가능한 비밀 파일을 만들면 누군가 채워서 커밋한다.

## 2026-08-25

- **Task 1: 소스 등록부 모델 + 마이그레이션 0015** (`backend/app/models/sources.py`, `backend/alembic/versions/0015_data_sources.py`). `DataSource` 모델(17 컬럼) + 사내 MSSQL 시드 행(id=1, is_managed) — TDD 테스트 2/2(마이그레이션 시드, unique 제약), 파이썬 타입 힌트·import 3그룹·CheckConstraint 엔진/access_mode. 테스트 336→338 passed, ruff clean.
- **Task 2: 스냅샷에 소스 축 추가 + 마이그레이션 0016** (`backend/alembic/versions/0016_snapshot_source.py`, `backend/app/models/catalog.py`, `backend/app/api/objects.py`, `backend/app/api/ingest.py`, `backend/app/schemas/ingest.py`). `Snapshot.data_source_id`(NOT NULL FK) — nullable 추가→백필→NOT NULL 순서(`batch_alter_table`, SQLite 대응)로 기존 스냅샷은 전부 사내 MSSQL(id=1)로 귀속. `resolve_snapshot(source_id=None)`은 미지정 시 기본 소스로 해석해 기존 호출자 하위호환 유지, `CatalogPayload.data_source_id`는 옵셔널(n8n 구 계약은 미전송→기본 소스). 드리프트 감지 테스트(`test_migration_matches_models`)가 모델에 빠진 인덱스 선언을 실제로 잡아냄 — `Index("ix_snapshots_source_status", ...)`를 `__table_args__`에 추가해 해결. 부수 수정: `Snapshot(...)`을 직접 생성하던 기존 테스트 3곳(`test_migrations.py`, `test_ai.py`, `test_collect.py`)에 `data_source_id` 백필, 에러 메시지 변경("no ready snapshot" → "no ready snapshot for this source")에 맞춰 `test_query_api.py` 단언 갱신. 마이그레이션 왕복(downgrade 0014→upgrade head) + 실데이터 백필 별도 검증 통과. 테스트 338→339 passed, ruff clean.
- **Task 3: 노출 정책을 소스별로 분리 + 마이그레이션 0017** (`backend/alembic/versions/0017_policy_by_source.py`, `backend/app/models/{preview_policy,categories}.py`, `backend/app/services/preview_policy.py`, `backend/app/api/{objects,admin,categories,validate,join_preview}.py`). `preview_allowlist`·`schema_categories` PK를 `(data_source_id, …)`로 확장 — 소스가 늘면 A서비스 `public` 허용이 B서비스 `public`까지 여는 **보안 결함**을 막는 게 목적이다. 미리보기는 실값이 화면으로 나가는 유일한 경로라 여기가 정책의 마지막 방어선. SQLite가 ALTER로 PK를 못 바꿔 `create → INSERT SELECT → drop → rename`으로 썼고(0012와 같은 관용), 기존 행은 전부 사내 MSSQL(id=1)로 백필. `is_preview_allowed(db, source_id, schema)`가 소스를 필수 인자로 받게 바뀌어 **호출부를 안 고치면 컴파일이 아니라 런타임에 터지는** 구조 — 브리프가 열거한 4곳 외에 `validate.py:/preview`·`join_preview.py:/preview` 두 곳을 grep으로 더 찾아 고쳤다(검증기가 사내 MSSQL 실행기 하나뿐이라 기본 소스로 판정). 하위호환은 "소스 미지정 = 기본 소스" 기본값으로 유지해 프론트엔드 무변경. 마이그레이션 왕복(head→0016→head, 소스 2건 실데이터)으로 복사·복원 검증. 테스트 339→340 passed, ruff clean.
- **Task 3 리뷰 반영: 조인 경로의 교차소스 판정 제거 + 감사 로그에 소스 기록** (`backend/app/api/{validate,join_preview,admin}.py`). 리뷰가 Critical로 잡은 것 — 앞 커밋이 `validate.py`·`join_preview.py`에서 소스를 `MANAGED_MSSQL_SOURCE_ID`로 하드코딩해, **소스 1에서 `public`을 허용해 두면 소스 2의 `public` 객체가 조인 미리보기 게이트를 통과**했다. PK를 바꾼 이유 그 자체가 세 경로 중 두 곳에 남아 있던 셈. 유예 근거("`resolve_column_ref` 시그니처를 바꿔야 하고 6개 모듈에 파급된다")가 틀렸다 — 그 함수는 이미 `CatalogColumn`을 돌려주므로 호출부가 `col.object_id`→객체→스냅샷으로 **시그니처 변경 없이** 소스를 복원할 수 있다. `resolve_column_source_id(db, col)` 헬퍼를 `validate.py`에 두고(두 번째 사용처가 실제로 생겼다) `join_preview.py`는 `involved`를 `qname → (소스, 스키마)`로 바꿔 테이블별로 판정한다. 함께: 허용 키가 `(소스, 스키마)`가 되면서 `detail=schema`만 남기던 감사 로그가 "어느 DB를 열었나"에 답할 수 없게 된 포렌식 공백을 `source={id} {schema}`로 메웠다. 회귀 방지로 실값이 나가는 세 경로(테이블 미리보기·2-way·N-way)에 대해 같은 픽스처를 두 소스에 적재해 스키마명을 겹치게 만든 뒤 "허용한 소스만 열린다"를 엔드포인트 레벨로 고정 — 수정 전 코드에서 이 테스트들이 실제로 실패(2-way는 200 누출, N-way는 게이트 통과)하는 것을 확인했다. 테스트 340→343 passed, ruff clean.
- **멀티 소스 DB 조회 설계 확정** (`docs/superpowers/specs/2026-08-25-multi-source-db-design.md`). 같은 71번 서버의 다른 도커 서비스 DB(PostgreSQL/SQLite)를 db-viewer에서 조회하기 위한 설계. 네 갈림길 결정: ① 범위는 탐색·미리보기·FK ERD까지 — 뷰 lineage·관계 온디맨드 발견·AI는 "FK 13개짜리 레거시 MSSQL"용 기계라 비-MSSQL에는 안 얹는다, ② 신규 소스만 백엔드 직결(psycopg·stdlib sqlite3 — 새 드라이버 0), 사내 MSSQL은 n8n 경유 유지, ③ 소스 등록은 관리자 UI + Fernet 암호화 저장(키 없으면 등록 자체 503), ④ 네트워크는 **B′ — 서비스당 전용 브리지 네트워크**. 핵심은 기존 `CatalogPayload` ingest 계약을 엔진별 수집기가 채우게 해 하류(lineage·ERD·검색·정책)를 통째로 재사용하는 것. 필수 변경 1건: `preview_allowlist` PK가 `schema` 단독이라 소스가 늘면 A서비스 `public` 허용이 B서비스 `public`까지 여는 사고 — `(data_source_id, schema)`로 확장한다.
- **멀티 소스 구현 계획 + 담당자 전달 문서** (`docs/superpowers/plans/2026-08-25-multi-source-db.md`, `docs/handoff/service-owner-prompt.md`). 15개 태스크 5페이즈 — 페이즈1(소스 축 도입)은 화면 무변경이 성공 기준이다. 계획 작성 중 스펙 오류 1건 정정: PG `pg_class.oid`는 unsigned 32bit라 `objects.object_id`(int4)를 넘길 수 있어 SQLite와 같은 **스냅샷 내 일련번호**로 사상한다. 발견한 단순화: PG/SQLite는 미리보기 용도에서 문법이 같고(`"`인용·LIMIT·CAST AS TEXT·LIKE ESCAPE) SQLAlchemy `text()` named 파라미터를 쓰면 paramstyle 차이도 없어 **SQL 빌더가 하나로 족하다**. 담당자 문서는 ①값표 → ②볼륨 사전확인(없으면 중단) → ③Claude Code 프롬프트 → ④읽기전용 계정 → ⑤보고양식 구성.
- **네트워크 대안 검토 기록** — A안(대상 기존 네트워크에 db-viewer가 합류)은 대상 무수정이지만 흔한 컨테이너명(`postgres`/`db`) DNS 충돌 + 대상 `compose down` 시 db-viewer 기동 불가. B안(공용 네트워크 1개)은 후자를 풀지만 서비스 간 격리를 깬다. B′는 대상 수정량이 B와 같으면서 둘 다 없앤다. 기존 subnet(172.36~46)·db-viewer(172.48)은 무변경, 신규는 172.50.x.0/24.
- **Task 4: Fernet 기반 소스 접속 비밀번호 암호화 + 설정** (`backend/app/sources/__init__.py`, `backend/app/sources/crypto.py`, `backend/app/config.py`, `backend/requirements.txt`, `backend/tests/test_source_crypto.py`). `is_crypto_configured()` / `encrypt_secret(plain)` / `decrypt_secret(token)` 3개 함수 + `CryptoNotConfigured` 예외. 핵심은 **키가 없으면 조용히 평문을 저장하지 않고 명시적으로 거부한다** — 발생 지점(`_get_cipher`)에서 RuntimeError 상속 예외를 던져 호출부가 리스너를 등록할 때까지 거부 상태가 유지된다. 복호화 실패(`InvalidToken`)도 거부(키 교체 신호) — 빈 값이나 기본값을 돌려주지 않는다. Settings에 `source_secret_key`(ENV, Fernet urlsafe base64 32B) / `source_connect_timeout`(5s, 튜닝) / `source_query_timeout`(15s, 튜닝) 세 필드 추가. 의존성: `cryptography==50.0.0`(브리프 지시 44.0.0이 아니라 현재 설치 버전으로 핀) — pyjwt[crypto]로 이미 설치되지만 직접 쓰므로 명시. TDD: 테스트 먼저 작성(ModuleNotFoundError 확인) → 구현 → 345 passed(+2), ruff 클린.
- **Task 5: 소스 레지스트리 + 연결 캐시** (`backend/app/sources/registry.py`, `backend/app/sources/connection.py`, `backend/tests/test_source_registry.py`). 두 모듈로 책임 분리 — registry는 소스 조회(`get_source`, `list_sources`) 및 URL 조립(`build_sa_url`), connection은 엔진 생명주기 관리(`get_sa_engine`, `clear_sa_engine`, `_engines` 캐시). URL 인코딩이 핵심 — 비밀번호에 `@`나 `/`가 들어가면 `urllib.parse.quote(..., safe="")` 없이는 파싱이 깨진다. 캐시는 FastAPI 스레드풀 환경에서 race condition 방지를 위해 `threading.Lock`으로 보호(`_engines_lock`) — check→create→store 구간과 pop 구간을 감싼다(double-check 패턴). Postgres는 `pool_size=2, max_overflow=1` + 연결/문장 타임아웃, SQLite는 `mode=ro` 읽기전용 URI + `check_same_thread=False`. TDD: 테스트 먼저(postgres URL 인코딩, sqlite 읽기전용, n8n 거부) → RED 확인 → 구현 → 345→349 passed(+4 tests: URL 인코딩·readonly·n8n 거부·동시성), 동시성 테스트는 create_engine 패치+time.sleep+threading.Barrier로 스레드 겹침을 강제 → 락 없으면 100% 실패·락 있으면 100% 통과, ruff 클린.
- **Task 6: 미리보기 SQL 빌더 (순수 함수)** (`backend/app/sources/preview_sql.py`, `backend/tests/test_preview_sql.py`). 보안 경계 — 식별자는 화이트리스트(`allowed_columns`)만 통과, 값은 전부 바인드 파라미터로 나간다. 여섯 연산자(`eq`, `neq`, `contains`, `not_contains`, `is_null`, `not_null`) 지원, AND 결합. PG/SQLite는 이 용도에서 문법이 동등(`"` 인용·LIMIT·CAST AS TEXT·LIKE ESCAPE)하고 SQLAlchemy `text()` named 파라미터(`:p0`)를 쓰면 paramstyle 차이가 없어 엔진별 분기가 불필요 — 빌더는 하나다. 핵심 함수: `build_preview_sql(schema, table, column_names, filters, limit, allowed_columns)` → `(sql, params)` 튜플, `quote_ident(name)` 식별자 인용(내부 따옴표 escaping), `escape_like(value)` LIKE 메타문자 escape(`%`, `_`, `\`). 부정 연산자(`neq`, `not_contains`)는 `col IS NULL OR` 절 추가 — fixture가 NULL을 빈 문자열로 취급하므로 화면 동작에 맞춤. 미지정/해석 불가 식별자는 `UnknownIdentifier` ValueError 상속 예외로 거부(침묵하지 않기). TDD: 10개 테스트(unfiltered select, contains 파라미터화, LIKE escape, negative ops NULL 포함, multiple AND 결합, 식별자 검증 3건 parametrize) — ModuleNotFoundError RED → 구현 → GREEN 모두 통과. 테스트 349→359 passed(+10), ruff 클린.
  - **재리뷰 반영: 바인드 파라미터 타입 안전성 수정** — 라인 44의 `value = "" if raw is None else raw`를 `value = "" if raw is None else str(raw)`로 수정. 이전 버전이 세 문제를 만들었다: ① falsy 값(0, False) 붕괴 → `escape_like`가 정수에 `.replace()` 호출해 `AttributeError` ② `contains`/`not_contains`의 LIKE escape 불안정 ③ SQL `UPPER(:pN)`은 텍스트 파라미터를 전제하는데 정수/불린이 들어가면 PostgreSQL에서 `UPPER(integer)` 런타임 에러. `str()` 변환으로 세 가지가 한 번에 해결: falsy 보존(`0` → `"0"`), escape_like 안전(`int` 불가 → `str` 필수), 바인드 파라미터 항상 텍스트(`UPPER` 양 엔진 호환). 현재 API는 문자열만 보내므로 no-op이지만 미래 호출자 대비 필수. 커버링 테스트 3개: numeric 0이 문자열로 변환, contains에서 `AttributeError` 없음, `None` 값이 빈 문자열로 변환. TDD: RED 2건(numeric 0 안 됨, contains crash) → 수정 → GREEN 3건. 테스트 361→363 passed(+2), ruff 클린.
- **Task 7: `DirectTablePreview` 실행기 + 팩토리 소스 라우팅** (`backend/app/sources/direct_preview.py`, `backend/app/adapters/__init__.py`, `backend/app/api/objects.py`, `backend/tests/test_direct_preview.py`). 이 태스크에서 처음으로 백엔드가 실제 외부 DB에 붙어 값 데이터를 읽는다 — `DirectTablePreview.rows`는 `build_preview_sql`(Task 6)로 SQL을 조립하고 `get_sa_engine`(Task 5)의 엔진으로 실행만 한다(조립 로직 중복 없음), `N8nTablePreview.rows`와 동일 시그니처라 `api/objects.py` 호출부가 실행기 종류를 몰라도 된다. `create_table_preview(settings, source=None)`은 `source.access_mode == "direct"`일 때만 새 경로로 가고 `source=None`(기존 호출자) 동작은 그대로 — direct 소스는 `SOURCE_MODE`와 무관하게 항상 실데이터(["source"] 응답 필드가 `"live"`)라 화면이 "원본이 0행"과 "실행기가 안 붙었다"를 여전히 구분할 수 있다. `get_object_preview`는 `get_source(db, ...)`로 소스 객체를 얻어 팩토리·`is_preview_allowed`·감사 로그에 일관되게 넘기도록 다듬었다(Task 3이 이미 소스별 판정으로 바꿔둔 토대 위에 소스 *객체*를 배선). `preview.rows` 호출을 `SQLAlchemyError` 한 겹으로 감싸 소스 접속 실패를 502로 격리 — 자격증명은 예외 메시지·로그에 싣지 않고 소스명·객체명까지만(`host`/비밀번호는 SQLAlchemy 연결 오류 문자열에 애초에 없다). 회귀 1건 발견·수정: `test_preview_policy_by_source.py`의 가짜 postgres 소스(`host="h"`, 실제로는 연결 불가)를 여는 테스트가 이전엔 factory가 `source`를 무시해 200(픽스처)이 났는데, 이제 진짜 연결을 시도해 502가 난다 — 그 테스트의 의도(allowlist 게이트가 더는 막지 않음)에 맞춰 `== 200`을 `!= 403`으로 정정(같은 파일의 join 계열 두 테스트와 동일 패턴). TDD: 실제 SQLite 파일 왕복 4개 테스트(무필터·대소문자 무시 `eq`·NULL 포함 `not_contains`·limit) + 502 격리 1개 — 502 테스트는 `tests/test_source_scoped_queries.py`(Task 12에서 생성 예정, 아직 없음)를 참조하는 브리프 결함을 피해 소스·스냅샷·객체·컬럼·allowlist를 테스트 안에서 직접 시드. `ModuleNotFoundError` RED 확인 → 구현 → GREEN. 테스트 363→368 passed(+5), ruff 클린.
  - **재리뷰 반영: 502 격리 범위 확장 + 드라이버 원문 노출 차단 + 조립 버그 오분류 방지** (`backend/app/api/objects.py`, `backend/app/sources/direct_preview.py`, `backend/tests/{test_direct_preview,test_preview_policy_by_source}.py`). 리뷰가 지적한 7건 — 전부 같은 두 파일. 핵심 셋: ① `create_table_preview` 호출(엔진 생성·자격증명 복호화 단계)이 502 가드 밖에 있어 `CryptoNotConfigured`(키 교체 시나리오)·`UnsupportedSource`가 500으로 샜다 — `preview.rows`와 한 `try` 블록으로 합치고 이 둘은 503(소스 장애가 아니라 이쪽 설정 문제)으로 분리했다. ② 502 응답 컨텍스트의 `str(e)[:300]`가 드라이버 원문(예: postgres 인증 실패 메시지에 실린 접속 계정명)을 그대로 클라이언트에 흘렸다 — `error_type`만 남기고 `logger.warning`에 `exc_info=True`를 추가해 상세는 로그로만. ③ 502를 `SQLAlchemyError` 전체로 잡아 `CompileError` 같은 우리 쪽 조립 버그까지 소스 탓으로 위장했다 — `DBAPIError`로 좁혀 조립 버그는 그대로 500으로 드러나게 했다. 부수 수정: `Decimal→float`가 정밀도 손실·`NUMERIC 'NaN'` 500 크래시를 냈던 것을 `str()`로 교체, 테스트 픽스처가 `build_sa_url` 문자열을 손으로 복제하던 것을 `get_sa_engine` 실호출로 정정(단 `source.id` 자동증가가 프로세스 전역 엔진 캐시와 충돌해 `clear_sa_engine` 가드를 테스트마다 추가), "다른 소스는 멀쩡하다" 단언 누락 보완, 이전에 완화했던 `!= 403`을 리뷰어가 502 결정론(psycopg 설치돼 있어 host 해석 실패가 항상 DBAPIError)을 직접 확인한 뒤 `== 502`로 되돌렸다. 새 테스트 2개(`SOURCE_SECRET_KEY` 제거 시 503, fault-injection으로 조립 버그가 502로 안 새는지) + 기존 3개 보강(502 테스트에 정상 소스 동시 조회 추가). 테스트 368→370 passed, ruff 클린.
  - **2차 재리뷰 반영: `DBAPIError` 좁히기가 너무 좁았다(풀 타임아웃 누락) + 조립 버그 테스트가 실제로는 아무것도 증명 못 했다** (`backend/app/api/objects.py`, `backend/tests/test_direct_preview.py`). ① `sqlalchemy.exc.TimeoutError`는 `DBAPIError`가 아니라 `SQLAlchemyError` 직계다 — `connection.py`가 postgres에 걸어 둔 작은 풀(`pool_size=2, max_overflow=1`)이 동시 요청으로 소진되면 풀 체크아웃이 이 예외로 떨어지는데, 직전 라운드의 `except DBAPIError`가 이걸 놓쳐 정확히 그 라운드가 없애려던 "소스가 힘들어하는" 상황을 다시 500으로 새게 만들었다(자체 회귀) — `except (DBAPIError, SATimeoutError, DisconnectionError)`로 확장(`TimeoutError`는 빌트인과 이름이 겹쳐 `as SATimeoutError`로 별칭). ② 조립 버그 테스트가 주입하던 `UnknownIdentifier`는 bare `ValueError`라 `SQLAlchemyError`였던 적이 없어 좁히기 전 코드에서도 이미 안 잡혔다 — 즉 그 좁히기에 대해 테스트가 아무것도 증명하지 않았다. `sqlalchemy.exc.CompileError`(SQLAlchemyError 하위·DBAPIError 아님)로 교체하고, fault-injection 지점도 `create_table_preview` 자체가 아니라 그게 돌려주는 실행기의 `.rows()` 안으로 옮겼다 — 전자를 패치하면 리뷰 이전 코드(두 호출이 별개 `try`였던 시절)에서는 애초에 그 `try`가 `create_table_preview`를 안 감쌌으므로 좁히기와 무관하게 항상 통과해, 어느 커밋을 대상으로 돌려도 회귀를 못 잡는 가짜 테스트가 됐을 것이다. 세 가지를 실측으로 관찰: (a) 새 `CompileError` 테스트를 `except SQLAlchemyError`였던 구코드(Task 7 원본 커밋)에 대고 돌려 실패 확인, (b) 새 `TimeoutError` 테스트를 `except DBAPIError` 단독이던 직전 라운드 코드에 대고 돌려 `sqlalchemy.exc.TimeoutError`가 미처리로 새는 것을 실제로 관찰, (c) 최종 코드에서 둘 다 통과 — 매번 `git show <commit>:path > path`로 파일만 바꿔치기해 그 커밋의 실제 동작을 그대로 재현했다. 테스트 370→371 passed(순증 +1: 기존 조립버그 테스트 1개 교체 + 신규 1개), ruff 클린.
- **Task 8: PostgreSQL 수집기** (`backend/app/sources/pg_collector.py`, `backend/tests/test_pg_collector.py`). Phase 3(수집)의 첫 태스크 — PG 카탈로그를 읽어 기존 `CatalogPayload`를 그대로 채운다(새 자료구조 없음, 하류는 엔진을 모른 채 동작). 핵심은 `object_id`에 `pg_class.oid`를 바로 안 쓴 것 — oid는 unsigned 32bit(최대 4,294,967,295)라 `objects.object_id`(int4, 최대 2,147,483,647)를 넘길 수 있어, 계약이 요구하는 스냅샷 내 유일성만 채우는 `map_oids_to_object_ids`로 수집 중에만 oid→일련번호 사전을 든다. `WITH ORDINALITY`로 `conkey`/`confkey` 순서를 보존해 복합 PK/FK의 컬럼 순서가 뒤집히지 않게 했고, 제외 스키마(시스템 카탈로그·toast·temp)를 가리키는 FK는 `src_oid`/`tgt_oid`가 `oid_map`에 없으면 버린다(없는 객체 참조 시 적재가 터짐). `max_length`는 varchar/char 선언 길이만 의미 있고 나머지는 MSSQL 관례대로 -1, `reltuples` 음수(ANALYZE 전)는 NULL로 보낸다. 뷰 정의는 `pg_get_viewdef`로 저장만 하고 T-SQL 파서에는 안 먹인다(그 게이트는 Task 10 몫). TDD: `ModuleNotFoundError` RED 확인 → 구현 → GREEN. 로컬 Docker로 실제 `postgres:16-alpine` 컨테이너를 띄워 통합 테스트까지 실행(스키마 생성→수집→검증→probe 스키마 삭제 확인, 컨테이너는 검증 후 제거) — 매핑 단위 테스트 1개 + 실PG 왕복 1개 모두 통과. 테스트 371→372 passed, ruff clean.
  - **리뷰 반영: 통합 테스트 정리를 픽스처 teardown으로, 복합 FK/PK 컬럼 순서 회귀 테스트 추가** (`backend/tests/test_pg_collector.py`). 리뷰가 지적한 2건 — 둘 다 브리프 원문을 그대로 옮긴 결과라 코드 자체(SQL 4종·oid 매핑·교차스키마 FK 배제)는 리뷰에서 전부 통과했다. ① 프로브 스키마 `DROP`이 여섯 assertion 뒤의 마지막 문장이라 assertion이 하나라도 깨지면 `TEST_POSTGRES_URL`이 가리키는 실DB에 `collect_probe`가 남는 구조였다 — 생성/삭제를 `probe_catalog` pytest 픽스처(`yield` 뒤 `DROP SCHEMA`)로 옮겨 teardown이 테스트 실패에도 돌게 했다. ② 복합키 컬럼 순서(`WITH ORDINALITY` + `ARRAY(...) 내부 ORDER BY u.ord`)가 이 태스크가 막으려던 조용한 실패의 핵심인데 정작 그걸 지키는 회귀 테스트가 없었다 — `parent2(x,y PK)`/`child2((b,a)→(y,x) FK, 자연 컬럼 순서와 반대)`를 프로브 스키마에 추가하고 FK·PK 페어를 순서까지 단언하는 테스트를 새로 썼다. 검증 3종을 실PG(Docker `postgres:16-alpine`)로 직접 확인: (a) 신규 테스트 3개 모두 통과, (b) assertion을 일부러 틀리게 해 테스트를 실패시킨 뒤 `collect_probe`가 DB에 안 남는 것 확인(픽스처 teardown이 실패 경로에서도 도는 증거) → 단언 원복, (c) `_KEYS_SQL`/`_FKS_SQL`의 내부 `ORDER BY u.ord`를 임시로 제거해 신규 복합 FK 테스트가 5회 연속 결정론적으로 실패하는 것 관찰(자연 attnum 순서 `[('a','x'),('b','y')]`로 뒤집힘) → 원복해 GREEN 확인. 테스트 372 passed(2 skipped — 통합 테스트 2개는 `TEST_POSTGRES_URL` 미설정 시 스킵), ruff clean.
- **Task 9: SQLite 수집기** (`backend/app/sources/sqlite_collector.py`, `backend/tests/test_sqlite_collector.py`). Task 8과 같은 `CatalogPayload` 계약을 SQLite `PRAGMA`로 채운다 — 스키마 개념이 없어 `schema="main"` 고정, `object_id`는 PG처럼 스냅샷 내 일련번호, 길이 제약을 안 두는 엔진이라 `max_length=-1` 고정. 핵심은 암묵 FK 해석: `PRAGMA foreign_key_list`를 `id`로 묶고 각 그룹을 `seq`로 정렬한 뒤(`_group_foreign_keys`), `to`가 NULL이면 대상 PK의 같은 자리 컬럼으로 해석한다(`_resolve_fk_pairs`) — 대상 테이블이 카탈로그에 없거나 자리 하나라도 못 풀면 그 FK 전체를 버린다(부분 페어를 남기지 않음). 브리프 코드를 줄 단위로 검토했으나 그룹핑·정렬·해석 로직 자체에 결함은 못 찾았다 — 대신 헬퍼 3개(`_collect_primary_key`/`_group_foreign_keys`/`_resolve_fk_pairs`)로 나눠 가독성을 올리고 bare `list` 타입힌트를 `list[dict[str, Any]]`로 좁혔다. 뷰의 `row_count`는 PG 수집기와 같은 이유로 `None`(COUNT를 안 돌림) — 뷰는 카디널리티가 조회 시점마다 재계산이라 시스템 카탈로그 관례상 저장 대상이 아니다. TDD로 처음부터 복합 FK 순서(자연 컬럼순 x,y/a,b와 반대인 (b,a)→(y,x)) 회귀 테스트를 포함해 8개 작성 — `ModuleNotFoundError` RED 확인 → 구현 → GREEN. `seq` 정렬을 실제로 제거해 회귀 테스트가 깨지는지 확인한 결과 **결정론적으로 깨지지 않았다** — `PRAGMA foreign_key_list`가 이 SQLite 버전에서 이미 seq 순서로 행을 반환해 정렬 유무와 무관하게 통과했다(PG의 `unnest`처럼 순서 보장이 없는 집합 연산이 아니라 정렬 없이도 우연히 맞는 경우). 정렬 코드 자체는 SQLite 문서가 반환 순서를 보장하지 않으므로 방어적으로 유지. 인프라 불필요 — 전부 `tmp_path` 실파일 왕복, skip 0개. 테스트 372→380 passed(+8), ruff clean.
  - **리뷰 반영: 커버리지 구멍 2건 + PG 뷰 row_count 일관성** (`backend/app/sources/{sqlite_collector,pg_collector}.py`, `backend/tests/{test_sqlite_collector,test_pg_collector}.py`). 리뷰가 지적한 3건 — 구현 자체(암묵 FK 위치 기반 PK 해석·복합 그룹핑·폐기 규칙)는 전부 맞다고 확인됐다. ① `test_resolves_implicit_fk_target_to_primary_key`의 픽스처가 `REFERENCES parent(id)`(컬럼 명시)라 `to`가 실제로는 NULL이 되지 않아 이름이 주장하는 폴백 경로에 한 번도 안 닿았다 — 브리프 자체의 결함. `REFERENCES parent`(컬럼 목록 생략)로 고쳐 진짜 암묵 참조를 태우고, 실제 `PRAGMA foreign_key_list`가 이 DDL에 `to=NULL`을 돌려주는 것을 sqlite3로 직접 확인했다. ② `ordinal`이 `table_info.cid`(0-base)를 그대로 써 `pg_collector`의 `attnum`(1-base, MSSQL `column_id`와 같은 관례)과 어긋났다 — `+1`로 통일하고 `id`(cid=0→ordinal=1)·`label`(cid=1→ordinal=2) 단언 추가. ③ PG 소스의 뷰 `row_count`가 `reltuples` 부호에만 의존해 뷰마다 다르게 나올 수 있는 구조라 `relkind IN ('v','m')`일 때 무조건 NULL로 명시했다 — 다만 리뷰가 근거로 든 "일반 뷰는 reltuples=0"은 **직접 확인 결과 이 PG 16.15 환경에서 사실이 아니었다**(신규 뷰는 -1, `ANALYZE`가 뷰를 아예 건너뛰어 영구히 -1 → 기존 `< 0` 분기만으로도 이미 NULL이 나갔다). matview는 REFRESH+ANALYZE 후 실제 카디널리티를 갖는 것도 직접 확인(3행 삽입 후 3 관찰) — 그런데도 리뷰 결정대로 `v`·`m` 모두 NULL로 통일했다: matview도 "언제 REFRESH됐는지" 이 컬럼만으론 알 수 없는 신뢰 불가 스냅샷이라는 점에서 결이 같고, 내부 표현(-1 기본값)에 기대지 않고 명시하는 편이 PG 버전에 안 흔들린다고 판단해서다. 코드 주석은 실측한 사실(reltuples=-1, ANALYZE가 뷰 스킵)대로 정정해 리뷰의 부정확한 전제를 그대로 옮기지 않았다. 세 항목 모두 실측: sqlite 8개 전체 GREEN, ordinal 픽스는 되돌려 `0 == 1` AssertionError로 RED 관찰 후 복원, PG는 `postgres:16-alpine` 컨테이너를 새로 띄워 3개 전체 GREEN(신규 `view.row_count is None` 단언 포함) 확인 후 프로브 스키마 빈 것 확인·컨테이너 정리(2회 반복 — 주석 정정 전후 모두 확인). 전체 스위트 380 passed(2 skipped, 불변), ruff clean.
  - **재리뷰 반영: matview row_count 되살리기 — 코디네이터 지시 근거를 재검증해 정정** (`backend/app/sources/pg_collector.py`, `backend/tests/test_pg_collector.py`). 앞 라운드에서 뷰·matview를 묶어 무조건 NULL 처리했던 결정을 matview만 되돌렸다 — `relkind IN ('v','m')`→`relkind = 'v'`로 좁혀 matview는 테이블과 같은 `reltuples` 분기(미분석 시 NULL, 분석되면 실값)로 되돌아간다. `type`(계속 `'view'`)과 `pg_get_viewdef` 게이트(계속 `('v','m')`)는 무변경. 지시에 실린 근거("CREATE/REFRESH MATERIALIZED VIEW가 CLUSTER/VACUUM FULL과 같은 heap-rewrite로 reltuples를 즉시 정확값으로 갱신한다, ANALYZE 불필요")를 실측으로 검증하니 **PG 16.15에서 사실이 아니었다** — 1000행 matview를 `WITH DATA`로 만들거나 명시적으로 `REFRESH`해도 `reltuples`는 -1(미분석) 그대로였고, `ANALYZE`를 직접 돌려야만 정확값(1000)이 됐다. 다만 결정 자체(matview를 테이블과 같은 취급으로 되돌리는 것)는 다른 근거로 여전히 타당하다고 판단해 그대로 적용했다 — matview는 실제 heap이 있어 ANALYZE가 통하는데(일반 뷰는 "cannot analyze non-tables"로 거부) 반해, "분석 전엔 NULL·분석되면 실값"이라는 대우가 테이블과 matview에 동일하게 적용되는 게 이 값의 자연스러운 의미이기 때문. 코드 주석·PG 통합 테스트를 정정된 근거로 다시 썼다: `probe_catalog` 픽스처에 `mv_child`(matview, `child`의 2행을 `WITH DATA`로 물질화 후 명시적 `ANALYZE`)를 추가하고, 한 테스트 안에서 `v_child.row_count is None`과 `mv_child.row_count == 2`가 함께 성립하는지 단언. `ANALYZE` 호출을 임시로 빼 `row_count is not None`이 실제로 깨지는 것(`AssertionError: assert None is not None`)을 관찰해 그 호출이 장식이 아님을 확인 후 복원. 실PG(`postgres:16-alpine`) 컨테이너로 3개 전체 GREEN, 프로브 스키마 빈 것 확인·컨테이너 정리. 전체 스위트 380 passed(2 skipped, 불변), ruff clean. 이걸로 이 리뷰 체인에서 재확인된 사실 오류가 둘("일반 뷰 reltuples=0", "matview heap-rewrite가 즉시 정확값 갱신") — 둘 다 구현 담당자가 직접 psql로 반증해 코디네이터가 수용했다.
- **Task 10: `DirectCollectRunner` + Phase 2 게이트** (`backend/app/sources/direct_runner.py`, `backend/app/adapters/__init__.py`, `backend/app/api/{collect,ingest}.py`, `backend/tests/{test_direct_collect,test_collect}.py`). Phase 3(수집)의 마지막 태스크 — Task 8/9 수집기를 잡 흐름에 배선한다. 브리프 결함 정정: 원안대로 `run_view_deps`가 스냅샷을 ready로 넘기게 두면, direct 소스는 뷰 의존 단계가 의미 없어(Phase 2는 `engine=='mssql'`에서만 돈다) 사용자가 [1단계]만 눌러도 스냅샷이 영원히 `collecting`에 머문다 — `run_catalog`가 적재 직후 스스로 스냅샷을 `ready`로, 잡을 완료 상태로 마감하고 `run_view_deps`는 이미 끝난 상태를 재확인만 하는 no-op으로 뒀다. `CollectJob.stage` CHECK 제약(`catalog_running/catalog_done/deps_running/ready/failed`)에 `'done'`이 없어 브리프 테스트가 요구한 종단값도 `'ready'`로 정정(기존 n8n 체인의 종단값과 동일). `ingest_view_deps`의 `run_phase2` 호출은 `snapshot.data_source_id`의 엔진이 `mssql`일 때만 돌게 게이트 — T-SQL 파서(sqlglot dialect=tsql)에 PG/SQLite DDL을 먹이면 무의미한 실패만 쌓인다. `create_collect_runner(settings, session_factory, source=None)`에 소스 인자 추가(기존 2-인자 호출 하위호환), direct 소스면 `DirectCollectRunner`로 라우팅 — `create_table_preview`와 같은 관용. `api/collect.py`: 기존 `get_collect_runner` DI(테스트 오버라이드 지점)는 그대로 두고 `get_collect_runner_for(source_id, db, session_factory)`를 추가해 `TriggerRequest`/`StepRequest.source_id`가 있을 때만 우회 라우팅. 구현 중 발견한 버그 2건, 둘 다 재현 후 수정: ① `get_collect_runner_for`가 요청 세션 `db`에서 읽은 `DataSource`를 그대로 넘기면, 배경 작업이 실행될 즈음(요청 세션이 이미 commit·close됨) 속성 접근이 "not bound to a Session"으로 죽는다 — `db.expunge(source)`로 해결. ② `_run_full`의 대기 루프가 `catalog_done`만 종료 조건으로 봐서, direct 소스가 `run_catalog` 한 번으로 곧장 `ready`가 되면 그 값을 15분간 기다리다 오탐으로 `failed`를 덮어쓴다 — 종료 조건에 `ready`도 추가(수정을 되돌리고 monkeypatch로 타임아웃을 1초로 줄인 회귀 테스트가 실제로 실패하는 것을 확인한 뒤 복원). TDD: `ModuleNotFoundError` RED 확인 → 구현 → 직결 SQLite 파일 왕복(스냅샷 `ready` 단언 포함) + Phase 2 게이트(비-mssql에서 `parse_status` NULL·counts에 phase2 키 없음을 직접 확인, "에러 없음"이 아니라) + API `source_id` 라우팅 2건(카탈로그 트리거, full 체인 무정지) 모두 GREEN. 테스트 간 소스 id 충돌도 발견 — 엔진 캐시(`app.sources.connection._engines`)가 프로세스 전역이라 서로 다른 테스트의 자동증가 id가 겹치면 이전 테스트가 지운 tmp 파일을 오독한다(전체 스위트에서 실제로 재현) — `test_direct_preview.py`와 같은 관용으로 소스 등록 전후 `clear_sa_engine` 호출. 기존 `tests/test_collect.py` 13개 전부 무변경 통과 확인. 테스트 380→385 passed(+5), ruff clean.
  - **리뷰 반영: 2단계 트리거의 direct 소스 거짓 409, run_view_deps의 무조건 덮어쓰기** (`backend/app/api/collect.py`, `backend/app/sources/direct_runner.py`, `backend/tests/{test_collect,test_direct_collect}.py`). 리뷰가 지적한 2건 — 여섯 질문(스냅샷 ready 단언, Phase 2 게이트 행동 검증, 시그니처 파급, 순수 추가 diff, 900초 미소진, 두 버그 판정)은 전부 통과, 이 2건만 Important/Minor로 남았다. ① `trigger_view_deps_step`의 `job.stage != "catalog_done"` 가드는 direct 소스 잡을 절대 통과 못 한다 — `DirectCollectRunner.run_catalog`가 `catalog_running`→`ready`로 곧장 가 `catalog_done`을 한 번도 안 거치므로, 이미 완료된 잡에 "카탈로그가 안 끝났다"는 **거짓 409**를 준다. 지금은 이 엔드포인트를 direct 소스로 부르는 곳이 없어 안 드러나지만 Task 11~14가 UI를 배선하면 그대로 밟는다. `_is_direct_source_job(db, job)` 헬퍼(스냅샷→소스 조회, `access_mode == 'direct'` 판정)를 추가해 `job.stage == 'ready'`이고 direct 소스면 409 대신 현재 잡 페이로드를 그대로 돌려주는 멱등 no-op으로 응답 — n8n/픽스처 소스는 `_is_direct_source_job`이 False라 기존 409 그대로(회귀 테스트로 고정: `catalog_running` 상태의 non-direct 잡은 여전히 409). ② `run_view_deps`가 잡 상태를 안 보고 무조건 `ready`로 덮어써, 1번 수정으로 이 함수의 도달 경로가 바뀌면 실패한 잡을 성공으로 뒤집을 길이 열린다 — `job.stage == 'failed'`면 아무것도 안 바꾸고 조용히 리턴하도록 가드 추가. 둘 다 RED 먼저 관찰: ①은 수정 전 코드로 `/api/collect/view-deps`를 direct 소스의 완료 잡에 호출해 실제로 409 응답(`"catalog step must complete before view-deps"`)을 받는 것을 확인, ②는 잡을 `failed`로 만든 뒤 `run_view_deps` 호출 시 `stage`가 `ready`로 뒤집히는 것을 확인 → 각각 구현 → GREEN. 리뷰가 "건드리지 말라"고 명시한 `test_collect.py`의 AAA 주석 위치 슬립(`# Act` 아래 있는 상태 단언)은 그대로 뒀다. `tests/test_collect.py` 15개(기존 13 + 이전 라운드 신규 2) 전부 무변경 통과 확인. 테스트 385→388 passed(+3), ruff clean.
- **Task 12: 조회 API에 `source_id` 파라미터** (`backend/app/api/objects.py`, `backend/app/api/erd.py`, `backend/app/api/snapshots.py`, `backend/app/api/categories.py`, `backend/tests/test_source_scoped_queries.py`). 브리프의 파일 목록을 그대로 믿지 않고 `resolve_snapshot` 호출부를 전수 grep — `objects.py`(검색·컬럼 인덱스) 2곳은 이번 태스크 대상, `categories.py` 2곳은 Task 3에서 이미 소스 인지라 확인만 하고(응답에 `source_id`만 추가), `keys.py`(`/api/join-keys`)·`ai.py`(`/search-tables`·`/chat`)·`ai_jobs.py`는 **의도적으로 제외** — Task 13 브리프가 소스 선택기를 연결할 함수로 `searchObjects`·`fetchAllObjects`·`fetchErdGraph`·컬럼 인덱스 4개만 못박아 근시일 내 소비자가 없고(join-keys), Task 3이 이미 AI/관계검증 서브시스템(`resolve_column_ref` 의존 5개 모듈)의 소스 인지화를 "범위 밖의 재구조화"로 명시 유보한 전례를 따랐다. `erd.py`는 `resolve_snapshot`을 안 쓰고 `func.max(CatalogObject.snapshot_id)`로 최신 스냅샷을 고르던 별도 경로라 grep에 안 걸리지만 브리프가 명시한 "그래프 엔드포인트"라 `Snapshot` 조인으로 소스 필터를 추가하고 없음(200+빈 그래프) 시맨틱은 유지했다. `snapshots.py` 목록은 결이 달라 `source_id` 생략 시 필터 없이 전체 이력을 보여주는 기존 동작을 유지하고 지정 시에만 그 소스로 좁혔다(관리자가 다중 소스 이력을 한눈에 보는 용도라 여기만 기본값이 "현재 소스"가 아니라 "전체"). 모든 응답에 `source_id`(스냅샷 있으면 `data_source_id`)를 나란히 실어 화면이 "지금 무엇을 보고 있는지" 확인하게 했다. 부수 수정: 기존 `test_erd_empty_catalog_is_empty_graph`가 응답 dict 전체를 정확히 비교하고 있어 새 `source_id` 필드에 맞춰 단언 갱신. TDD: 소스 격리 테스트 4개(검색·컬럼 인덱스·ERD 그래프·스냅샷 목록, 각각 두 소스 시딩 후 한쪽 결과에 다른 소스 객체가 안 섞이는지 단언 + 생략 시 기본 소스로 동작하는 하위호환 확인) — 필드 부재로 RED 확인 → 구현 → GREEN. 전체 스위트 413→417 passed(+4, 2 skipped 불변), ruff clean.
  - **리뷰 반영: `keys.py`(`/api/join-keys`) 제외 근거가 틀렸다 — source_id 배선** (`backend/app/api/keys.py`). 유예 근거("Task 13 브리프가 join-keys를 안 배선하니 소비자가 없다")는 태스크 mandate("`resolve_snapshot` 호출부 전부")를 다른 태스크의 문서로 좁힌 것이라는 지적을 받아들였다 — 조인키 바는 스펙이 명시한 "카탈로그 탐색"의 일부고, grep으로 이미 찾아놓고 하류 문서를 근거로 상류 완전성 기준을 축소하면 이 태스크가 막으려던 바로 그 실패("에러 없이 잘못된 소스 데이터")가 남는다. 나머지 제외(AI·관계발견 계열)는 리뷰어가 스펙(`docs/superpowers/specs/2026-08-25-multi-source-db-design.md:20-21`, "비-MSSQL 소스 비목표")으로 재확인해 그대로 유지 — 인용했던 "Task 3 선례"는 실제로는 절반만 맞았다(`validate.py`/`join_preview.py`는 그 리뷰에서 결국 소스 인지로 뒤집혔고 `ai.py`/`relations.py`/`scan.py`만 유예됨). `search_objects`/`get_columns_index`와 동일한 3줄 패턴(`source_id` 파라미터 → `resolve_snapshot`에 전달 → 응답에 echo)을 적용. 커버링 테스트 1개(두 소스에 서로 다른 이름의 FK 컬럼을 시딩해 `source_id`로 조회 시 다른 소스의 키 이름이 섞이지 않는지 단언) — 수정 전 코드에 대고 돌려 `{'MSSQL_KEY'} == {'PG_KEY'}` AssertionError로 RED 실측 후 원복해 GREEN. 전체 스위트 417→418 passed(+1, 2 skipped 불변), ruff clean.
- **Task 11: `/api/sources` CRUD + 연결 테스트** (`backend/app/api/sources.py`, `backend/app/sources/registry.py`, `backend/app/main.py`, `backend/tests/test_sources_api.py`). Phase 4(API·화면)의 첫 태스크 — 소스 등록 API가 없으면 Task 1~10이 전부 잠자는 코드였다. 관용은 `admin.py`(라우터 자체가 `require_sysadmin`, 쓰기는 `require_preview_admin` 추가 게이트, `AuditLog` 기록)를 그대로 따랐다. 직렬화를 `_serialize` 하나로 묶어 `password_enc`나 평문 비밀번호가 응답에 실릴 경로 자체를 구조적으로 차단(응답은 `has_password: bool`만). 앞선 태스크 리뷰에서 이 태스크로 넘어온 이월 4건: ① `SourceUpdateRequest`를 `SourceCreateRequest` 상속(`# type: ignore[assignment]`)이 아니라 독립 `BaseModel`로 작성 — 상속이면 부모가 요구하는 필드를 자식이 옵셔널로 좁혀 LSP 위반(pyright가 잡는다), `engine` 필드는 아예 뺐다(엔진 변경은 host/port/file_path 조합을 통째로 갈아치우는 일이라 부분수정으로 안전하게 못 담는다 — 소스를 새로 만드는 쪽으로 유도). ② DELETE는 스냅샷뿐 아니라 `preview_allowlist`·`schema_categories`에 그 소스의 행이 있어도 거부 — 두 표 모두 `data_source_id`가 FK가 아니라(설계상 의도) 삭제하면 고아 행이 남고 id 재사용 환경(SQLite)에서 낡은 허용이 새 소스에 적용될 수 있다. ③ `is_enabled=False`가 라우팅에 반영 안 되던 문제 — `registry.get_source()` 한 곳에 `is_enabled` 체크를 추가했다(404 다음 409). 미리보기(`objects.py`)·수집 트리거(`collect.py`의 `get_collect_runner_for`)·이번 태스크의 연결 테스트가 전부 이 함수로 소스를 얻으므로 한 곳만 지키면 셋 다 막힌다. 관리 API의 목록·수정·삭제는 이 함수를 안 쓰는 별도 `_get_editable`을 쓰게 해 비활성 소스도 계속 보이고 다시 켤 수 있게 뒀다(이 게이트에 걸리면 되살릴 방법이 없어진다). `/test` 엔드포인트도 자체 `db.get` 대신 `get_source`를 쓰게 해 같은 게이트를 공짜로 물려받는다 — 즉 비활성 소스는 연결 테스트도 거부(끄면 어떤 실접속 경로도 안 탄다는 단순한 의미로 통일). ④ `clear_sa_engine`을 PATCH·DELETE가 실제로 호출하는지 — 캐시 dict(`app.sources.connection._engines`)를 직접 들여다보는 화이트박스 테스트 2개로 확인. 연결 테스트(`/test`) 실패 처리는 브리프 초안의 `str(e)[:300]`을 응답 context에서 뺐다 — `objects.py`의 502 핸들러(Task 7 보안 지적으로 정착된 관용)를 그대로 따라 `(DBAPIError, SATimeoutError, DisconnectionError)`만 좁혀 잡고 `type(e).__name__`만 응답·`last_error` 컬럼에 남기고 전문은 `logger.warning(exc_info=True)`로만 보낸다 — `last_error`가 `_serialize`를 거쳐 모든 목록 응답에 실리므로 여기서 새면 드라이버 원문이 상시 노출된다. 구현 중 직접 잡은 버그 1건: FastAPI의 `yield`-의존성(`get_db`)은 라우트가 던진 예외를 받으면 세션을 롤백한다 — `/test` 실패 핸들러가 `last_error`를 기록만 하고 바로 `HTTPException`을 던지면 그 기록 자체가 롤백에 쓸려 사라진다. `db.commit()`을 예외를 던지기 전에 넣어 해결했고, 그 줄을 지운 채로 회귀 테스트를 돌려 실제로 깨지는 것(422 아님, `last_error` 단언 실패)을 확인한 뒤 복원해 이 테스트가 허수가 아님을 검증했다. `_engines` 캐시가 프로세스 전역이라 테스트마다 새 임시 DB로 id가 2부터 재사용되는 문제(Task 10에서도 나온 것과 동일 클래스)는 이 파일에 소스가 여럿 생겨 고정 id 앞뒤 clear 대신 `autouse` 픽스처로 테스트 전후 캐시 전체를 비우는 쪽을 택했다. TDD: 브리프 4개 + 이월·보안 요구 커버 테스트 15개(비밀번호 미노출 2, 키 미설정 503 2, `is_managed` 보호, 비밀번호 게이트, 목록 가시성, 삭제 거부 3종 + 성공 1, 캐시 클리어 2, 비활성 차단 3, `/test` 성공·실패·n8n 소스 분기) 총 19개 — 라우터 부재로 전부 404 RED 확인 → 구현 → GREEN. 전체 스위트 388→407 passed(+19, 2 skipped 불변), ruff clean, pyright(`sources.py`·`registry.py`) 0 errors.
  - **리뷰 반영: 422가 비밀번호를 되비추는 Critical + 6건** (`backend/app/main.py`, `backend/app/api/{sources,collect}.py`, `backend/app/sources/registry.py`, `backend/tests/test_sources_api.py`). Critical(C1): 전역 `RequestValidationError` 핸들러(`main.py`)가 `jsonable_encoder(exc.errors())`를 그대로 실어, pydantic이 타입 불일치로 거부한 원본값(`input`)을 그대로 되비췄다 — 숫자·리스트 비밀번호를 보내면 422 응답에 평문이 실렸다(`"input":["hunter2"]`, `"input":12345678`), 프론트가 `Number(input)`을 하거나 폼이 오조합되면 실사용에서 밟는 경로다. `_serialize`가 유일한 통로라는 이 태스크의 핵심 보장을 422 경로가 통째로 우회한 셈. `_redact_validation_error(err)` 헬퍼로 `loc`에 `"password"`가 있는 오류만 `input`을 `"***"`로 지우고 나머지(예: port 오류의 진단값)는 그대로 뒀다 — 수정 전 코드로 실제 앱에 재현해 RED 확인 후 고쳤다. Important(I1): `/test`가 `get_source`의 `is_enabled` 게이트를 그대로 물려받아 비활성 소스는 연결 테스트조차 막혔는데, 이게 "자격증명 수정→테스트로 확인→재활성화"라는 정상 운영 순서를 뒤집어(확인 없이 먼저 켜야만 테스트 가능) 정확히 반대 결과를 냈다 — `get_source(db, source_id, allow_disabled: bool = False)`에 키워드 전용 인자를 추가해 `/test`만 `allow_disabled=True`로 부르고, 라이브 연결이 실제로 걸리는 미리보기·수집 트리거는 여전히 기본값으로 막히게 했다(`_get_editable`로 바꾸지 않은 이유: 그러면 `is_managed` 409가 먼저 발화해 n8n 소스의 400 분기 의미가 깨진다). Minor 5건: ① `collect.py`의 `_is_direct_source_job`(스냅샷의 `access_mode`만 읽는 상태 조회, 실접속 없음)이 `get_source`를 통해 `is_enabled` 게이트를 물려받아, 완료된 잡의 소스를 나중에 비활성화하면 `trigger_view_deps_step`의 멱등 200이 409로 뒤집혔다 — Task 10에서 없앤 거짓 409가 다른 경로로 돌아온 것이라 `db.get(DataSource, ...)` 직접 조회로 되돌렸다. ② `/test`가 `mssql`+`direct` 같은 API로는 못 만들지만 DB 직접 편집으로 도달 가능한 조합에서 `UnsupportedSource`를 못 잡아 500이 났다 — 프로브 선택을 `if/elif/else`로 명시해 미지원 엔진을 붙어보기 전에 400으로 걸러내고, `except` 튜플에도 `UnsupportedSource`를 추가해(`objects.py`가 `CryptoNotConfigured`와 묶어 503으로 처리하는 것과 같은 관용 — 코디네이터가 제안한 400/502 대신 기존 코드베이스 관용을 우선했다) 이중으로 방어했다. ③ `CryptoNotConfigured` 분기가 `last_error`를 안 건드려 키를 돌린 뒤에도 목록이 낡은 "정상" 상태를 계속 보여줬다 — `DBAPIError` 분기와 같은 패턴(`last_error` 기록 + `db.commit()`)을 적용했다. ④ `list_sources` docstring이 "모든 활성 소스"라 실제 동작(비활성 포함 전부 반환)과 반대로 읽혔다 — 한 단어 수정 + 이유(비활성도 보여야 재활성화 가능) 추가. ⑤ 유출 방지 테스트(`test_create_source_never_returns_the_password`)가 목록 확인을 파싱된 `items`만 봐서 그 바깥 필드는 검사하지 못했다 — 원문 `res.text`로 교체. 새 테스트 6개(422 리스트/숫자 비밀번호, PATCH 422도 동일, 비-비밀 필드는 진단값 보존 확인, 비활성 소스의 `/test` 200 + 수집 여전히 409, `mssql`+`direct` 조합 400, 키 교체 후 `last_error` 갱신) + 기존 1개 뒤집기(`test_disabled_source_refuses_connection_test` → `test_disabled_source_allows_connection_test`, 실제 sqlite 파일로 200 왕복 확인). `test_collect.py`·`test_direct_collect.py` 21개 무변경 통과(M2 회귀 확인). 전체 스위트 407→413 passed(+6, 2 skipped 불변), ruff clean, pyright(`sources.py`·`registry.py`·`main.py`·`collect.py`) 0 errors.
- **Task 13: 프론트엔드 소스 선택기** (`frontend/src/lib/source-param.ts`, `frontend/src/lib/use-data-sources.ts`, `frontend/src/components/SourceSelector.tsx`, `frontend/src/components/AppHeader.tsx`, `frontend/src/app/page.tsx`, `frontend/src/app/erd/page.tsx`, `frontend/src/components/erd/ErdViewer.tsx`, `frontend/src/lib/api.ts`, `frontend/src/lib/use-preview-allowlist.ts`). Phase 4 마무리 — 소스 등록·수집·조회 API가 다 있어도 화면에서 못 고르면 Task 1~12가 안 보인다. 선택은 `?source=`(URL)에 싣고 `readSourceId`/`withSourceParam`(TDD, 5 테스트)이 API용 `source_id` 쿼리로 바꾼다. 브리프가 못박은 4개 함수(`searchObjects`·`fetchAllObjects`·`fetchErdGraph`·컬럼 인덱스) + 이월 1(`fetchJoinKeys`, `ef8692b`가 백엔드에 이미 배선)만으로 끝내지 않고 `api.ts` 전수 대조 + 백엔드 grep으로 `fetchSchemaCategories`/`assignSchemaCategory`(좌측 스키마·카테고리 목록)와 `fetchPreviewAllowlist`(미리보기 버튼 활성 판정 — `usePreviewAllowlist(sourceId)`로 훅에 인자 추가)도 소스별임을 확인해 추가 배선했다. 이월 2(비-MSSQL 진입점 숨김)는 브리프의 범위 힌트("app/ 페이지들과 헤더")를 따라 `AppHeader`에 `sourceEngine` prop을 추가해 `/verify`·`/parsing` 네비 링크와 `ChatPanel`(AI 챗)을 엔진이 mssql이 아닐 때만 숨기는 것으로 한정하고, `TableDetail`·`ErdViewer` 내부 개별 AI/검증 버튼(조인 체크, AI 요약 등)은 범위 밖 재구조화로 보류했다 — 다만 `page.tsx`의 컬럼 클릭→검증 이동(`handleOpenColumn`)은 같은 파일이라 `isMssqlSource` 가드로 조용히 막았다(안 막으면 다른 소스의 객체 id가 기본 소스 카탈로그의 우연히 같은 id를 가리켜 엉뚱한 테이블의 검증 화면이 열릴 수 있다). 소스 전환 시 이전 소스를 가리키던 상태(`selected`/`detail`/미리보기 탭/`selectedKey`/`category`)는 모두 초기화하고 `dbFilter`(스키마명 필터)도 `[]`로 리셋했다 — 안 그러면 새 소스에 없는 스키마명이 필터에 남아 목록이 통째로 빈 것처럼 보인다(버그로 오인하기 쉽다). ERD는 `<ErdViewer key={sourceId}>`로 소스마다 통째로 리마운트시켜 펼침·수동배치·하이라이트 같은 내부 상태가 이전 그래프의 노드 id를 참조한 채 남는 걸 원천 차단했고, 노드 우클릭 메뉴의 홈 복귀 링크(미리보기·상세)에도 `&source=`가 빠져 있던 것을 코드 추적 중 추가로 발견해 배선했다(없으면 다른 소스를 보던 중 ERD에서 미리보기를 열 때 기본 소스로 튕긴다). SSR 안전성: 브리프 예시대로 `window.location.search`로 초기값을 읽으면 이 앱이 `output: standalone`라 `npm run build`가 `/`·`/erd`를 실제로 정적 프리렌더하는 과정에서 `window is not defined`로 깨진다 — 이미 쓰던 `useSearchParams()`에서 초기값을 뽑고 이후 갱신만 `history.replaceState`로 하도록 바꿔 회피(빌드 성공으로 실측). 게이트 4종(tsc/lint/vitest/build) 전부 통과, vitest 106 passed(13 files, `source-param.test.ts` 5개 신규).
  - **리뷰 반영: TableDetail/ErdViewer의 AI·검증 진입점 누락 + URL 소스 유실** (`frontend/src/components/browser/TableDetail.tsx`, `frontend/src/components/erd/ErdViewer.tsx`, `frontend/src/app/{page,erd/page}.tsx`, `frontend/src/lib/source-param.ts`). Important 2건. ① `TableDetail`의 "조인 가능성 검증"(T2 일괄+개별)·"AI 요약"·"뷰 설명" 버튼이 `sourceEngine`을 전혀 모른 채 무조건 렌더됐다 — 이 셋이 부르는 `runJoinCheck`/`generateAiSummary`/`explainViewAi`는 전역 싱글턴 검증기(`SOURCE_MODE`만 보고 고르는 `N8nJoinValidator`/`FakeJoinValidator`)라 소스 파라미터 자체가 없어서, Postgres 소스의 아무 테이블이나 열고 누르면 MSSQL 전용 검증기가 그 이름으로 실행돼 에러거나 동명 MSSQL 객체의 그럴듯한 결과가 나온다 — **특별한 조작 없이 아무 소스·아무 테이블에서 바로 재현되는 1급 버그**였다(이전 라운드의 "헤더·app 페이지 범위" 판단이 이 컴포넌트 내부 버튼까지는 못 미친 것). `isMssqlSource: boolean` prop을 뚫어 세 버튼과 유사 테이블 목록의 개별 "검증" 버튼을 통째로 숨겼다. `ErdViewer`의 나머지 3개 검증 링크(노드 메뉴 "검증", `focusMissing` 배너, 빈 상태 "검증 시작")도 같은 규칙(`sourceEngine` prop 추가)으로 숨겼다. ② `page.tsx`의 `selectTable`/`changeCategory`/미리보기 딥링크 소진 효과가 전부 `router.push("/?table=...")`로 쿼리 문자열을 통째로 교체해 **테이블 행을 클릭하는 가장 흔한 인터랙션 한 번에 `?source=`가 사라졌다** — 세션 중엔 React state가 진실이라 화면은 멀쩡해 보이지만 새로고침·링크 공유 시 조용히 기본 MSSQL 소스로 돌아가, `source-param.ts`가 애초에 문서화한 목적(링크 공유·새로고침 생존)을 정면으로 무너뜨렸다. `withSourceQuery(path, sourceId)`(`withSourceParam`의 API `source_id` 대응 — 브라우저 라우팅용 `source`, vitest 3개로 커버) 헬퍼를 추가해 세 경로 전부와 `handleOpenErd`·`ErdViewer`의 노드 메뉴 홈 복귀 링크(이전 라운드에서 직접 문자열 조합했던 것)를 통일했다. 게이트 4종 재실행 전부 통과, vitest 106→109 passed(13 files, `withSourceQuery` 3개 신규).

- **Task 14: 관리자 소스 패널** (`frontend/src/components/admin/DataSourcePanel.tsx`(신규), `frontend/src/app/admin/page.tsx`, `frontend/src/lib/api.ts`, `frontend/src/components/admin/PreviewAllowlistPanel.tsx`, `frontend/vitest.config.ts`). Phase 4 마지막 코드 태스크 — 소스 등록 화면이 없으면 Task 1~13이 전부 잠자는 코드였다. `DataSourcePanel`은 목록·등록·수정(엔진 고정, 비밀번호는 채웠을 때만 교체)·활성화 전환·삭제·연결 테스트를 미리보기 허용 목록과 같은 `X-Preview-Password` 게이트로 묶었고, `is_managed`(사내 MSSQL) 소스는 배지만 달고 수정·삭제·전환 버튼을 아예 렌더하지 않는다(백엔드 409를 화면에서 미리 막음). `SOURCE_SECRET_KEY` 미설정(`secret_key_configured=false`)이면 등록 폼 자체를 렌더하지 않고 배너로 안내(503을 보고 나서 알게 하지 않음) — 반면 활성화·비활성화·삭제는 그 게이트와 무관해 계속 동작하게 뒀다(브리프 예시 코드는 이 셋도 `keyConfigured`로 묶었지만, PATCH가 비밀번호를 안 건드리면 암호화 경로를 안 타 실제로 막을 이유가 없어 더 정확한 쪽으로 갈랐다). 연결 테스트는 성공·실패 모두 서버가 `last_ok_at`/`last_error`를 커밋하므로 공용 `run()`(실패 시 재조회 생략)과 별도로 항상 재조회하게 했고, 회신된 `database`/`version`을 메시지로 그대로 노출해 흔한 컨테이너명 오접속을 눈으로 잡게 했다. 이월 1(관리 화면의 소스 선택 — `PreviewAllowlistPanel`이 `fetchSchemaCategories`를 소스 무관하게 부르던 것): `DataSourcePanel`에 선택 상태를 두지 않고 기존 `SourceSelector`/`useDataSources`(Task 13 산출물)를 `PreviewAllowlistPanel` 위에 그대로 재사용 — 관리 화면 소스 선택이 이미 확립된 패턴이라 새 결합을 만들지 않는 쪽을 택했다. 허용 목록 admin API 3개(`fetchPreviewAllowlistAdmin`/`addPreviewAllow`/`removePreviewAllow`)가 전부 `(data_source_id, schema)` PK인데 프론트는 하나도 소스를 안 넘기고 있었던 것도 같이 발견해 세 함수 모두 `sourceId` 트레일링 인자로 배선(GET/DELETE는 `withSourceParam` 쿼리, POST는 본문 `source_id`). 백엔드 계약 섹션이 명시한 수집 트리거(`POST /api/collect/catalog` + `source_id`)도 반영 — `triggerCollectCatalog`에 옵셔널 `sourceId`를 추가(기존 CollectPanel 호출은 인자 없이 불러 하위호환)하고 비관리 소스 행에 '카탈로그 수집' 버튼을 달았다: direct 소스는 뷰 의존 단계가 없어 이 한 번으로 끝나고(Task 10), `CollectJob` 목록이 소스 구분 없이 최신순이라 트리거만 하면 바로 아래 `CollectPanel`이 진행률을 그대로 보여준다(중복 구현 없음). TDD: 순수 함수 3개(`buildCreateInput`/`buildUpdateInput`/`isSourceFormValid`, `sources.py`의 `_validate_shape`과 동일 규칙) — vitest가 `.test.ts`만 잡고(`node` 환경, "순수 로직만") 컴포넌트 렌더 테스트가 이 프로젝트에 전례가 없는데, `.tsx`에서 named export한 함수를 `.test.ts`가 import하자 `tsconfig`의 `jsx:"preserve"`(Next SWC 전용)를 vite(oxc)가 그대로 물려받아 파싱이 깨졌다 — 파일을 쪼개지 말라는 제약이 있어 분리 대신 `vitest.config.ts`에 `oxc.jsx.runtime: "automatic"`(테스트 전용, 프로덕션 빌드엔 미사용)를 추가해 해결, RED(모듈 없음) 확인 후 구현 → GREEN. 게이트 4종(tsc/lint/vitest/build) 전부 통과, vitest 109→118 passed(15 files, 신규 9개).
  - **리뷰 반영: 삭제 409 개수 유실, n8n 소스에 절대 성공 못 하는 테스트 버튼, 취소 후 비밀번호 잔류** (`frontend/src/lib/api.ts`, `frontend/src/components/admin/DataSourcePanel.tsx`, `frontend/src/lib/api.test.ts`). Approved + 3건. ① 백엔드가 삭제 409의 `context`에 `snapshots`/`preview_allowlist`/`schema_categories` 개수를 실어 보내는데, 공용 `handle()`(모든 API 호출이 거치는 공유 헬퍼)이 `message`만 남기고 버려 화면이 "무엇이 얼마나 막고 있는지"를 보여줄 수 없었다 — 코디네이터 지시대로 `handle()`은 그대로 두고 `deleteDataSource`만 `deleteJson`을 거치지 않고 직접 `fetch`(+`authHeaders()`)해 `context`를 읽게 바꿨다. 개수 문구 조립은 순수 함수 `formatDeleteBlockedMessage(context, fallback)`로 분리(음수/누락 카운트 스킵, 카운트가 하나도 없으면 원본 메시지로 폴백 — `is_managed` 409처럼 context에 개수가 없는 경우도 커버). ② `is_managed`(사내 MSSQL) 소스는 `access_mode="n8n"`이라 `sources.py`가 연결 테스트를 언제나 400으로 거부하는데, "비활성 소스도 테스트 가능"이라는 디스패치 근거를 "n8n 소스"에도 잘못 적용해 그 행에도 버튼을 그려뒀다 — 리뷰 지적이 정확했다(비활성=허용, n8n=항상 거부는 별개 축). `!item.is_managed` 게이트 안으로 옮겨 관리형 행에서 버튼 자체를 없앴다(기존 수정·삭제·전환과 동일하게 "비활성화"가 아니라 "미렌더"). ③ 편집 취소가 `editingId`만 지우고 `editForm`은 안 비워, 입력했다 취소한 접속 비밀번호가 다음 편집까지 컴포넌트 상태에 남았다(렌더·전송·로그 유출은 없음, 실제 누수는 아니라는 리뷰 판정에 동의) — 취소 핸들러에 `setEditForm(EMPTY_FORM)` 한 줄 추가. 리뷰가 기각한 항목은 그대로 뒀다: 소스별 카탈로그 수집 트리거(코디네이터가 디스패치에 명시했으나 브리프 파일만 본 리뷰어에겐 범위 밖으로 보였던 것)와 관리 화면 소스 피커의 URL 미동기화(의도적 트레이드오프로 이미 신고). TDD: `formatDeleteBlockedMessage` 5개 케이스(context 없음/전부 0/단일 카운트/복수 카운트 순서 고정/일부만 0) — `.ts` 순수 함수라 이번엔 `.tsx` JSX 우회 없이 바로 작성. 게이트 4종 재실행 전부 통과, vitest 118→123 passed(+5).
- **Task 15: 설정 동기화 + 배포 문서 + 담당자 프롬프트** (`.env.example`, `docker-compose.yml`, `docs/connect-sources.md`(신규), `docs/handoff/service-owner-prompt.md`, `README.md`). Phase 5 마지막 태스크 — 코드는 안 건드리고 설정·문서만. 브리프 대신 코드를 직접 대조해 확인한 5가지: ① 환경변수는 브리프와 실제(`config.py`)가 이미 일치(`SOURCE_SECRET_KEY`/`SOURCE_CONNECT_TIMEOUT`=5/`SOURCE_QUERY_TIMEOUT`=15) — 오히려 설계 스펙 §8이 낡아 있었다(`DBV_SECRET_KEY`/`SOURCE_PREVIEW_TIMEOUT`라는 옛 이름을 그대로 들고 있음, 구현 단계에서 개명된 것으로 보이나 스펙 자체는 미수정). ② `/test`는 성공 시 `{ok, version, database, latency_ms}`, 503은 `CryptoNotConfigured`/`UnsupportedSource`(이쪽 설정 문제), 502는 `DBAPIError`/`SATimeoutError`/`DisconnectionError`(소스 접속 실패, 컨텍스트는 `host`/`database`/`error_type`만 — 드라이버 원문 비노출). ③ 삭제 거부는 스냅샷뿐 아니라 `preview_allowlist`·`schema_categories` 정책 행도 포함(셋 다 0이어야 삭제 허용). ④ 비활성 소스는 연결 테스트(`allow_disabled=True`)만 되고 미리보기·수집은 막힌다 — "자격증명 고치고 → 테스트 확인 → 재활성화" 순서를 지키기 위한 설계(`registry.py` 주석 재확인 후 문서에 근거로 인용). ⑤ head는 0017(`0015_data_sources → 0016_snapshot_source → 0017_policy_by_source`), 브랜치 시작 시점 head는 0014. 이월 2건: PostgreSQL 미실행 마이그레이션(개발 환경에 psql/docker 없어 SQLite 왕복만 검증됨)에 대해 배포 런북에 "`alembic upgrade head` 로그를 눈으로 확인" 필수 체크와 `alembic downgrade 0014` 롤백 절차를 명시했고, 기존 담당자 문서(`service-owner-prompt.md`, 계획 단계 산출물)는 새로 안 만들고 갱신 — 운영자 사전 작업에 `SOURCE_SECRET_KEY` 생성 커맨드 추가, 회신 정보를 `/admin`에 등록하는 절차(⑥ 신설, 기존 ⑥ 문제해결은 ⑦로 이동)와 연결 테스트가 "흔한 컨테이너명이라 엉뚱한 DB에도 연결 자체는 성공할 수 있다"는 이유를 명시했다. `docker-compose.yml`은 backend `environment`에 3개 변수 추가 + 하단에 대상 서비스 합류 방법을 주석 예시로 남겼다(실제 네트워크명은 배포마다 달라 코드에 반영하지 않음). 코드 무변경이라 테스트 베이스라인 그대로(백엔드 417 passed/3 skipped, 프론트 123 passed 재확인), `docker compose config -q` 통과.
  - **리뷰 반영: 사실 오류 2건 + 인용 잘림 1건 + 문서 내 자체 모순 1건 + 스펙 §8 변수명 정정** (`docs/connect-sources.md`, `docs/handoff/service-owner-prompt.md`, `docs/superpowers/specs/2026-08-25-multi-source-db-design.md`). 운영 안전성(볼륨 게이트·`down` 금지·`default:` 함정 위치·읽기전용 계정 SQL·`SOURCE_SECRET_KEY` 순서)은 전부 통과했으나 "코드와 한 줄씩 대조했다"던 영역에서 방향이 뒤집힌 주장 2건이 나왔다. ① `/test`가 엔진이 postgres/sqlite가 아닌 행을 거부하는 코드(`sources.py:220-224`)는 try/except **이전**이라 500대가 아니라 **400** — 트러블슈팅 표의 503 행에서 그 원인을 빼고 400 행을 신설했다. ② `decrypt_secret`(`crypto.py:37-42`)이 `InvalidToken`을 **`CryptoNotConfigured`로 재발생**시키는데(메시지에 "rotated" 안내 포함), 문서는 "`CryptoNotConfigured`가 아니라 복호화 실패"라고 정반대로 적어 놨었다 — 예외 타입 주장만 뒤집어 정정("복호화 실패는 `CryptoNotConfigured`로 보고된다"). 그 외: 비활성 소스 오류 메시지 인용이 `"disabled — enable it..."`로 잘려 있어 `registry.py:39-41`의 전문(`"this data source is disabled — enable it before connecting"`)으로 맞춤(운영자가 로그에서 grep할 문자열이라 부분 인용은 못 찾는다), `service-owner-prompt.md` 60행이 기존 대역을 "172.36~172.48"이라 적어 같은 문서 30행("172.36~46", db-viewer 자신은 172.48로 별개)과 자체 모순이던 것을 일치시켰다. 코디네이터가 직접 판정해 지시한 항목 1건: 설계 스펙 §8 표가 구현 단계에서 개명된 옛 변수명(`DBV_SECRET_KEY`/`SOURCE_PREVIEW_TIMEOUT`)을 들고 있던 것 — 이 태스크 파일 목록엔 없었지만 "설계 기록에 남는 오도성 정보"라는 근거로 표의 세 행만 실제 이름(`SOURCE_SECRET_KEY`/`SOURCE_QUERY_TIMEOUT`)으로 정정(다른 내용은 무수정). 그대로 둔 것: "기존 서비스가 172.36~46을 쓴다"·"PG 15 미만에서 PUBLIC이 CREATE 권한을 기본 보유해 프로브가 거짓 통과할 수 있다"는 이 저장소에서 검증 불가능한 외부 사실 2건(코디네이터 판정). 다섯 건 모두 재수정 후 `grep`으로 코드 원문과 재대조(리포트에 출력 포함). 코드 무변경, 테스트 재확인 동일(백엔드 417 passed/3 skipped, 프론트 123 passed), `docker compose config -q` 통과.
- **최종 전체 리뷰 수정 웨이브 (33커밋 통합 리뷰 → 단일 수정 패스)**. 개별 태스크 리뷰가 구조적으로 못 보던 브랜치 전체 문제 9건. ① **C1**: 0015의 시드 행이 id를 명시해 넣어 PostgreSQL SERIAL 시퀀스가 1에 머문다 — 관리자가 등록하는 첫 소스가 시드 행과 PK 충돌해 배포 런북 첫 동작이 결정론적으로 500(재시도하면 성공해 원인이 안 밝혀진다). SQLite는 rowid 별칭이라 이 사고를 감춘다. `setval` 추가 + 등록 충돌을 409로. 도커 PostgreSQL 16에 실제로 마이그레이션 전체를 올려 RED(nextval=1) → GREEN(=2) 확인. ② **I1**: 스냅샷 id가 전 소스 공통 시퀀스라 비-MSSQL 소스를 한 번 수집하면 소스를 안 건 "최신 ready" 조회 4곳(스캔·임베딩·/verify 프리필·/parsing)이 조용히 엉뚱한 스냅샷을 본다 — 넷 다 MSSQL 전용 기능이라 사내 MSSQL로 고정(일반화 금지 주석 명시). ③ **I2·I3**: qname만으로 조회하는 상세 패널의 관계·AI 요약을 비-MSSQL 소스에서 건너뛰고, 검증·조인·AI 진입점은 `resolve_column_ref`/join-check 한 곳에서 다른 엔진의 id를 400으로 거부한다 — UI 게이팅은 경계가 아니라는 판단(북마크 URL 하나면 뚫린다). ④ 나머지: 수집 실패 예외 원문이 관리 화면까지 가던 것 제거(이 경로는 복호화된 자격증명을 들고 있다), 일반 사용자가 소스를 영영 못 고르던 것(`/api/sources/options` 신설 — 접속정보 미포함), SQLite 조합 제약명이 `varchar(128)`을 넘겨 PostgreSQL 적재를 실패시키던 것.
  - 문서: SQLite **WAL 저널 모드면 읽기 전용 열기가 실패할 가능성이 높다**는 사전 체크(`PRAGMA journal_mode`)를 런북에 추가 — 실제 WAL 소스로 검증할 수 없어 우회책은 단정하지 않고 go-live 전 [연결 테스트] 확인을 요구. 다운그레이드가 파괴적임(0017 이전 → 비-MSSQL 소스 노출 정책, 0015 이전 → `data_sources` 전체 + 암호화된 비밀번호)을 README 롤백 줄과 런북 표에 명시. 스펙 §10의 "기존 Playwright에 시나리오 추가"는 이 저장소에 없는 인프라를 전제한 **스펙 오류** — 실제(vitest 순수 헬퍼만, 컴포넌트/E2E 없음)로 정정하고 그래서 비목표 경계를 백엔드로도 강제한다는 근거를 남겼다.
  - 기존 테스트 3건의 기대값을 의도적으로 갱신했다(회귀가 아니라 이 웨이브가 바꾼 동작): 수집 실패 메시지가 예외 원문 대신 타입, 조인 계열 허용목록 테스트 2건이 403(허용목록)보다 앞선 400(엔진 게이트)에서 막힘. 백엔드 417→426 passed(+9, PG 가드 1건 skip 추가), 프론트 게이트 4종 그대로 통과(123 passed).

## 2026-08-11

- **어드밴스드 필터 UX 정정 — 스테이징 + [조회] 스텝 분리** (fix/preview-filter-staged-query). 사용자 지시: 추가마다 쿼리를 날리지 말 것. 칩 추가·제거는 로컬 스테이징만 하고 칩 행의 [조회](돋보기 아이콘)가 한 번에 재질의 — 조건 여러 개를 쌓아도 쿼리는 1회. 미적용 칩은 대시 테두리로 구분(적용되면 실선), [필터 해제](× 아이콘)는 1행에서 칩 행으로 이동해 전체 해제+무필터 재질의, [필터 추가]는 secondary로 강등(주 행동은 조회). 셀 더블클릭도 스테이징만. 검증: tsc·eslint·vitest 101/101, 헤드리스 12/12 — 스테이징 불변(행 유지)→조회 1회 반영→대시/실선 전환→로컬 제거 후 재조회 복귀→해제 왕복.

- **미리보기 어드밴스드 필터 — 복수 조건·제외·NULL, SQL 반영** (feature/preview-advanced-filter). 단일 값 필터를 AND 결합 조건 목록(op 6종: contains/eq/not_contains/neq/is_null/not_null, 상한 5)으로 확장 — 전 스택 관통: API `filters` JSON 파라미터(구 filter_column/value/mode 교체, Pydantic 검증·컬럼 실재·값 필수·개수 상한 400, 감사 로그에 `A~'v' AND B IS NULL` 표기) → FakeTablePreview(AND 평가, is_null은 진짜 None만) → N8nTablePreview(filters 배열 + 단일 긍정 조건은 구 W2 호환 필드 병송, **응답 query에 WHERE 없으면 raise** — 구 W2가 무필터 행을 필터 결과로 속이는 사고 차단) → W2 빌더(AND 결합 WHERE, JSON 재생성 — **운영 n8n엔 W2 재임포트 필요**). UI는 추가/제거 즉시 재조회되는 칩 방식(칩은 항상 적용된 상태만 표시), 같이 구현한 편의 기능: NULL 연산자, 셀 더블클릭 eq 빠른 필터, 값 자동완성 datalist(로드 행 고유값). SQL 보기 `WHERE a\n  AND b` 렌더 + 토크나이저 AND/NOT/IS/NULL. 검증: 백엔드 335 passed(+4: 결합·검증 400·n8n body/가드)·ruff 클린, 프론트 tsc·eslint·vitest 101/101(+5), 헤드리스 실측 9/9 — contains 칩→neq 제외→is_null 0행·값 입력 비활성→SQL 3조건 반영→칩 제거 복귀→더블클릭 eq 칩→datalist→전체 해제 왕복. 제안만 남긴 것: OR 그룹, 범위 연산자(>, <, BETWEEN), 필터 프리셋, WHERE만 복사.

- **SQL 칩 편집 복원 불가 수정 — 고스트 칩 양방향 편집** (fix/sql-pill-restore). 사용자 리포트: 적용 후 다시 열면 스테이징이 "현재 보이는 컬럼"에서 시작해 **누적 제외만 가능**했다. 스테이징을 전체 컬럼 위의 포함 집합(Set)으로 바꿔 — 숨긴 컬럼은 흐린 대시 고스트 칩으로 남고 클릭하면 복원, 「모두 표시」로 일괄 복원, 적용 라벨은 −숨김/+복원 델타 표기. 같이 잡은 것: 모달을 닫아도 editOpen이 남아 재진입 첫 「컬럼 편집」 클릭이 열기가 아니라 닫기가 되던 상태 잔존(모달 열 때 초기화). 검증: tsc·eslint·vitest 98/98, 헤드리스 — 2개 숨김 적용→모달 재진입 시 고스트 2개, 고스트 클릭 「적용 (+1)」로 개별 복원(다른 숨김은 유지), 모두 표시로 20/20 복원.

- **미리보기 십자 하이라이트 + SQL 보기 컬럼 칩 편집** (feature/preview-crosshair-sql). ① 셀 호버 시 행(기존 tr:hover)+열 동시 하이라이트, 교차 셀은 한 단계 진하게, 헤더 호버는 해당 열만 — 열 축은 React 상태가 아니라 **위임 mouseover로 클래스만 토글**(500행 × N열 호버마다 리렌더 방지), colspan 빈 상태 행은 제외. ② SQL 보기에 「컬럼 편집」 — SELECT 목록을 `[컬럼명 ×]` 필로 늘어놓고 × 클릭·**Backspace(마지막 칩부터 컬럼 단위)**로 빼서 「적용 (−n)」 한 번에 일괄 숨김(자유 텍스트 편집은 이름 오타를 불러 필 방식으로 확정 — 사용자 지시), 남은 순서가 표시 순서(order)로도 반영, 초기화로 스테이징 복원, 0개 적용은 차단. 덤으로 잡은 기존 갭 2건: SQL 보기가 exact 필터도 LIKE로 그리던 것(`= N'v'` 분기+테스트), 드래그 컬럼 순서 미반영(applyColumnOrder 적용). 검증: vitest 98/98·tsc·eslint 클린, 헤드리스 실측 — 셀/헤더 호버 열 클래스 21셀 균일, exact SQL `= N'1000'`, 필 20→(×1+BS1)→18·적용 후 테이블/SQL 양쪽에서 소거 확인.

## 2026-08-10

- **미리보기 값 필터 부분/정확 모드 — 검색 모드 위치 정정** (feature/preview-filter-mode). 직전에 테이블 검색에 붙였던 정확/포함 토글은 요청 의도가 **미리보기 값 재검색**(부분일치)이었음이 확인되어 리버트하고, 값 필터 쪽에 부분 일치(LIKE, 기본)/정확 일치(=) 셀렉트로 옮겨 구현. 값 필터는 소스 쿼리 WHERE로 내려가므로 전 스택 관통: API `filter_mode`(Literal, 응답 filter에 echo·감사 로그에 ~/= 연산자 표기) → FakeTablePreview(대소문자 무시 — MSSQL 기본 collation과 결) → N8nTablePreview(W2 body, 구버전 W2는 무시하고 LIKE 폴백) → W2 빌더 템플릿(`= N'v'` 분기, 워크플로 JSON 재생성 — **운영 n8n엔 W2 재임포트 필요**). 플레이스홀더 "값 (부분 일치)"은 "값"으로 — 방식은 셀렉트가 말한다. 검증: 백엔드 331 passed(+exact 케이스, n8n 바디·드리프트 가드 갱신), 프론트 tsc·eslint·97/97, 헤드리스 실측 — contains '100' 11행(전부 포함)/exact '100' 0행(빈 상태)/exact '1000' 1행(정확 일치), 테이블 검색 칩 제거 확인.

- **미리보기 컬럼 드래그 순서 변경** (feature/pending-list-filter 후반, 직전 리버트에서 로그 항목만 유실되어 재기록). 헤더 HTML5 DnD — `applyColumnOrder`(저장 순서 적용, 새 컬럼은 뒤에)·`moveColumn`(대상 좌/우 절반으로 앞/뒤 삽입) 순수 함수, 순서는 탭 상태로 숨김 컬럼 상대 위치까지 보존, CSV도 화면 순서. 드롭 위치는 inset 프라이머리 바, 리사이즈 핸들은 draggable=false로 간섭 차단. vitest(order 5케이스)+헤드리스 드래그 실측 통과.

- **검증 대기 반응형 필터 + 홈 3열 리사이저** (feature/pending-list-filter). ① /verify 피커에서 테이블을 고르면 우측 대기 큐가 관련 항목만 남는다 — `filterPendingRelations` 순수 함수(테이블은 어느 쪽 끝이든 매치, 여러 개면 전부 걸려야, 빈 선택은 원본 참조), 목록이 전량 로드라 재조회 없는 클라이언트 필터. 카운트는 「n / 전체 m」으로 왜 짧은지 설명하고, 필터로 빈 것은 큐가 빈 것과 다른 문구로 구분. ② 홈 3열(카테고리·목록·상세) 사이 드래그 리사이저 — PreviewTable 컬럼 리사이즈와 같은 window 리스너 관용구, 섹션별 클램프(카테고리 150–300, 목록 260–520, 상세는 lg:min-w-80 최후 방어), 핸들은 gap-4 안에 음수 마진으로 파묻어 시각 간격 불변, wrap 모드(좁은 화면)에선 숨김. 검증: vitest 92/92(+필터 3케이스)·tsc·eslint 클린, 헤드리스 실측 — 필터 (100)→(2/100)→해제 복귀·전 항목 관련성, 리사이저 4방향 드래그 클램프 실측(176→300→150→210 / 320→520→260) 전건 통과.

- **DB 목록·분류 편집 재디자인 + 체크박스 커스텀** (feature/db-list-redesign). ASCII 목업 3안 제시 → A안(한 줄 행+분류 칩) 채택. 2줄 행을 한 줄로(행 27px): 필터(체크박스+이름)는 label, 분류 편집은 label 밖 칩으로 분리해 칩 클릭이 체크를 토글하지 않게 함. 지정 분류는 칩+행 호버 시 연필(PencilIcon 신설, opacity라 레이아웃 불변), 미지정은 스키마명 반복 대신 「+ 분류」 대시 고스트 칩으로 지정 유도. 헤더의 밑줄 링크 2개(필터 해제/전체 선택)는 icon-button으로 — 형제 워딩이라 컨트롤 형태로 구분. 체크박스는 `.checkbox` 토큰 기반 커스텀(appearance:none, 15px·radius 4, 체크 시 옐로+온프라이머리 L자 마크, focus-visible 링) — OS 기본 컨트롤의 테마 불일치 제거, PreviewSection 컬럼 메뉴에도 동일 적용. 검증: vitest 89/89·tsc·eslint 클린, 헤드리스 실측 — 행 높이·칩 종류(dashed)·체크 스타일 전환(옐로 bg+마크)·필터 해제 버튼·칩 편집 왕복(고스트→「결재」+연필→빈 값 커밋으로 원복) 전건 통과.

- **미리보기 잠금 표시 확장 — 카테고리 집계 + 풀린 자물쇠** (feature/preview-lock-states). 허용 상태도 명시적으로: 허용 스키마엔 풀린 자물쇠(그린, LockOpenIcon 신설 — 색은 보조 인코딩, 1차 구분은 열린 고리 형태), 미허용은 기존 잠긴 자물쇠. 홈 카테고리 탭에도 표시 — `getCategoryLockStates` 순수 함수로 카테고리별 허용/미허용 포함 여부를 집계해 **DB 혼재 카테고리는 풀림·잠김을 나란히** 띄운다(전체 행 포함). 3곳(카테고리·DB 탭·ERD 필터) 렌더링은 공용 `PreviewLockMarks`로 통일(툴팁·testid 규격 공유). 검증: vitest 89/89(혼재/단일/빈 집계 3케이스), tsc·eslint 클린, 헤드리스 실측 — 미허용→잠김(무채색)·허용→풀림(그린 --rel-confirmed)·툴팁 문구 전환을 카테고리/DB 탭/ERD 필터 전부에서 확인.

- **ERD 진입 동선 3종 — 우클릭 메뉴·스키마 필터·미리보기 잠금 표시** (feature/erd-context-menu). 노드 우클릭 메뉴(미리보기·테이블 상세로·조인 검증하기·컬럼 펼치기/접기·테이블명 복사) — PreviewTable 헤더 메뉴와 같은 관용구(fixed 좌표, 바깥 mousedown 닫기)에 팬·줌 시작 시 닫기와 `clampMenuPosition` 가장자리 클램프를 더함. 미리보기는 `/?table=&preview=1` 원샷 딥링크 — 홈이 선택 후 자동 오픈하고 파라미터를 소진(미허용 스키마면 소진만, 상세의 잠긴 버튼·사유가 설명), 복사는 http 배포(비보안 컨텍스트) 대비 execCommand 폴백 + 「복사됨」 피드백. 좌측 스키마 필터는 홈 좌측 레일과 같은 자리·접기 지원(접힌 상태에 선택 배지) — `filterGraphBySchema` 순수 함수로 노드·경계 엣지를 거르고 fitView 1회 가드를 풀어 남은 그래프에 재맞춤, 검색도 필터된 목록만 뒤진다. 미리보기 허용/미허용은 스키마 행에서 바로 보이게 잠금 아이콘+툴팁(홈 DB 탭 + ERD 필터, LockIcon 신설). 검증: vitest 86/86(+filter·clamp·menu 순수 로직 8케이스)·tsc·eslint 클린, 헤드리스 실측 — 메뉴 항목 구성·비활성 0.35+툴팁·클립보드 실값·상세/검증 네비게이션·딥링크 자동 오픈(허용)/차단(미허용)·잠금 아이콘 유무 전환·가장자리 클램프 전건 통과.

- **UX 감사 → 일괄 폴리시** (feature/ux-polish) — "게으른 마우스·통일성 결벽" 디자이너 관점으로 전 화면을 헤드리스 실측(스크린샷 + computed style 전수)해 15건을 수정. 실측으로만 잡힌 대표: 조인키 **+N 더보기 버튼이 1440px에서 화면 밖**(x=1443, 스크롤바는 hover 전 투명이라 단서 0) → 스트립 밖 형제로 이동, **비활성 secondary/icon 버튼이 opacity 1**로 활성과 동일(`.btn-primary:disabled`만 존재) → 비활성 문법 전 버튼 통일, **ERD 초기 뷰 minZoom 0.1 스트립** — 원인은 ELK가 아니라 layoutGroups의 연결요소 세로 일렬 적층 → `packGroupRows` 행 패킹(순수 함수+테스트 5케이스)으로 0.17에 뷰포트형 그리드, **다크 그립 틴트 rgba(0,0,0,.05)가 #1a1a1a 위에서 식별 불가** → `--ink` 기반 color-mix(컬럼 행 호버 동일). 나머지: primary/secondary 2px 높이 차(투명 보더), 헤더 유틸 26/28px·radius 혼재 고정(header 스코프 — ERD 노드 헤더는 HEADER_H=36 산식이라 제외), `:focus-visible` 브랜드 링(UA 블루 → `--focus-blue`), 전역 reduced-motion 가드, verify 스텝 버튼 라벨 옆 재배치(ml-auto 우측 밀착 제거)+헤더 중복 안내 제거+대기열 선택 표시(옐로 좌보더 관례 통일), 챗패널 트리거 아래 앵커(ERD 범례 가림 해소), 파싱 격리 뷰 → `/?table=` 딥링크(막다른 목록 해소)+그리드 직사각화(row-span-3), admin 수집 버튼 위계 통일·비밀번호 경고 중복 축약, ERD 검색어 유지·노드 토글 히트박스 ::after 확장. 검증: vitest 80/80·tsc·eslint 클린 + 수정 전후 같은 항목 헤드리스 재실측으로 전건 개선 확인.

## 2026-08-08

- **ERD 노드 드래그 + 헤더 호버 + 위치 초기화 — 브랜치 머지 완료** (feature/erd-node-drag, 5커밋, 프론트 전용). 헤더만 드래그 그립(`dragHandle` — 컬럼 행 클릭·스크롤 비간섭, grab 커서·호버 틴트가 그립을 알림), 수동 위치는 `movedRef` + `applyManualPositions` 순수 함수로 ELK 재배치 위에 덮어써 펼침/접힘에도 유지(ref 저장이라 드래그가 ELK 재실행을 안 유발, 세션 한정), 초기화는 ELK 재실행 없이 `elkPlacedRef` 캐시 복원(카메라 불변, 이동 없으면 비활성). 핵심 제약: `onNodesChange`는 position 변경만 통과 — dimension까지 적용하면 기존 measured 수동 관리(엣지 플래시 방지)와 충돌. 리뷰 루프가 잡은 실결함: 초기화가 `placedRef`/`elkPlacedRef`를 같은 Map으로 앨리어싱해 다음 드래그가 순수 ELK 캐시를 오염(드래그→초기화→드래그→초기화 시 복원 실패) — `new Map` 복사로 수정, 헤드리스에서 멱등성 실증. 검증: vitest 75/75(+병합 함수 3케이스)·tsc·lint + 실브라우저 5항목 + 헤드리스 Playwright 12어서션. 검증 해프닝: Chrome 창 숨김 시 rAF 스로틀로 React Flow 초기화·카메라가 멈춰 회귀로 오인 — main A/B로 앱 무관 확증. 파킹: 비활성 초기화 버튼 툴팁이 xyflow 기본 `pointer-events:none`에 막힘(접근성 후속), 다크 테마 호버 틴트 미묘(행 호버 관례 동일 계열), ELK 완료가 드래그 도중 착지 시 순간 스냅백(자기 치유). 스펙/계획: `docs/superpowers/{specs,plans}/2026-08-08-erd-node-drag*.md`.

## 2026-08-07

- **ERD 호버 동작 재조정** — 실사용 피드백: 호버만으로 노드가 펼쳐지면 포인터 아래 그래프가 요동. 펼침을 엣지 **클릭**으로 이동(카드 열기와 동시), 호버는 라벨·강조 + 이미 펼쳐진 노드의 컬럼 하이라이트만. 라벨 가독성 위해 ELK 간격 증가(레이어 간 80→110, 노드 간 32→44).

- **ERD·검색 UX 개선 — 브랜치 머지 완료** (feature/erd-search-ux, 12커밋, 프론트 전용). 사용자 피드백 9건 전부 반영: 꺾은선 엣지 + 호버 컬럼 내비(자동 펼침은 해제 후 유지 — 요동 방지), 범례 우하(엣지 카드와 세로 스택), ERD 맵 검색(애니메이션 포커싱), ELK 간격, 헤더 더블클릭 토글(+캔버스 줌 오발동 차단), 호버 전용 페어 라벨, 컬럼 행 한 줄 고정, 검증 콤보박스, 4단계 검색 랭킹(`search-rank.ts`) 3곳 통일. 리뷰 루프가 잡은 실결함: 빈 검색어에서 기본 목록 순서 파괴(정렬 가드), 범례·카드 동일 앵커 겹침, measured 미동봉 노드 교체로 호버 엣지 1프레임 언마운트(@xyflow/system 소스 실측 — `measured` 동봉으로 근본 수정), d3 dblclick.zoom 동시 발동. 파킹: rank3(이름 순서 유사)이 컬럼 매치보다 선점하는 랭킹 체감(실사용 피드백 대기), 콤보박스 드롭다운 클리핑(portal 미도입), 60행 청크 밖 컬럼 스크롤 no-op. **시각 스모크 미수행**(브라우저 확장 미연결) — 호버 인터랙션 체감 확인 필요.

- **ERD·검색 UX 개선 설계·계획** — 머지 직후 사용자 피드백 9건(꺾은선 엣지·호버 컬럼 내비·범례 이동·맵 검색·더블클릭 토글·엣지 라벨·타입 줄바꿈·검증 콤보박스·검색 랭킹 통일)을 스펙+6태스크 계획으로. 핵심 결정: 검색 랭킹은 백엔드 무변경 — 전량 로드 목록 위 클라이언트 4단계(정확>접두어>포함>순서 유사) 공통 lib, 호버 자동 펼침은 해제 후에도 유지(레이아웃 요동 방지), 엣지 라벨은 호버 시에만. 스펙/계획: `docs/superpowers/{specs,plans}/2026-08-07-erd-search-ux*.md`.

- **검증 분리 + 읽기 전용 ERD — 브랜치 머지 완료** (feature/verify-page-readonly-erd, 21커밋). `/verify` 신설(피커→후보 페어→게이트→containment→프리뷰→확정, 검증 대기 목록+AI 제안 이사)·`/erd` 재작성(confirmed+FK 전체 그래프, 연결요소 클러스터, `?focus=` 딥링크)·앵커 그래프 API와 캔버스 검증 UI(ErdCanvas·JoinBuilder 등 10파일) 삭제. 주요 결정: 게이트 샘플 통계는 페어가 아닌 **컬럼 속성으로 캐시**(0014), 게이트는 원본 값 비노출이라 allowlist·감사 무관, 빈 표본은 차단 근거로 안 씀(ratio 1.0), 검증 UI 경합(페어 전환 중 stale 응답)은 요청 시점 페어 캡처로 폐기. 리뷰 루프가 잡은 실결함: 수동 페어 선택 불능, stale 응답의 게이트 우회, union-find 무한 루프, last_verified_at 커버리지 유실 — 전부 수정 후 재리뷰 통과. 검증: 백엔드 330 passed·프론트 62+tsc+lint+build, API 스모크(43건 대기→게이트 캐시 적중→확정→ERD 성장) 통과. **시각 스모크는 미수행**(브라우저 확장 미연결) — 후속 확인 필요. 파킹: 근거 카드 kind 축약, erd.aiNotice 키 네이밍, 링크 label 인코딩, 검색 stale 가드, ErdCanvas-* testid 잔재, docs/ui-review.md 구 시나리오, relations confirm의 rejected 미거부(기존 코드).

- **검증 분리 구현 계획 작성** — 스펙을 17개 태스크(백엔드 9 → /verify 2 → ERD 2 → 정리 4)로 분해, 태스크별 실제 코드·테스트 포함. 사전 정찰로 확정한 사실: `get_db`가 요청 성공 시 커밋이라 게이트 캐시는 flush만으로 지속, `runJoinPreview`/`fetchGraph`는 ErdCanvas 전용이라 삭제 안전. 계획: `docs/superpowers/plans/2026-08-07-verify-page-readonly-erd.md`.

- **검증 분리 + 읽기 전용 ERD 설계 확정** — 캔버스 위 검증(JoinBuilder 드래그·AI 제안·스캔)이 버그의 온상이라 역할을 분리하기로. 검증은 신설 `/verify`에서 1:1 흐름(게이트 → containment → 프리뷰 → 확정)으로, ERD는 confirmed+FK만 그리는 읽기 전용 전체 그래프로(앵커·검색·뷰 제거 — 뷰 계보는 TableDetail lineage가 이미 담당). 핵심 결정: 사전 게이트는 값 겹침이 아니라 **타입 패밀리 + TOP 200 유니크니스**로 판정(TOP 200은 인덱스 순서라 겹침 검사는 오차단), 샘플 통계는 컬럼 단위 캐시. 백엔드 3단계 파이프라인은 이미 존재(containment·preview·confirm)해 신규는 gate·pair-candidates·`/api/erd`뿐. 스펙: `docs/superpowers/specs/2026-08-07-verify-page-readonly-erd-design.md`.

## 2026-08-06

- **로컬에서 실제로 띄워 보고 잡은 두 건** — 화면을 띄우지 않았으면 못 봤을 것들. (1) 직전 커밋의 행 액션 숨김이 **비활성 버튼에는 안 먹고 있었다** — `.btn-primary:disabled`(0,2,0)가 `.row-action`(0,1,0)을 이겨, 비밀번호를 넣기 전 「허용 추가」가 hover 없이도 계속 보였다. 스크린샷에서 "왜 보이지?"로 시작해 CDP로 계산값을 재니 opacity가 0이 아니라 정확히 `0.35`(= `.btn-primary:disabled` 값)여서 특이도 문제로 확정됐다. `:disabled` 변형을 추가하고 규칙을 `.btn-primary:disabled` **뒤로** 옮겨 해결(같은 특이도는 순서가 결정) — hover 시엔 흐리게 드러나 왜 못 누르는지가 보인다. 교훈은 CSS 유틸을 새로 만들 때 기존 상태 규칙(`:disabled`·`:hover`)과의 특이도를 같이 봐야 한다는 것. (2) `tools/seed_ui_states.py`가 `/api/ai/suggest-relations`의 202+job_id 전환을 못 따라가 `KeyError: 'created'`로 죽어 있었고, **그 뒤의 AI 요약·화이트리스트 시딩까지 통째로 건너뛰고 있었다** — 완료까지 폴링하도록 고쳐 `docs/ui-review.md`의 리허설이 끝까지 돌게 했다.

- **관리 콘솔 4건 — 감사 로그 페이지 신설 + 행 액션 정리 + ERD 탭 제거** — 사용자가 실사용에서 짚은 것들. (1) 감춤 상태에선 관리 화면도 스키마 **이름 대신 건수만** 보여준다 — 목록에서 뺀 이유가 존재를 안 드러내는 것인데 관리 화면에 이름이 남으면 앞뒤가 안 맞는다. 다만 이름은 `/api/objects/hidden-schemas`로 어차피 나간다(화면이 차단·숨김을 적용하려면 알아야 한다) — **표시 정책이지 비밀 보장이 아니다**. (2) 표의 행 액션 버튼이 좁은 열에서 라벨이 줄바꿈되며 버튼·행 높이를 밀어 올렸다 — `.row-action`으로 nowrap + 행 hover에서만 노출로 통일(미리보기 허용·로그인 화이트리스트 양쪽). `opacity: 0`만 두면 안 보이는 「허용 해제」가 눌리므로 `pointer-events`를 같이 끄되, tab 순서는 그 영향을 안 받아 `:focus-within`으로 키보드 접근은 살렸다. (3) 감사 로그는 **쓰기가 이미 다 있었고**(허용 등록·해제, 화이트리스트, 감춤 토글, 실값 반출) 읽을 화면이 없던 것이라 조회 API + `/admin/audit` 페이지만 붙였다 — 동작별 필터·페이징이 있고, `total`을 함께 주는 건 잘린 목록이 "이게 전부"라고 거짓말하지 않게 하려는 것(objects 검색과 같은 이유). (4) 상단 ERD 탭 제거 — 앵커 없이 들어가면 빈 캔버스라 실질적 진입점이 아니었다(테이블 상세의 「ERD 열기」·딥링크만 의미가 있다). `/erd` 라우트는 그대로.

- **감춘 스키마를 좌측 목록에서도 빼고, 노출 여부를 관리 콘솔 토글로** — 실제 스키마에 적용해 보니 테이블은 못 열리지만 왼쪽 스키마 목록과 테이블 목록에는 그대로 남아 있는 게 의도와 달랐다(사용자 확인). 권한을 두 층으로 쪼갠 게 이번 결정의 핵심이다 — **무엇을 감출지는 `HIDDEN_SCHEMAS` 환경변수**가 계속 쥐고(배포 권한 없이는 못 바꾼다), **이름을 목록에 노출할지만** 관리 콘솔에서 토글한다. 노출을 넓히는 방향이라 게이트는 미리보기 허용 목록과 같은 `PREVIEW_ADMIN_PASSWORD`를 재사용하고 감사 로그에 남긴다. 토글은 표시 방식일 뿐이라 켜도 컬럼·검증·진입은 그대로 막혀 있다(테스트로 고정). 저장은 `app_flags` KV 한 줄(0013) — 행이 없을 때 기본값을 "안 그림"으로 둔 건, 감추라고 설정했는데 배포 직후 목록에 보이면 설정이 안 먹은 것처럼 읽히기 때문이다. 프론트 필터 지점은 두 곳이었다: 카테고리 집계와 테이블 목록은 `typedObjects` 하나에서 갈라져 나오지만, 좌측 DB(스키마) 목록만은 카탈로그가 아니라 `/api/schema-categories`에서 와서 그 필터를 안 탄다 — 같은 규칙을 따로 걸어야 했다.

- **스키마 전환이 놓친 문서 잔재 정리** — 허용 단위를 qname→schema로 옮긴 `93de63a`가 운영 문서 세 곳을 빠뜨렸다(`.env.example`, README 트러블슈팅표, `n8n/workflows/README.md`) — 셋 다 "미리보기 허용 테이블"로 안내해 관리 콘솔에서 찾을 수 없는 메뉴를 가리키고 있었다. 같은 줄에 `HIDDEN_SCHEMAS`가 허용보다 우선한다는 안내도 넣었다(등록했는데도 안 열리는 경우의 유일한 남은 원인). `PROGRESS.md`의 당시 기록과 마이그레이션 0011의 docstring은 그대로 뒀다 — 둘 다 그 시점에 실제로 한 일을 적은 이력이라 고치면 오히려 사실과 어긋난다.

- **`HIDDEN_SCHEMAS` — 이름은 남기고 컬럼만 감추는 스키마 정책** — 매핑 테이블처럼 "존재는 알아야 하지만 컬럼 단위로 파고들 필요는 없는" 스키마를 위한 축이다(사용자 요청). 미리보기 허용 목록과 **독립**이고 통제 대상이 다르다 — 저쪽은 *값*, 이쪽은 *구조(컬럼)*. 두 정책이 겹치면 감춤이 이긴다(허용 목록에 잘못 올라와도 막힌다). 차단은 백엔드에서 강제한다: 컬럼이 나가던 네 경로(detail·columns-index·ERD graph·candidates)에서 빼고, 컬럼 id만 알면 우회되던 검증 경로(containment·2-way/N-way 미리보기·batch join-check)는 403. 지렛대는 `load_scoring_columns` 한 곳이었다 — 후보 추천·배치 조인체크·T3 스캔·AI 제안이 이 로더를 공유하므로 여기서 한 번 거르면 네 경로가 갈라지지 않는다. 다만 로더가 조용히 비면 "후보 0건"으로 끝나 이유를 알 수 없어, 진입점마다 명시적 403을 따로 뒀다. ERD는 노드째 제외(사용자 결정) — `qname_to_id`에서 빼면 인접 계산·엣지가 자동으로 따라오지만 FK·lineage 로더는 객체 id로 직접 조회해 그 경로를 안 타므로 별도 필터가 필요했다. 프론트는 `TableRef` 한 곳에서 링크를 죽인다(관계·FK·유사·조인체크 목록이 전부 이 컴포넌트를 지난다) + 목록 항목 비활성 + 컬럼 영역에 사유 표시. 훅은 모듈 단위 캐시를 둔다 — 관계 링크마다 호출되므로 캐시가 없으면 같은 응답을 수십 번 받는다. 대소문자 무시로 비교하는 건 운영자가 케이스를 틀렸을 때 조용히 열리는 걸 막기 위해서다.

- **N-웨이 조인 미리보기가 허용 목록을 통째로 우회하던 구멍** — "조인 검증은 미리보기 권한과 어떻게 상호작용하나"라는 질문에서 드러났다. 정책은 두 층으로 갈리는 게 맞다: `containment` 판정은 집계값(비율·고아 수·카디널리티)만 내보내므로 허용 목록과 무관하게 열려 있고, 실값이 나가는 경로만 막는다. 그런데 그 게이트가 `objects.py`(테이블 미리보기)와 `validate.py`(2-way 조인 샘플)에만 있었고, **조인 빌더가 실제로 호출하는 유일한 경로인 `POST /api/join/preview`(N-way)에는 없었다** — `is_preview_allowed`를 import조차 하지 않았고 라우터에도 로그인 게이트만 붙어 있었다. 즉 차단된 스키마라도 ERD에서 컬럼 둘을 끌어 「미리보기」를 누르면 실제 행 20건이 그대로 나왔다. 아이러니하게도 막혀 있던 2-way 경로는 프론트에서 쓰이지 않는다. `validate.py`의 주석이 정확히 이 위험("여기가 열려 있으면 허용 목록이 우회된다")을 적어 뒀는데도 새 경로가 그 옆에 생기며 빠졌고, `test_join_preview_uses_the_same_allowlist`라는 이름의 테스트가 2-way만 검사해 커버된 것처럼 보이던 것이 발견을 늦췄다. 스텝의 참여 테이블 전부를 모아 하나라도 닫혀 있으면 403(막힌 테이블명을 `context.objects`에 전부 실어서) — 조인 결과 행은 열린 쪽과 닫힌 쪽 컬럼이 한 줄에 붙어 나오므로 부분 허용이 성립하지 않는다. 화면도 `getBlockedPreviewTables`로 같은 규칙을 미리 적용해 버튼을 잠그고 이유를 옆에 띄운다(허용 목록 조회 실패 = 빈 집합 = 전부 잠금, 기존 판단 유지). 직전 커밋의 드래그 수정이 이 경로의 접근성을 직접 높인다는 점에서 타이밍도 맞물렸다.

- **ERD 조인 드래그가 처음부터 잡히지 않던 문제** — "컬럼을 끌 수가 없다"는 제보로 시작. 컬럼 핸들은 `top:0; bottom:0`으로 행 전체를 그립으로 삼으려 했지만 React Flow 기본 `.react-flow__handle`의 `height:6px`가 살아 있어, CSS over-constrained 규칙이 `bottom`을 버리고 **22px 행의 위 8px만** 히트박스로 남겼다(실측: 행 세로 중앙의 `elementFromPoint`가 핸들이 아니라 `.erd-node__row`를 반환 → pointerdown이 핸들에 닿지 않아 드래그가 시작조차 안 됨). 드롭은 `connectionRadius` 최근접 탐색이라 관대했던 탓에 "시작만 안 되는" 형태로 숨어 있었다. 앞선 커밋(`c186b95`)이 같은 증상을 고치며 폭(12px)만 반영되고 높이는 조용히 무시된 것이 직접 원인. 높이를 명시(`height:100%` + `border-box`)해 100% 커버로 되돌리고, 함께 두 가지를 더 손봤다 — 기본 `connectionMode: strict`는 source(오른쪽)→target(왼쪽) 방향만 허용해 **상대 테이블이 왼쪽에 있을 때의 자연스러운 드래그가 조용히 거부**되므로 `Loose`로 전환(자기조인은 `canAddStep`이 이미 `same_table`로 막는다), 그리고 `opacity:0` 핸들이라 어디를 잡아야 하는지 단서가 전혀 없던 문제를 행 hover 시 그립 바 노출로 해결했다. Loose는 왼쪽 그립에서 출발하면 React Flow가 source/target을 뒤집어 넘기는데 이 방향이 containment 기준이자 첫 스텝의 FROM이라 의미가 있어, `dragOriginRef`로 **"끌기 시작한 쪽 = left"**를 고정했다(`onConnect`가 `onConnectEnd`보다 먼저 불리는 것을 라이브러리 소스로 확인). 남은 것: 노드 헤더의 좌표 전용 핸들이 드롭 스냅 후보에 섞여 헤더에 떨어뜨리면 무반응 — `isConnectable={false}`가 답이지만 이번 범위 밖으로 뒀다.

- **미리보기 허용을 테이블 단위 → 스키마 단위로** — 관리 콘솔에서 "지금 뭐가 열려 있나"를 보려면 테이블명을 검색해야만 후보가 뜨는 구조였고, 실제 운영 단위는 개별 표가 아니라 스키마라는 판단으로 정책 자체를 옮겼다(사용자 결정). `PreviewAllowlist` PK를 `qname`→`schema`로 바꾸고(0012), 테이블 미리보기·조인 샘플 두 경로 모두 객체의 스키마로 판정한다. **노출 범위가 넓어지는 변환이라** 마이그레이션은 기존 qname 행을 스키마 1건으로 접되(테이블 하나만 열려 있던 스키마가 통째로 열린다) docstring과 README에 그 사실을 명시했다 — 좁히려면 업그레이드 후 콘솔에서 해당 스키마를 뺀다. downgrade는 어느 테이블이 열려 있었는지 복원할 정보가 없어 빈 표(전부 차단)로 되돌린다. 관리 화면은 검색해야 뜨던 후보 목록을 걷어내고 **카탈로그의 전체 스키마 + 객체 수 + 허용 여부**를 AD 사용자 목록과 같은 `max-h-96` 스크롤 박스에 상시 노출한다(허용된 것 상단 정렬). 픽스처가 단일 스키마라 조인 샘플의 "한쪽만 열면 여전히 차단" 케이스는 표현할 수 없어, 대신 닫힌 상태에서 오류 컨텍스트에 **양쪽 객체가 모두** 실리는지로 양방향 검사를 고정했다. 요청 본문 필드명은 `schema`(ingest 계약과 동일) — pydantic이 BaseModel 예약 이름과 충돌해 alias를 쓰는데 FastAPI 본문 모델에선 오탐 경고를 내며, alias 동작 자체는 테스트로 잠갔다.

- **화면 사용성 수정 7건 — 잘림·겹침·막다른 링크** — 사용자가 실제로 쓰다 걸린 것들만 골라 손봤다. 뿌리가 하나인 게 둘 있었다: 좌측 레일과 ⓘ 툴팁은 **같은 `overflow` 컨테이너 문제**로, 말풍선이 `absolute`라 레일 경계에서 잘리고 있었다 — 말풍선을 `body`로 포털해 `fixed`로 띄우고 좌표를 아이콘 위치에서 계산하게 바꿔 레일뿐 아니라 상세·미리보기 등 다른 스크롤 영역의 툴팁까지 한 번에 풀었다(`align` prop 의미는 유지해 10개 호출부 무수정). 레일 자체는 `.scroll-area--y`로 가로 스크롤만 끄고(탭 칩이 176px를 몇 px 넘겨 생기던 것) 탭 헤더를 sticky로 고정했다 — `py-3`을 헤더의 `pt-3`으로 옮겨야 스크롤된 목록이 헤더 위 여백으로 비치지 않는다. 컬럼 칩은 수십 개짜리 테이블이 나머지 섹션을 화면 밖으로 밀어내던 문제라 2줄로 접고 `ResizeObserver`로 실제 높이를 재서 max-height 전환을 건다(칩 줄바꿈은 패널 폭에 따라 달라져 상수로는 「더보기」 노출 여부를 못 정한다). 나머지 셋: 「이 테이블을 사용하는 뷰」가 평문이라 막다른 목록이던 것을 기존 `TableRef`로 배선, 카테고리를 바꿔 선택된 표가 목록에서 빠지면 상세도 닫도록(목록에 없는 표의 상세가 남으면 무엇을 보는지 어긋난다), ERD 검색창이 뷰 표시 토글과 **좌표가 정확히 같아**(둘 다 `left-3 top-3`) 토글을 덮던 것을 `top-14`로 내리고 기본 접힘으로.

- **최종 전체 리뷰 반영 — Critical 2건 포함 수정 3웨이브** — 태스크별 리뷰 18회를 다 통과한 뒤 브랜치 전체를 한 번에 본 리뷰가 통합 지점의 결함을 찾아냈다. 가장 위험한 것은 **W2 재임포트가 잘 돌던 기능을 죽이는** 문제였다: Webhook은 `responseData: allEntries`(= 마지막 노드 아이템 **배열**)인데 새 `Attach query` 노드는 `{query, rows}` 아이템 하나를 내므로 본문이 `[{query,rows}]`가 되고, `isinstance(payload, dict)`로 분기하는 파서는 이를 레거시로 흘려보내 래퍼를 *한 행*으로 반환한다 → `containment()`가 `KeyError`로 500. 즉 N-웨이를 켜려고 워크플로를 올리는 순간 드래그→판정이 죽는 구조였다(같은 설정의 W1을 리스트로 파싱하는 `collect_runner.py`가 방증). W2를 `firstEntryJson`으로 바꾸고 파서는 단일 원소 리스트도 받아들여 반쪽 배포가 폭발 대신 degrade하게 했다. 두 번째 Critical은 **LEFT JOIN 처방이 드래그 방향의 절반에서 정반대로 나가던 것** — 아직 안 들어온 테이블에서 끌면 `LEFT JOIN <새테이블>`이 생성돼 판정이 고아를 센 쪽이 아니라 반대쪽을 보존한다. 「LEFT JOIN 적용」을 누른 사용자가 지키려던 그 행을 잃는다. 새 테이블이 왼쪽일 때 `RIGHT JOIN`으로 뒤집고, 양쪽이 이미 바인딩된 자리(=`AND`라 독립 방향이 없음)는 조용히 무시하는 대신 명시적으로 거부하게 했다. 카디널리티 라벨도 뒤집혀 있었다 — 백엔드는 *타깃이 유니크할 때* `1:N`을 내보냈는데 `src:tgt` 표기로는 `N:1`이라, 까그발이 PK 쪽에 붙고 있었다(스펙 §8이 지목했던 위험을 계획이 "정규화 불필요"로 단정하고 그 단정을 테스트로 잠근 것이 원인 — 스펙 문구도 정정). 그 외: 백엔드 오류가 전부 "값 데이터가 없어 검증할 수 없습니다"로 표시되던 문제(`status:"failed"` 렌더 분기 부재 — 위 Critical을 가리는 경로였다), 딥링크가 후보를 한 개도 못 켜던 무동작(후보 보유 노드 자동 펼침 + 스펙 §1.2.3의 미구현분인 첫 후보 스크롤 구현), 「빌더에 추가」가 타깃을 버리고 이동만 하던 것(드롭과 같은 코드 경로로 스텝을 실제로 추가), 중간 스텝 삭제 시 끊긴 드래프트가 남던 것(연쇄 삭제 — 백엔드의 순서 의존 규칙과 어긋나는 union-find는 반례로 기각), 그리고 스펙이 약속했으나 사라져 있던 관계 확정 경로 복원. 마스킹은 이 브랜치 유일의 보안 불변식인데 테스트가 없어, 실제 치환 경로와 W2 별칭 형식 일치를 함께 고정했다.
- **Phase 5 마무리 — 검증 패널 삭제 2건 + 발견→빌더 다리 놓기 + T2/T3 용어 제거 (Task 16~18)** — 드래그 조인 빌더가 자리잡으면서 옛 검증 UI인 `ColumnPanel`(Task 16)과 `ColumnCheckModal`(Task 17)을 차례로 걷어내고 컬럼 클릭은 ERD 딥링크로 직행하도록 정리했다. 다만 `TableDetail`의 테이블 단위 일괄 조인검증은 "이 테이블은 뭐랑 조인되나"라는, 드래그로는 답할 수 없는 질문을 답하는 발견 기능이라 남기기로 하고, 대신 그 결과가 빌더로 이어지지 못하던 단절을 메웠다 — 응답에 `src_column_id`·`tgt_column_id`를 실어(이름만으로는 컬럼을 특정할 수 없어서) 결과 행마다 [빌더에 추가] 버튼을 달아 발견에서 빌더로 한 번에 넘어가게 했다(Task 18). 화면에 남아 있던 T2/T3라는 내부 검증 계층 명칭도 `panel.verify`·`joincheck.*` 사용자 문구에서 걷어내 실질 설명("실데이터 포함률 검증" 등)으로 바꾸고, 앞선 삭제로 무참조가 된 i18n 키 2개(`detail.columnsHint`·`join.stepFailed`)를 마저 정리했다 — 코드 주석·백엔드 식별자의 T2/T3는 실제 아키텍처 용어라 그대로 남겼다.
- **`ColumnCheckModal` 삭제 — 컬럼 클릭이 ERD 조인 빌더로 직행 (Task 17)** — 컬럼 칩 클릭 시 상위 후보 3건에 자동으로 실 DB 질의를 돌리며 진행바를 보여주던 모달을 없애고, `handleOpenColumn`을 옛 `handleContinueErd`의 라우팅 로직(Task 16이 만든 `/erd?col=` 딥링크)으로 교체했다 — 드래그 조인 빌더가 생긴 뒤로는 클릭 한 번에 예비 질의 세 번을 미리 쏘기보다 ERD에서 드래그로 바로 검증하는 편이 자연스럽다는 판단. 모달 전용 `check.*` i18n 키 12개(`check.target` 포함 — 이 키는 삭제 전에도 모달 안에서 실제로는 미사용이었음)는 grep 무참조 확인 후 제거했고, `fetchCandidates`·`runContainment`는 `ErdCanvas`가 여전히 호출해 이번엔 `api.ts`에 새 orphan이 남지 않았다.
- **`ColumnPanel` 삭제 — 딥링크를 드래그 대기 상태로 재해석 (Task 16)** — 드래그 조인 빌더가 대체한 옛 T2/T3 검증 사이드패널 `ColumnPanel.tsx`를 삭제했다. 마지막 참조자였던 `ColumnCheckModal`의 `PATTERN_LABELS` import는 이미 이관돼 있던 `join-verdict.ts`로 재접속했고, `/erd?col=` 딥링크는 패널을 여는 대신 `ErdCanvas`의 새 `initialColumnId` prop으로 그 컬럼의 T1 후보를 미리 하이라이트해 드래그 출발점만 알려주는 방식으로 바꿨다(컬럼 행 클릭은 이제 no-op — 드래그가 유일한 조작이라 `onSelectColumn`은 존치만 하고 배선은 남겨둠). `ColumnPanel` 전용이던 i18n 키 12개(`panel.preview20`·`panel.confirm`·`panel.noCandidates`·`panel.history`·`scan.*` 7개·`ai.explainValidation`)는 grep 무참조 확인 후 제거했고(`panel.verify`는 `TableDetail`이 여전히 써서 존치), 같은 이유로 무참조가 된 `api.ts`의 `confirmRelation`·`explainValidationAi`·`fetchHistory`·`runPreview`는 이번 태스크 범위 밖이라 정리하지 않고 남겨뒀다.

- **프론트 연결 — 조인 미리보기 SQL 탭 / 행 탭 (Task 15)** — Task 14의 `/api/join/preview`를 소비하는 첫 화면: `JoinBuilder`의 미리보기 버튼이 비어 있던 `onPreview` 스텁을 채워 실행 결과를 `JoinPreviewPanel`(SQL 탭·행 탭)로 띄운다. SQL 탭은 `result.query`를 가공 없이 그대로 렌더한다 — 프론트가 재조립하면 화면 문장과 실제 실행문이 어긋날 수 있어서다. 행 테이블은 원본 행에 안정적인 id가 없다는 점에서 출발해 `key={i}`(리뷰에서 이미 한 번 지적된 패턴) 대신 행 내용 시그니처를 키로 쓰고 완전 동일한 행은 등장 순번을 덧붙여 충돌만 피했다. null/undefined는 빈 문자열로 눙쳐 화면에 "undefined" 글자가 새지 않게 했다(문자열 "null" 자체는 실데이터라 그대로 노출). 모달 바깥 닫기는 프로젝트 전례(`abbe1c0`)를 따라 mousedown 기준으로 통일. N-웨이 미리보기는 n8n 워크플로 재배포 전이라 브라우저 종단 확인은 못 했다 — `tsc`/`eslint`/vitest(67건, 11파일) 클린만 확인.
- **`POST /api/join/preview` 추가 — N-웨이 조인 미리보기 HTTP 엔드포인트 (Task 14)** — Task 13의 `JoinStepRef`/`multi_join_preview` 위에 얹는 첫 소비자로, 원본 값이 나가는 유일한 경로라 서버 고정 상한(TOP 20, 클라이언트 무관)·응답 조립 전 컬럼별 마스킹·호출마다 `AuditLog` 기록 세 가지를 필수 불변식으로 다뤘다. `FakeJoinValidator.multi_join_preview`의 `NotImplementedError`는 503으로 매핑해 합성 조인 결과가 실값처럼 화면에 뜨는 재발을 막았다(연결성 검증은 `dbo.HR_CERT.APPOINT_NO`↔`dbo.HR_APPOINT.APPOINT_NO`라는 실제로 끊긴 FK 페어로 테스트). 코드 리뷰에서 실사용 결함 두 건이 드러나 같은 커밋에서 고쳤다: ① 마스킹 키를 테이블명만으로 두면(`"{table}.{column}"`) 스키마가 다른 동명 테이블(`ATM.PI_x`/`SAP.PI_x` — 실제 스키마 목록에 존재)을 한 조인에 넣을 때 W2가 만드는 별칭이 겹쳐 응답 JSON에서 한쪽 값이 다른 쪽을 덮어쓴다 — 마스킹 이전에 데이터 자체가 틀리는 문제라, `tools/build_n8n_workflow.py`의 `multi_join_preview` 별칭을 스키마까지 포함한 `"{schema}.{table}.{column}"`로 바꾸고 백엔드 마스킹 키도 맞췄다(node로 jsCode를 실행해 별칭이 실제로 갈리는지 검증하는 회귀 테스트 추가). ② 8스텝 최대 조인 + MSSQL 식별자 상한(128자)이면 감사 로그 경로 문자열이 `AuditLog.detail`(600자 고정 VARCHAR) 한도를 넘는데, SQLite는 길이를 강제하지 않아 테스트로는 못 잡고 프로덕션 Postgres에서만 커밋 시 500으로 죽는 잠복 결함이었다 — 컬럼 확대 대신 단계 수·행 수는 무조건 남기고 경로만 남는 공간만큼 잘라 마커를 붙이는 `_build_audit_detail`을 추가해 항상 컬럼 길이 안에 들어오게 했다(한도는 모델에서 직접 읽어 나중에 컬럼을 넓혀도 테스트가 조용히 안 맞아지지 않게).
- **W2에 `multi_join_preview` kind + 실행 SQL 반환 (ERD 조인 빌더 Task 12)** — N-웨이 조인은 프론트가 SQL을 그리면 화면과 실제 실행문이 어긋나므로, `Build query` 노드가 스텝 배열(`left/right schema·table·column`, `join_type`)을 받아 별칭 `t0..tN`으로 FROM+JOIN을 조립하게 했다. 조인 체인 3분기(오른쪽 신규/왼쪽 신규/양쪽 기바인딩) 중 세 번째는 새 JOIN을 추가하는 대신 직전 JOIN의 `ON`에 `AND` 조건을 덧붙여 별칭 중복을 막는다. 응답 계약도 바꿨다 — 새 `Attach query` 노드가 `{query, rows}` 단일 객체로 감싸 반환(`Build query`가 만든 실행문 그대로 노출, 0행 결과에서도 `alwaysOutputData`가 만드는 빈 아이템은 필터링). W2가 3→4노드가 되면서 두 실행기 공용이던 `test_executors_are_short_and_stateless`의 "3노드" 단정이 W2에서 깨져, 워크플로별 기대 노드 수를 딕셔너리로 바꿔 고쳤다(기존 계약 테스트 문서에 없던 파생 수정). 백엔드 어댑터(`n8n_query.py`)는 후속 태스크 몫이라 그대로 뒀다 — 지금은 생성기·JSON·테스트만 합의된 상태.
- **코드 리뷰 반영: "양쪽 다 바인딩됨" 분기가 엉뚱한 JOIN에 조건을 붙이던 결함 수정** — 세 번째 분기가 `from[from.length-1]`(가장 최근 clause)에 `AND`를 붙이던 원래 구현은, A-B/B-C(INNER)/C-D(LEFT) 다음에 A-C를 닫는 스텝처럼 두 endpoint 사이에 다른 JOIN이 끼어들면 그 조건이 C의 JOIN이 아니라 D의 LEFT JOIN ON절에 붙어버린다 — 예외 없이 D의 null-확장 여부만 바뀌고 A-C는 전혀 제약되지 않는 조용한 오류. alias→`from[]` 인덱스 맵(`fromIndex`)을 두고 두 alias 중 **나중에 바인딩된 쪽**의 clause를 타깃으로 고치고(그 지점이 둘 다 스코프에 든 유일한 시점), 왜 마지막 clause가 아닌지 WHY 주석을 남겨 재단순화를 막았다. node로 실제 jsCode를 실행해 산출 SQL을 검증하는 회귀 테스트를 추가(문자열 검사로는 분기별 산출이 갈리는 이 버그를 못 잡는다) — 수정 전 실행해 정확히 예측된 형태(AND가 D의 LEFT JOIN에 붙음)로 실패하는 것을 먼저 확인한 뒤 고쳤다. 부수로, 실제로는 `st.join_type`을 쓰는데 `b.join_type` 부재만 확인해 항상 통과하던 무의미한 화이트리스트 단정도 "join_type 참조는 이 삼항연산자 한 곳뿐"이라는 실질 조건으로 교체.
- **재리뷰 반영: node 미존재 시 조용한 skip 제거 + pyright 타입 오류 해소** — CI 설정이 없어 백엔드 테스트가 실제로 node 있는 환경에서 도는지 보장할 수 없는데, `@pytest.mark.skipif(NODE_BIN is None, ...)`로 조용히 건너뛰면 이 회귀 클래스의 유일한 커버리지가 그린 스위트 뒤에서 사라진다 — skip을 없애고 node 부재 시 `pytest.fail()`로 크게 실패하도록 바꿨다(문자열 검사로는 검증 불가하다는 이유를 메시지에 명시). `_run_build_query_js`가 `NODE_BIN`(`str | None`)을 좁히지 않고 `subprocess.run`에 그대로 넘겨 pyright가 `reportCallIssue`/`reportArgumentType`를 냈던 것도 함수 내부 `assert NODE_BIN is not None`으로 좁혀 해소(호출부가 이미 fail-fast로 보장하므로 런타임 동작은 그대로, 타입 체커만 만족). `pyright backend/tests/test_n8n_workflow.py` 재실행으로 해당 두 오류 소멸 확인(무관한 기존 `sys.path.insert` 기반 import 미해결 경고 1건은 그대로 — 이 테스트 파일이 원래부터 갖고 있던 정적분석 한계).
- **미리보기 허용 테이블 목록 (기본 전부 차단 + 환경변수 비밀번호 게이트)** — 실제 값이 화면에 나가는 경로가 카탈로그 전체(2,342 테이블)에 열려 있던 것을 명시 허용 목록으로 좁혔다. 키는 스냅샷이 아니라 qname이라 재수집해도 살아남는다(`preview_allowlist`, 마이그레이션 0011 — `schema_categories`와 같은 이유). 목록이 비면 전부 차단이 기본값: 설정을 잊은 배포가 열리는 쪽으로 기울지 않게 한 선택이라, live 전환 시 등록 전에는 403이 정상이다. 게이트는 테이블 미리보기(`/api/objects/{id}/preview`)와 **조인 샘플**(`/api/validate/preview`, 양쪽 테이블 모두 필요) 양쪽에 건다 — 후자를 열어두면 허용 목록이 그대로 우회된다. 목록 **수정**만 `PREVIEW_ADMIN_PASSWORD`를 요구하고(상수 시간 비교, 미설정이면 열어두는 대신 수정을 503으로 막음), 읽기는 버튼 활성 판단에 필요해 일반 사용자에게도 연다. 화면은 관리 콘솔의 관리 섹션 + 테이블 상세·ERD 우클릭 버튼 잠금 — 클라이언트 목록은 힌트고 차단은 서버가 한다. 함께: 미리보기 응답의 `source`로 0행일 때 "원본 소스가 0행 반환(쿼리는 실행됨)"을 화면에서 구분.

- **W2 미리보기 "빈 표" 원인 규명 불가 상태 해소** — 재임포트한 W2로 미리보기가 빈 표로만 나오는데, 백엔드가 n8n 응답을 `payload if isinstance(payload, list) else [payload]` 한 줄로 받아 네 가지 결과(정상 행 / 중첩 recordset / 상태 봉투 / HTTP 오류)를 전부 "0행"이나 "가짜 1행"으로 뭉갰다 — 화면만 봐서는 워크플로 비활성·credential 미연결·실제 빈 테이블을 구분할 수 없었다. 응답 형태를 명시 분류(중첩 배열 평탄화, `{"message":…}` 상태 봉투·비JSON 본문은 `N8nQueryError`로 승격, 집계인 containment의 0행은 즉시 오류)하고, HTTPError는 상태코드+본문을 인용해 4xx는 재시도 없이 즉시 보고하며, 502 예외 핸들러로 그 문자열을 화면까지 보낸다. 증상별 조치표는 `n8n/workflows/README.md` — 옛 W2와 `dbv-query` 경로를 나눠 갖는 경우, W1은 `sys.*`만 읽어 통과해도 W2는 사용자 테이블 SELECT 권한이 따로 필요한 경우가 실전 후보. W2 JSON 자체는 W1과 구조가 같아 무변경.

## 2026-08-05

- **조인 드래그가 브라우저에서 아예 시작되지 않던 회귀 수정** — 정적 검사·유닛 테스트는 전부 통과했지만 실제 브라우저에서 컬럼 행 드래그가 시작조차 안 되는 결함을 헤드리스 크롬으로 단계적 소거해 원인 두 가지를 확인했다: ① `.erd-node__scroll`의 `overflow-y: auto`가 CSS 규칙상 `overflow-x`도 `auto`로 강제해 좌우 클리핑 경계가 생기는데, 기본 `Handle`이 정확히 그 경계 위에(`left:0`/`right:0` + `translate ∓50%`) 앉아 있어 `elementFromPoint`가 핸들 대신 부모 `.erd-node` div를 반환, ② 핸들 히트박스가 1px이라 클리핑이 없어도 잡기 어려움. 스크롤 컨테이너의 `overflow-y: auto`·`position: relative`(오프셋페어런트 보정)는 그대로 두고 `TableNode.tsx`의 컬럼 행 핸들만 클립 경계 안쪽으로 3px 인셋 + 12px 실제 히트박스로 교체해 두 원인을 동시에 해소 — CSS는 무변경. Playwright 실브라우저로 5항목 전부 재현 확인(핸들 히트테스트·연결선 시작·호버 자동펼침+하이라이트·드롭 후 조인 스텝 생성·스크롤 유지), `tsc`/`eslint`/vitest 67건 그대로.
- **ERD 조인 빌더 완성 (Phase 2~3: Task 5~11)** — 컬럼 행에서 드래그를 시작하면 T1 후보가 노드별로 하이라이트되고 드롭 즉시 T2가 자동 실행돼, 결과는 수치 나열이 아니라 증상과 처방으로 뜬다("짝 없는 행 12건 — INNER로 묶으면 유실됩니다 → LEFT JOIN 권장"). 그래프가 붐빌수록 지금 조립 중인 경로를 못 알아본다는 문제엔 빌더에 든 테이블·엣지만 살리고 나머지를 낮추는 경로 강조로 대응했고, 메타데이터 기반 추천이 0개로 돌아올 때만 이름이 달라도 값이 겹치는 컬럼을 백그라운드로 훑는 「숨은 짝 찾기」를 별도 블록이 아니라 추천의 보강 수단으로 얹었다. T2/T3라는 검증 UI 용어 자체를 걷어내고 조인 조립 경험으로 대체한 것이 이 국면 전체를 관통하는 결정이다.
- **ERD 노드 컬럼 절단 폐기 — 최대높이 + 내부 청크 스크롤 (Phase 2: Task 5~6)** — 조인 빌더가 컬럼 행에서 드래그를 시작하려면 24개 절단으로 잘린 컬럼이 드래그 대상이 될 수 없다는 제약에서 출발했다: `layout.ts`의 `MAX_VISIBLE_COLUMNS` 절단을 `MAX_NODE_HEIGHT`(520px) 상한으로 대체하고 `estimateNodeSize`가 같은 값을 쓰게 해 ELK 배치와 실제 렌더가 어긋나지 않게 했다(Task 5). `TableNode`는 전체 컬럼을 그리되 `.erd-node__scroll` 내부 스크롤 + `TableList`와 같은 IntersectionObserver 청크(60행)로 프레임 끊김을 막았다(Task 6). 핵심 결정은 엣지 핸들 해석을 레이아웃 이펙트에서 분리한 것 — 스크롤 뷰포트는 노드 좌표와 무관하므로 `viewportColumns`를 ELK 레이아웃 이펙트 의존성에서 빼고, 원본 `GraphEdge`를 엣지 `data`에 실어뒀다가 별도 `anchoredEdges` useMemo에서 매 렌더 `resolveEdgeHandles`로 해석한다(스크롤이 ELK 재실행을 유발하지 않는다). `NodeAnchorInfo.visibleColumns`의 의미를 "렌더된 컬럼"에서 "스크롤 뷰포트 안의 컬럼"으로 재해석했지만 `resolveEdgeHandles` 로직 자체는 무변경(집합 멤버십만 검사).
- **ERD 엣지 시각 언어 정리 (Phase 1: Task 1~4) — 3등급 압축·까그발 마커·뷰 기본 숨김·범례 축소** — 설계에서 정한 방향(아래 [설계] 항목)을 4개 태스크로 나눠 구현했다: `getEdgeGrade`가 fk/confirmed→확정, inferred/ai_suggested→추정, unresolved→미검증, view_lineage→계보로 등급 판정을 전담하고(Task 1), 카디널리티는 텍스트 라벨 대신 까그발 SVG 마커로 그려 줌아웃·드래그 상호작용에도 안 깨지게 했다(Task 2). 뷰 882개가 실규모에서 그래프를 폭발시키므로 `showViews`를 표시 계층(`renderedIds`)에서만 필터해 기본 OFF로 전환했고(Task 3), 범례는 등급 압축을 따라 5줄 상시 노출에서 3등급+계보 4줄로 줄이고 접을 수 있게 했다(Task 4).
- **[설계] ERD 조인 빌더 + 그래프 가독성 — T2/T3 UI 폐기 결정** — "T2/T3가 직관적이지 않다"는 출발점이 브레인스토밍 중에 더 근본적인 요구로 드러났다: *ERD에서 두 개 또는 N개 테이블의 컬럼을 골라 조인 쿼리가 유효한지와 결과를 미리 보고 싶다*. 그래서 계층 용어를 다듬는 대신 검증 UI 자체를 조인 빌더로 대체하기로 했다 — 컬럼 행에서 드래그하면 T1 후보가 하이라이트되고, 드롭 즉시 T2가 자동 실행되며, 결과는 수치 나열이 아니라 **증상명+처방**("짝 없는 행 12건 — INNER로 묶으면 유실됩니다 → LEFT JOIN 권장")으로 나온다. T3는 별도 블록에서 빠져 *추천이 0개일 때만* 뜨는 「숨은 짝 찾기」가 된다. 흩어져 있던 진입점 중 `ColumnPanel`·`ColumnCheckModal`은 제거하고 `TableDetail` 일괄검증만 발견 기능으로 남긴다(빌더로 대체 불가 — "이 테이블은 뭐랑 조인되나"를 답한다). 핵심 제약은 SQL 조립처: 프론트에서 표시용 SQL을 그리면 보이는 문장과 실행 문장이 어긋나므로 **W2가 만들고 실행문을 응답에 실어 되돌려주는** 쪽을 택했고, 그 대가로 N-웨이 조인은 n8n 재배포가 전제가 된다(`multi_join_preview` kind 신설). ERD 쪽은 뷰 882개가 그래프를 폭발시키는 문제로 **뷰 렌더 기본 OFF**, 엣지 5종×3단계 투명도를 확정/추정/미검증 **3단계로 압축**(근거는 선이 아니라 클릭 시 표시), 카디널리티를 텍스트 라벨에서 까그발 마커로. 노드 컬럼 24개 절단은 드래그 대상이 잘리면 조인이 불가능하므로 폐기하고 최대높이+내부 청크 스크롤로 바꾼다 — `estimateNodeSize`에 같은 상한을 반영하지 않으면 ELK 배치가 벌어진다. 스펙: `docs/superpowers/specs/2026-08-05-erd-join-builder-design.md`.
- **분류 축을 테이블명 접두어 → DB(스키마)로 전환 + DB 필터 탭** — 실 스키마가 `ATM.PI_~`처럼 DB 단위임이 확인되면서(ATM·BCMS·SAP·LIMS… 29개) 기존 분류가 무의미해졌다: 기존 `deriveCategoryCode`는 `T_`/`TB_`/`V_` 접두어를 떼고 첫 토큰을 27개 하드코딩 목록과 대조하는 방식이었는데, 그 목록은 픽스처 생성기(`tools/fixture_gen.py:30`)와 같은 합성 데이터용이라 실 스키마는 전부 "기타"로 떨어진다. 분류 단위를 스키마로 바꾸고 매핑을 서버로 옮겼다(`schema_categories` 테이블, 마이그레이션 0010, 스키마명이 키라 재수집해도 살아남는다) — 미지정 스키마는 스키마명 자체가 카테고리라 설정 전에도 목록이 채워지고, 한 스키마의 카테고리를 바꾸면 그 DB의 테이블이 통째로 이동한다(일괄 이동). 카테고리 매핑은 조직 공용(서버), "지금 내가 볼 DB" 필터는 개인(localStorage) — 층위가 달라 저장소를 나눴다. 좌측 레일에 카테고리/DB 탭을 두고 DB 탭에서 체크박스 필터 + 인라인 카테고리 편집을 하며, 필터가 걸리면 탭에 개수를 표시해 목록이 왜 짧은지 화면에서 드러나게 했다. 테이블 목록에 스키마 접두어 노출(동명 테이블 구분 — `ATM.PI_x` vs `SAP.PI_x`).
- **테이블 목록 1,000개 절단 해소 (전량 페이징 + 무한 스크롤)** — "전체 테이블이 1000개까지만 뜬다, 근데 링크 타고 가면 다른 테이블이 나온다"는 신고: `fetchAllObjects`가 `limit=1000` 한 방이었고 서버 상한도 정확히 1000(`le=1000`)이라 실규모 3,224개(테이블 2,342+뷰 882) 중 2,224개가 **조용히** 잘렸다. 목록은 (schema, name) 정렬 상위 1,000개만 담겼는데 상세·ERD 경로는 id로 DB를 직접 조회해서, 목록에 없던 테이블이 링크로만 열리는 모순이 생겼다. 상한만 올리면 다음 규모에서 또 깨지므로 계약을 고쳤다: `/api/objects`에 `offset` 추가 + 필터 적용 후 `total` 반환(정렬에 id 타이브레이크 — 페이지 경계 중복·누락 방지), 프론트는 total까지 페이징해 전량 수집. 렌더는 별개 문제라 `TableList`를 100행 청크 무한 스크롤로 바꿨다(3,224행 일괄 렌더는 프레임이 끊긴다) — 검색·카테고리 집계는 전량 집합 위에서 돌아 초성·컬럼 검색 범위가 그대로 유지된다. 목록 상단에 전체 개수를 표기해 절단 여부를 화면에서 판별할 수 있게 했다.
- **브랜드 마크 실체화 + 아이콘 플랫 SVG 통일** — "노란 박스 안에 아이콘이 안 보인다"는 신고는 렌더 실패가 아니라 미구현이었다: `.logo-mark`는 노란 사각형만 그리는 CSS였고 DB 실린더는 `app/icon.svg`(파비콘)에만 있어 탭에만 보였다. `LogoMark` 컴포넌트로 파비콘과 같은 지오메트리를 화면에도 렌더(헤더 18px·로그인 40px, `--primary`/`--on-primary` 토큰이라 테마 대응) — 둘이 어긋나면 탭과 화면의 브랜드가 달라 보이므로 지오메트리를 맞춰 유지한다. 이모지·기하 글리프(💬 ☀︎ ☾ ✕ ▲▼ ▾▸)는 `components/icons.tsx` 플랫 라인 SVG로 일괄 교체 — 기존 SearchPanel 돋보기 규격(24 뷰박스·`currentColor`·stroke 2.4·round cap) 계승, 이모지는 OS 폰트마다 모양·색이 달라 UI 톤이 깨진다. 고아가 된 `.logo-mark` CSS 제거. 남은 글리프는 `✓`(확정 배지)·`⚠`(급락 라벨)·`+`(이웃 확장) — 텍스트 라벨에 섞여 있어 별도 판단.
- **임베딩 서버를 채팅 LLM과 분리 (사내 공통 변수명 정렬)** — 임베딩 서버 주소가 채팅 LLM과 달라 연결 불가였던 문제: 기존엔 임베딩 URL 설정이 아예 없어 `AI_BASE_URL`을 재사용했다. BPM 등 사내 타 서비스와 **같은 변수명**(`EMBED_URL`·`EMBED_MODEL`·`EMBED_TIMEOUT_SECONDS`)으로 신설해 그쪽 `.env` 값을 그대로 복사할 수 있게 했고, `AI_EMBED_MODEL`은 `EMBED_MODEL`로 이관. URL은 `/v1` 루트·`/embeddings` 전체 경로 모두 수용(복붙 시 `/embeddings/embeddings` 방지 — BPM `kb/embed_client`와 같은 규약). 사내 임베딩 서버는 무인증이라 토큰 설정을 두지 않고 채팅 토큰도 보내지 않는다(다른 호스트로 시크릿 전송 방지). 게이트를 `isinstance(ai, LlmAiClient)`에서 임베딩 설정 유무로 바꿔 **채팅 LLM 없이 임베딩만으로도 의미 검색이 동작** — 이 경우 LLM 재랭크가 불가하므로 코사인 순위를 그대로 반환한다(키워드 스코어러로 넘기면 어휘가 겹치지 않는 의미 질의가 빈 결과가 된다). `EMBED_DIM`은 벡터를 JSON 텍스트로 저장해 쓸 곳이 없어 제외. 튜닝 3종(`AI_EMBED_BATCH`·`AI_EMBED_JOB_CAP`·`AI_EMBED_SLEEP_MS`)도 `EMBED_*`로 마저 옮겨 네임스페이스를 통일했다 — 서버 `.env`에 아직 안 들어간 값이라 지금이 무비용 시점.
- **합성 데이터가 실값처럼 노출되던 경로 차단 (fake 어댑터 전수조사)** — 실서버 미리보기가 전 컬럼 `EMPCOD001` 식으로 나오던 신고에서 출발: `SOURCE_MODE=fixture`면 `FakeTablePreview`가 선택되는데 컨테이너엔 `value_sets.json`이 없어(빌드 컨텍스트 밖) 값 집합 0건 → 전 컬럼 `_sample()` 합성, 게다가 응답·화면 어디에도 합성 표시가 없었다. 실배포 판별을 값 집합 유무가 아니라 `N8N_WEBHOOK_BASE`로 잡은 이유는 실측 때문 — 픽스처 값 집합은 조인 키 전용이라 전 컬럼 커버리지 객체가 0개(`HR_EMP` 2/19)라서 값 기준으로 막으면 오프라인 데모가 전멸한다. 이미 `create_collect_runner`가 쓰던 규칙을 그대로 적용해 원천이 붙은 배포는 `SyntheticDataRefused`→503(조치 문구 포함), 순수 오프라인만 합성 유지. 어댑터 전수조사 결과 `FakeAiClient`도 `chat`에만 mock 표시가 있어 검색·요약·검증 해석·뷰 해석까지 확장하고, 목업 요약은 캐시에 넣지 않도록 했다(실 LLM 연결 후 가짜 재사용 방지 — 대신 AI 미연결 시 ERD 툴팁 요약은 비어 있다). 프론트는 목 데이터 생성기가 없어 배지 노출만 추가. `FakeJoinValidator`·`FixtureCollectRunner`는 이미 명시 실패라 무변경.
- **실서버 수집 안정화 + n8n 단문 실행기 전환 (브랜치 머지: container-memory-optimization)** — 실서버 수집 실전 결함 일괄 수정: 수집 경로를 SOURCE_MODE에서 분리(N8N_WEBHOOK_BASE 유무로 판단 — 컨테이너에 픽스처 없음), compose env 전달 누락 3건(N8N_WEBHOOK_BASE·N8N_QUERY_TIMEOUT·LDAP_CA_BUNDLE) 추가, 상대 FIXTURE_DIR 저장소 루트 앵커(`resolved_fixture_dir`), n8n webhook 응답 기본값(firstEntryJson)으로 행이 잘리던 문제(W1·W2에 allEntries 명시 — W2는 live 전환 전 선제), 제약 그룹핑을 (이름+소유 객체)로 교정(스키마 간 동명 FK 병합·PK 오플래그 수정), 카탈로그 밖 뷰 참조(함수·시노님 등)는 미해석 강등(deps_unresolved/skipped — 2단계 사망 방지), 스냅샷 밖 FK는 스킵+카운트. 구조 전환(사용자 지시 "n8n은 짧게, 연쇄는 서비스에서"): n8n을 3노드 단문 쿼리 실행기(W1)로 통합하고 수집 캐스케이드를 백엔드 `N8nCollectRunner`로 이관 — 워크플로 5→3개, `$env`·API키 참조 제거(임포트 후 편집 불필요), W1a도 객체 창(300) DB 분할로 5분+ 병목 해소, 멈춘 수집 잡 취소 API. AD 사용자 서버 검색+무한 스크롤(AdUserList 분리), Pretendard+JetBrains Mono 자가호스팅 폰트 확정.
- **AI 후속 사이클(1~5) 구현 완료** — ① 판정 근거 영속: 수용/기각 전부 relations에 reason과 함께 적재(rejected는 기존 dedupe에 걸려 재실행마다 다음 후보로 전진 — 전량 기각 정체 소멸), ERD 엣지 hover 카드·상세 행 표시, 검증 승격 시 기각 사유 자동 정리. ② suggest·임베딩 인덱싱을 ai_jobs 202+폴링으로 전환(kind별 중복 409, 마이그레이션 0009). ③ 임베딩 의미 검색: /embeddings 배치 인덱싱(잡 상한 1,000·배치 32·호출 간 500ms — 부하 제약, source_hash 증분) → 코사인 top-50 → LLM 재랭크, 임베딩 문제는 전부 키워드 자동 폴백(재랭크 실패만 502·손상 벡터 스킵). ④ 스키마 Q&A 챗: 검색 top-8 메타 컨텍스트, 전역 플로팅 패널(라우트 간 세션 유지·mock 배지·테이블 칩→ERD). 테스트 190→237(백)·33→38(프론트). 스펙: `docs/superpowers/specs/2026-08-05-ai-cycle2-design.md`, 계획: `docs/superpowers/plans/2026-08-05-ai-cycle2.md`. 최종 전체 리뷰 반영: 재기동 고아 잡 정리·unjudged 카운트 노출·임베딩 상한 2,000 강제·잡 에러 컨텍스트 보존.

## 2026-08-04

- **AI 실 LLM 프로바이더 전환 (설계→구현 완료)** — 사내 셀프호스트 LLM(OpenAI 호환 /v1/chat/completions)을 stdlib urllib 동기 클라이언트로 연결(의존성 0), `AI_BASE_URL` 유무로 Fake/LLM 스위치 — 로컬·CI는 오프라인 유지. 관계 제안은 스코어러 후보(뷰 JOIN + 정규화 동명↔PK 우주 — 전 컬럼 O(N²) 회피)를 LLM이 재판정하고, 기존 관계 중복 제거를 상한(40) 앞에 두어 재실행마다 다음 순위로 페이징(리뷰 룰링 — 픽스처 실측 597후보). 탐색은 프리필터 50 → LLM 재랭크(환각 qname 제거·NaN 점수 0 강등), 요약·검증 해석·뷰 해석은 1:1 교체(수치 창작 금지 지시). 장애는 AiUnavailableError → 502 명시(폴백 없음). 테스트 156→181. 스펙: `docs/superpowers/specs/2026-08-04-ai-llm-provider-design.md`, 계획: `docs/superpowers/plans/2026-08-04-ai-llm-provider.md`. 최종 전체 리뷰 반영: compose 기본값(구 .env 부팅 함정)·LLM 불량 출력 가드 3종·판정/생성 카운트 표기·런북 §9 검증 기준 재작성.
- **로그인 화면 리디자인 + 게이트 카드 통일** — "화면 대비 작아 답답" 피드백: 170px짜리 미니 카드를 브랜드 카드로 재구성(40px 옐로 마크 + DB-viewer 30px 타이틀 + 태그라인 + 풀폭 CTA `btn-primary--lg` + SSO 힌트, max-w-sm/p-10). silent 확인 중·수동 버튼 두 상태가 같은 카드 셸 공유(전환 점프 없음), 소문자 db-viewer 브랜딩 불일치 정정. MeGate 에러·화이트리스트 차단 카드도 같은 셸 규격으로 통일. 두 테마 실측 + 전 화면(메인·파싱·관리·ERD) 재검토 — 나머지는 기존 감사 수준 유지로 판단.
- **인증 에러 화면 탈출 동선** — `/api/me` 실패 시 순수 텍스트로 갇히던 화면(실서버에서 프록시 500으로 재현)을 카드로 교체: [다시 시도] 재조회 + [다시 로그인] 세션 제거 후 /login 수동 카드(AUTH ON에서만 렌더 — useAuth 안전 경계). 원인이 서버 장애든 토큰 만료든 막다른 화면을 남기지 않는다.
- **서버 로그인 500 근본 수정 — standalone 프록시 빌드 타임 베이크** — 실서버 첫 배포에서 로그인 시 "request failed (500)" = 전 `/api/*` 프록시 실패. 원인: `output: standalone`은 rewrites를 **빌드 시점**에 manifest로 굽는데 BACKEND_URL을 compose 런타임 env로만 주입 → 이미지에 기본값 `localhost:8000`이 고정(프론트 컨테이너 안엔 무존재). 로컬 재현 실험으로 확정(런타임 주입 9999 무시, 구운 8000만 응답) 후 BACKEND_URL을 빌드 ARG로 이동, 잘못된 Dockerfile 주석("기동 시 평가") 정정. 로컬 dev는 호스트 8000에 백엔드가 있어 절대 재현 안 되는 함정 — 런북 §2의 `/api/health` 눈 확인이 이 계층을 잡는 관문.

## 2026-08-03

- **수집 분할 + 청크 진행 UI(실측 5.7배 규모 대응)** — 벌크 지점 분할: ① W1a 카탈로그는 DB 스캔 단일 유지(반복 OFFSET이 오히려 부하 증가)하되 **전송을 객체 300개 청크**로 분할(n8n Code가 N아이템 반환 → HTTP 노드 순차 POST, ingest가 스냅샷에 이어붙임·마지막 청크에서 catalog_done), ② W1b 뷰 의존은 **DB 작업 자체를 뷰 100개 단위 분할** — 백엔드 러너가 offset/limit 페이징으로 webhook 반복 호출, 청크 콜백 확인 후 다음 진행(SQL 06/07에 뷰 윈도우 절). ③ 진행 카운터(counts.*_chunks_done/total)를 수집 패널 진행 바로 표시. 튜닝 3종(.env: COLLECT_*) 신설. 나머지 벌크 후보(T3 스캔·일괄 검증·미리보기)는 기존 상한·배치·진행 표시로 충분 확인. 테스트 156+33.
- **정지점 16 통과(실서버 정찰 완료)** — W0 결과: FK 13 / 테이블 2,342 / 뷰 882(blocked 0) / 크로스 DB 0 / 중첩 뷰 286 / DMV 스모크 정상 / warnings 없음. **실 규모가 계획 전제(409 테이블)의 5.7배** — 문서 3곳의 규모 서술을 실측으로 갱신. FK 13개는 추론 중심 설계의 전제 그대로. 도중 정찰 05 타임아웃은 n8n MSSQL credential 기본 Request Timeout(15s)이 원인 — 300s로 상향 안내(뷰 882 커서 도는 W1b에도 필수).
- **워크플로 한 세트 통합** — deploy/ 이원화가 헷갈린다는 피드백으로 `$env.DB_VIEWER_* ?? '리터럴'` 폴백 표현식 방식 전환: 로컬 리허설은 compose env가 덮어쓰고 실서버(env 불가)는 폴백 사용, 같은 파일이 양쪽 겸용. deploy/ 폴더·사본 생성 기계 제거, 임포트 절차는 n8n/workflows/README.md로 이동. 테스트로 "모든 $env 참조는 폴백 필수" 강제. 비밀키는 여전히 플레이스홀더 — UI에서 교체.
- **0행 결과 체인 중단 수정(W0 실전에서 발견)** — 실서버 W0 실행 중 recon 04(크로스 DB 참조)가 0행을 반환하자 n8n이 후속 노드를 스킵해 리포트까지 못 가는 문제 확인. 정상적으로 0행일 수 있는 쿼리 노드 6종(recon 04, 03/04 key·FK, 06/07 view deps, W2 Run query)에 `alwaysOutputData` 부여 + 조립 jsCode에 빈 아이템 가드, 백엔드 어댑터는 빈 dict 행을 빈 리스트로 정규화. FK 없는 레거시 DB가 프로젝트 전제라 수집(W1a) 실전에서도 터질 버그를 선제 수정. 수정은 생성기(tools/build_n8n_workflow.py) 단일 소스에서 — deploy/ 사본도 생성기가 함께 재생성·테스트로 정합 강제(수동 동기화 제거). 테스트 150건.
- **n8n 배포용 워크플로 사본** — 실서버 n8n이 기존 운영 중(UI 접근만 가능, 컨테이너 env 추가 불가)임이 확인되어 `n8n/workflows/deploy/`에 `$env` 참조를 리터럴로 치환한 임포트 전용 사본 5종 신설. INGEST 키만 플레이스홀더(비밀키 커밋 금지) — 임포트 후 UI에서 X-API-Key 필드 교체. 원본은 로컬 리허설(compose env 주입)용으로 유지 — 이원화 사유·동기화 규칙은 deploy/README.md에 기록. 런북 §4를 신규 docker run 전제에서 UI-only 절차로 재작성.
- **컨테이너 메모리 튜닝** — backend에 `MALLOC_ARENA_MAX=2`(glibc arena 제한으로 멀티스레드 상주 메모리 절감 — frontend/postgres는 alpine·musl이라 미적용), frontend에 `NODE_OPTIONS=--max-old-space-size=512`(Next.js 셀프호스팅 메모리 가이드). compose 리터럴로 고정 — 배포별 가변값이 아니라 .env 미등재.

## 2026-08-02

- **로딩 UX 전면 감사·적용** — 비동기 전 지점 감사 후 6곳 적용: ① **T3 전수 탐색 UI 신설**(백엔드 202+폴링·진행도는 있었으나 UI 부재) — ERD 검증 패널에 버튼+진행 바(done/total 폴링 1.5s)+상위 결과 목록, 이름 무관 100% 발견 실증(LEAD_SEQ·PICKING_SEQ). ② 일괄 검증 경과초+스켈레톤, ③ AD 전체 동기화 경과초·중복 클릭 방지(실AD 분 단위 대비), ④ ERD 그래프 계산 배지(fetch+ELK 동안), ⑤ AI 관계 제안 busy, ⑥ 공용 useElapsedSeconds 훅. 기존 양호 지점(수집 스테퍼·상세 스켈레톤·미리보기 탭)은 유지.
- **컬럼 클릭 검증 흐름 개편** — ERD 즉시 이동 대신 진행 모달 → 요약 모달 → [ERD에서 상세 검증]/[돌아가기]. 컬럼 클릭 시 상위 3 후보에 T2를 순차 실행하며 진행 바(n/N + 현재 대상 + "수십 초 가능" 안내), 요약은 containment·카디널리티·패턴·고아 표시(값 없음/실패 구분). live 장시간 검증 대비 UX — 취소 안전(cancelled 플래그), 진행 중 ERD 버튼 비활성. 기존 API 재사용(백엔드 무변경).
- **GitHub 퍼블리시 + main 통일 + 런북 실환경화** — private 리포(Nocharm/db-viewer, 사내 IP 포함이라 비공개), master→main 전환. 런북을 실제 경로(맥→GitHub→윈도우 git pull→서버 zip 업로드→Docker)로 재작성: LF 검증 한 줄(`git ls-files --eol`), n8n 신규 기동 docker run 스니펫(MSSQL 계정은 기발급), Keycloak 클라이언트 등록 표, **모든 통과 기준을 화면 눈 확인으로** 구성(사내 서버 복붙 제약 반영).
- **연결 단계 준비 완료(정지점 16~18 코드측)** — live 실행 경로를 n8n W2 경유로 확정(계획 §4.3 pyodbc 직결 대신 — 사용자 방침, 편차 기록). ① W2 쿼리 실행 워크플로: webhook 동기 응답(lastNode), kind 3종(containment LEFT JOIN 집계·join_preview·table_preview WHERE LIKE) **고정 템플릿만** 실행 — 식별자 브래킷·리터럴 이스케이프, 동적 SQL 미수신, limit 1~500 클램프 (계약 테스트로 강제). ② N8nJoinValidator·N8nTablePreview 어댑터(재시도 1회+로깅, 빈 소스 0-나눗셈 가드) — DB 자격증명은 n8n에만. ③ 게이트 전환: live는 N8N_WEBHOOK_BASE 필수(없으면 명시 오류), preview 501 제거·팩토리化. 보안 승인 게이트는 SOURCE_MODE=live 전환 절차로 유지. ④ docs/connect.md 런북(정찰→수집→live 순서·체크리스트). 테스트 149건(HTTP 목킹 어댑터 4건·W2 계약 2건 포함).
- **미리보기 컬럼 리사이즈 + SQL 보기** — ① 헤더 세로선 드래그로 컬럼 폭 조절(48~800px 클램프), 좁힌 컬럼은 말줄임(…)+호버 전체값, 더블클릭 시 오버라이드 제거로 내용 맞춤 자연 폭 복원(JS PointerEvent 검증 154→94→154px), 테이블 전환 시 초기화. ② "SQL로 보기": 현재 상태(보이는 컬럼·필터 LIKE·정렬·TOP N)와 동치인 T-SQL 생성(브래킷/따옴표 이스케이프, vitest 2건) + 클립보드 복사 — 평문 HTTP에선 navigator.clipboard가 없어 execCommand 폴백(bpm 레슨). 메인·ERD 카드 공용.
- **반응형 wrap + ⓘ 툴팁 + 아이콘 통일** — ① 메인 3열이 lg 미만에서 wrap(카테고리+목록 위 60vh 캡, 상세는 아래 전체 폭 70vh — 950px 실측), 상세 그리드 md 미만 1열·버튼/헤더 행 flex-wrap. ② InfoTip 컴포넌트(순수 CSS 말풍선, 좌/우 가장자리 정렬 변형) — 조인키·카테고리·목록·상세 7개 섹션·미리보기에 의미 설명 툴팁(ko/en 사전 11건), 컬럼 섹션의 인라인 힌트를 툴팁으로 이동해 밀도 완화. ③ 아이콘 플랫 통일: 🔑→모노 "PK" 마크(옐로), 🔍→currentColor SVG 돋보기, ⚙→ƒ — 컬러 이모지 제거로 두 테마 일관.
- **미리보기 대확장** — ① 행수 기본 20 + 선택 확장(50/100/200/500, 서버 상한 500 유지 — §3.5 상한 원칙 보존, 422 가드), ② 다중 탭 + 분할 2패널(같은 테이블 중복 열기는 탭 활성화로 차단, 탭 수명은 테이블 선택과 독립), ③ 컬럼 드롭다운 체크 숨김 + 헤더 우클릭 메뉴(오름/내림 정렬·숨김·고유값 모달 — 전부 로드된 행 기준임을 UI에 명시, lib/preview-utils 순수 함수 + vitest 3건), ④ CSV 다운로드(보이는 컬럼·현재 정렬·마스킹 유지, 엑셀 한글 BOM), ⑤ ERD 카드 상단 드래그 높이 조절(140px~뷰포트 70% 클램프, PointerEvent 시퀀스로 로직 검증 256→436px) + 공용 PreviewTable로 ERD·메인 통일. 전부 브라우저 실측(분할·고유값 모달·TOP 50 갱신).
- **파서 주석 가드** — 주석 처리된 옛 쿼리가 조인·lineage 증거로 오인되지 않도록 `strip_sql_comments` 전처리 신설(사용자 실환경 경험 반영). 현 sqlglot 버전은 기본 케이스를 이미 무시하지만 파서 버전·엣지 케이스(T-SQL 중첩 블록 주석, 미종결 주석)에 의존하지 않도록 앞단에서 확정 제거. 문자열 리터럴(`''` 이스케이프)·`[대괄호]`·`"따옴표"` 식별자 안의 주석 기호는 보존 — 회귀 테스트 4케이스.
- **뷰 1급 시민화 + AI 확장(질문으로 설계 확정)** — ① 타이틀 DB-viewer 통일, 뷰 식별 색 `--obj-view`(블루, TBL/VIEW 라벨의 보조 인코딩 — ERD 좌측 스트라이프·목록 V 칩·검색·상세 배지). ② 메인에 뷰 통합: 전체|테이블|뷰 필터 칩, 카테고리가 뷰 접두(V_/V_SUM_/V_RPT_)를 벗겨 동일 분류, 뷰 상세는 구성 테이블(lineage 역추적, 신규 base_tables) 표시 + FK·유사 섹션 숨김, 미리보기는 기존 합성 경로 그대로 재사용(백엔드 타입 가드 없음 확인). ③ ERD 우클릭 메뉴에 미리보기 → 하단 부유 와이드 카드. ④ AI 3종(전부 메타데이터 전용 어댑터 뒤): 상세 요약 생성 버튼(기존 summarize 재사용), 뷰 정의 자연어 설명(신규 explain-view — 정의·lineage·JOIN·출력 컬럼), T2 결과 자연어 진단(신규 explain-validation — 이력에서 패턴 재산출, §3.4 confidence 규칙 재사용). Q&A 챗은 실 프로바이더 연결 후로 보류(사용자 확정).
- **ERD 상호작용 개편(질문으로 설계 확정)** — ① 숨기기: 노드 우클릭 메뉴(이 테이블/다른 테이블 모두 숨기기) + 바탕 우클릭 플라이아웃(숨긴 목록·개별/전체 복원), 숨긴 노드는 레이아웃·엣지에서 제외. ② 엣지를 매칭 컬럼 행에 직접 도킹 — 컬럼별 투명 Handle + 순수 헬퍼 resolveEdgeHandles(접힘·표시상한 초과는 헤더 폴백, vitest 3건), 접기/펼치기 시 updateNodeInternals로 재측정. ③ 호버 강조: 엣지·노드 호버 → 대상 엣지 선명+굵게, 양쪽 매칭 컬럼 행 옐로 하이라이트, 나머지 엣지 투명도 0.12 — 강조는 렌더 데코레이션만(ELK 재배치 없음). ④ 검색·검증 패널을 플로팅 오버레이로(캔버스 전체폭, 검색은 접기+선택 시 자동 접힘, 검증 카드는 우측 부유). 전부 브라우저 실측.

## 2026-08-01

- **버튼 트리거 수집(단계별+전체)** — n8n 주기 수집(W1)에 더해 UI 버튼으로 수집을 단계별 제어. 구조: `/api/collect`(202+폴링, sysadmin 게이트) → CollectRunner 어댑터(fixture=리플레이·실환경=n8n webhook W1a/W1b 신설, SQL 분할 01-05/06-07) → ingest가 collect_job_id 콜백으로 잡 단계 갱신(triggered→catalog_done→deps_running→ready). full 모드는 FastAPI가 catalog_done 콜백을 대기 후 2단계 자동 체인(상한 15분). 관리 콘솔에 수집 패널(단계 버튼·스테퍼·카운트·최근 잡, 1.5s 폴링). 오프라인 실측: 1단계 → objects 495·columns 9,432 → 2단계 → ready(lineage 428·view_joins 18). 결정: 수집 잡은 마이그레이션 0008 별도 테이블(스냅샷 FK 없음 — 수명 분리).
- **ERD 헤더 드래그 이동** — React Flow controlled 노드에 onNodesChange가 없어 드래그 불가였던 것을 적용, dragHandle을 헤더로 한정(컬럼 클릭과 무충돌). 수동 배치는 ref에 기록해 접기/펼치기 재레이아웃에도 유지, 앵커 변경 시 초기화. 실측: 드래그 이동 → 더블클릭 펼침에도 제자리 유지.
- **편의 기능 묶음(4커밋)** — ① 테이블 단위 조인 검증: `POST /api/objects/{id}/join-check`가 타깃별 최고 후보 페어에 T2 일괄 실행(배치 상한 8 — T2는 원본 질의, 값 데이터 없는 페어는 no_data 표시), 상세 패널에 일괄 버튼 + 유사 테이블 행별 검증 버튼. ② 경량 i18n(사전+컨텍스트, 라이브러리 무추가) 한/영 토글 — 카테고리 라벨은 도메인 데이터라 한글 유지, admin·login 화면은 후속. ③ 헤더: 로고 클릭=의도적 풀 리로드(필터 전체 초기화), 사용자 드롭다운(로그아웃), DB 실린더 파비콘+탭 타이틀. ④ ERD 전 노드 기본 접힘(헤더+컬럼수만) — 앵커만 자동 펼침, 더블클릭 토글, 모두 펼치기/접기 툴바. 백엔드 132·프론트 24 테스트 통과, 두 테마·양 언어 브라우저 실측.
- **디자인 전면 감사(시선·마우스 동선 렌즈)** — 전 페이지 실측 후 고임팩트만 수정: ① 조인키 바 40칩 → 12칩 + "+N" 접기(선택 키는 접혀도 고정 노출), ② 상세·미리보기 헤더를 24px/700 시선 앵커로, ③ 로딩 텍스트 → 스켈레톤, 빈 상태에 아이콘, ④ /parsing을 히어로 스탯(성공률 56px)+보조 타일로 재구성 — 위험 지표(실패·미지원)는 값>0일 때만 에러색, ⑤ ColumnPanel 액션 위계(T2 검증만 btn-primary) + 후보 점수 우측 스캔축, ⑥ 미리보기 필터바 40px 높이 통일, 다크에서 라이트 보더 새던 하드코드 폴백 제거, ⑦ 헤더 옐로 로고 마크. 핵심 결정: `--stat-ink` 토큰 신설 — 옐로 스탯 텍스트는 다크 전용, 라이트에선 잉크(흰 바탕 옐로 텍스트는 실독). 두 테마 브라우저 실측 + tsc·eslint·vitest 24건 통과.
- **디자인 v2: ClickHouse 다크 + 라이트 토글** — 기준 시스템 Cohere→ClickHouse 전환(사용자 제공 분석 md 보존, design-app.md v2 재작성). 근흑 캔버스·일렉트릭 옐로 CTA(btn-primary, 블랙 텍스트)·다크 서피스 카드·Inter. 다크/라이트 두 토큰 세트 + 헤더 토글(data-theme, localStorage, 페인트 전 적용). **신뢰도 팔레트 테마별 재검증**: 다크 세트(#059669/#2a62cf/#cf5fd9/#ef4444) dataviz validator ALL PASS(CVD 7.4는 파선·배지 보조 인코딩으로 6–8 합법 대역), 라이트는 v1 검증본 유지. 구 토큰명 별칭으로 컴포넌트 수정 최소화. **조인 검증 진입점**: 위치는 ERD 우측 패널이며, 브라우저 상세의 컬럼 칩 클릭 → `/erd?col=` 딥링크로 패널 자동 오픈. 브라우저 실측: 다크/라이트 토글, 옐로 선택 상태, 딥링크 패널.
- **브라우저 3차 개선(피드백 반영)** — ① 재검색 의미 확정: 받은 20행 필터가 아니라 **원본 소스 재질의**(조건이 소스 쿼리 WHERE로 — live 실행 경로 pyodbc vs n8n은 연결 단계에서 사용자 방침대로 확정, 어댑터 주석·UI 문구 명시), ② 3열을 선 없는 카드 레이아웃으로(옅은 스톤 바탕 + 여백, panel-section도 보더 대신 면), ③ 상세 내 테이블명(유사·FK·관계) 클릭 → 선택 전환, 선택을 URL(?table=)로 관리해 **브라우저 뒤로가기가 이전 테이블 복귀** — 딥링크 부수 효과. 전부 브라우저 실측(HR_EMP_FAMILY→HR_LOG→back).
- **브라우저 2차 개선(피드백 반영)** — ① 밀도 완화(행·패널 여백 확대), ② 미리보기를 3열 아래 별도 섹션으로 분리(열리면 페이지 세로 스크롤 + scrollIntoView 자동 이동), ③ 미리보기 상단 컬럼+값 필터 재검색(백엔드 filter_column/value — fixture는 큰 풀 생성 후 필터로 live WHERE 대응, 감사에 필터 기록), ④ 검색엔진 강화: 초성(한글 카테고리 매칭)·컬럼명 검색(`/api/objects/columns-index` 1회 로드)·검색어 하이라이트 — 순수 매처 `lib/search.ts` + 테스트. 브라우저 실측: JUDGE→QC 컬럼 매칭, ㅍㅈ→품질, LOT_NO 필터 1건. 운영 레슨: dev 백엔드는 --reload로 띄울 것(구프로세스 404 삽질 2회).
- **메인 화면 전면 개편: 테이블 브라우저** — 사용자 레이아웃 요구 반영: 상단 조인키 필터 바(FK·뷰JOIN·검증관계 집계, 신규 `/api/join-keys`) → 좌측 카테고리(테이블명 접두어→한글 업무 분류, 순수함수+테스트) → 테이블 목록(이름 필터) → 우측 상세(신규 `/api/objects/{id}/detail`: 사용 뷰·컬럼명 일치율 유사 테이블·FK in/out·추론/확정 관계·AI 요약·조인키 마킹) + 미리보기 TOP 20(신규 `/preview`: §3.5 원칙 준용 — 서버 고정 상한·마스킹·감사, fixture는 값집합+타입관례 합성, live는 정지점 18에서 pyodbc 교체). ERD 캔버스는 `/erd`로 이동(딥링크 `?anchor=`), 공용 AppHeader 내비. 디자인: 호버 시에만 보이는 스크롤바, pressable(ease-in-out+클릭 스케일), overflow hidden 프레임. 브라우저 실검증 — fresh 마운트 setCenter는 ReactFlow onInit 이후로 보류하는 패턴으로 수정.
- **첫 화면 UX 수정** — "아무것도 안 뜬다" 피드백의 원인은 크래시가 아니라 앵커 방식의 안내 없는 빈 캔버스. ① 빈 상태 가이드 + 예시 칩(원클릭 시작), ② 초기 뷰포트를 전체 맞춤(깨알 줌) 대신 ELK 좌표 기반 앵커 setCenter로(측정 타이밍 무관·확장 시 시점 유지). 브라우저 실검증(스크린샷)으로 확인. 부수 레슨: dev 서버 실행 중 prod build 금지(.next 캐시 충돌 500), fitView의 nodes 필터는 측정 타이밍에 취약.
- **더미데이터 사실화** — 픽스처 어휘를 제조 ERP 수준으로(모듈별 엔티티 400여 개: HR_EMP·ORD_SO_HDR·MES_BATCH_HDR·QC_SAMPLE_RSLT 류, 엔티티 기반 PK명, 접미사→타입 관례 _YMD→char(8) 등, 업무형 뷰명 V_*_SUMMARY/V_SUM_/V_RPT_). 규모·엣지 케이스·회귀 자산은 불변 — 특수 뷰(V_CHAIN·V_CYCLE 등) 이름 유지, 122 테스트 통과(이름 결합 테스트 3건만 조정). 로컬 스택 재시드 — 히어로 앵커 HR_EMP(엣지 4종 동시), 체크리스트 앵커 갱신. 알게 된 것: inferred 투명도 0.7/1.0단은 행수 가중치 때문에 픽스처 규모로는 도달 불가(실DB 전용) — 문서에 명시.
- **bpm 운영 레슨 이식** — 실서버 런북(docs/deploy/deploy.md)에서 추출: ① 인증 면제 `/api/health` + compose healthcheck(frontend는 backend healthy 대기), ② `.gitattributes` LF 고정(Windows 경유 전송), ③ README에 Web origins(token CORS는 redirect URI가 아니라 이것), preferred_username 매퍼, federation↔bind 계정 별개, 첫 전체 동기화 프룬 주의, 트러블슈팅 표, 롤백 절, ④ ui-review에 LAN IP 확인 항목(secure context 함정은 localhost에서 재현 불가). crypto.randomUUID류 직접 사용 없음 확인(oidc 라이브러리 조합은 bpm 운영으로 검증됨). dev 서버 사고 1건: 실행 중 prod build가 .next 캐시 충돌 → 캐시 삭제·재기동으로 복구.
- **UI/UX 로컬 리뷰 킷** — 로컬 검증 범위를 프론트 UI/UX로 확정(통신 검증은 서버 배포 후). `tools/seed_ui_states.py`가 API로 전 시각 상태 프라이밍(확정 ✓·고/저신뢰 inferred·AI 제안·요약·화이트리스트) — 실행 중 스택에 적용해 실측 확인(HR_APRV 앵커에 confirmed/fk/ai_suggested 혼재). 체크리스트 15항목은 `docs/ui-review.md`(seed 42 기준 검색어 명시). Docker 킷(local-test.md)은 배포 리허설용으로 유지.
- **로컬 리허설 킷** — `docker-compose.local.yml` 오버레이: 로컬 Keycloak(realm·클라이언트·테스트 유저 2명 자동 임포트, `keycloak.local` alias로 브라우저·백엔드 issuer 일치) + `--profile collect`로 MSSQL(시드 스키마: FK/무FK 관계·중첩 뷰) + n8n(W0/W1 리허설, env 자동 주입). LDAP은 AD 전용 속성이라 로컬 대체 불가 → 끔(동기화만 비활성, 단위 테스트가 커버). `tools/seed_fixtures.py` CLI 신설(픽스처 없으면 즉석 생성). 검증: compose config·realm JSON·스크립트 — Docker 데몬 미기동이라 전체 기동은 사용자 실행 필요(runbook: docs/local-test.md).
- **인증 마무리** — /parsing 페이지의 raw fetch를 인증 공용 클라이언트로 교체(auth ON에서 401 나던 격차), 로그인 기록 추가(audit_logs action=login, KST 하루 1건 중복 제거 — bpm 패턴). 스모크 재기동으로 실검증: dev 모드 /api/me·whitelist CRUD·ingest 재시드·프론트 프록시 전부 정상. 서브넷 172.48.0.0/16 사용자 확정.
- **보안 리뷰 반영(인증)** — ① LDAP TLS 인증서 검증 강제(ldap3 기본 CERT_NONE → CERT_REQUIRED + `LDAP_CA_BUNDLE`, StartTLS에도 적용), ② LDAP 필터 이스케이프를 RFC 4515로(백슬래시 우선), ③ /api/me 로그인 동기화에 사용자별 5분 스로틀(AD 부하 공격 차단), ④ PKCE는 하드코드 비활성 대신 `!window.isSecureContext` 자동 판정 — HTTPS 전환 시 수정 없이 복원(평문 HTTP에선 crypto.subtle 부재로 불가피, bpm 동일 제약).
- **인증: Keycloak + LDAP + 화이트리스트** — bpm 패턴 이식(react-oidc-context/PyJWT·JWKS/ldap3, 라우터 단위 Depends, AUTH_ENABLED 플래그 쌍, 렌더 단계 토큰 주입, disablePKCE=HTTP 제약). bpm에 없는 것 2건 신설: ① 화이트리스트 로그인 게이트(테이블+관리 API+/admin 화면, sysadmin 우회, 감사 기록) — 요구사항, ② n8n 머신용 `X-API-Key` ingest 게이트(auth ON이면 키 필수). AD 동기화: 로그인 시 단건 + 관리자 전체 동기화(5분 스로틀, 빈 스캔 전삭제 방지, source=local 보존). 미등록 사용자는 전 API 403 + 안내 화면.
- **정찰 워크플로(W0) + 배포 구성** — 정찰 6종을 n8n 임포트 JSON으로(`w0_recon_queries.json`, 수동 트리거 → 종합 리포트 노드가 권한 차단·DMV 실패를 warnings로 자동 표시). [6]은 TOP 1 뷰를 골라 TRY/CATCH로 DMV 즉시 호출하는 단일 배치로 재구성. 배포: compose(postgres+backend+frontend), 외부 노출은 frontend 6678 단일 포트(UI+/api 프록시 — n8n도 이 주소로 POST), 네트워크 172.48.0.0/16(요청값 — RFC1918 아님 주의 명기), Next standalone + non-root 이미지.
- **정지점 15: AI 엔드포인트 — Phase 5 완료** — AI 클라이언트도 어댑터 뒤(무연결 원칙): 인터페이스가 메타데이터 타입만 받아 원본 값 유출 경로를 구조적으로 차단, 실 프로바이더 연결은 연결 단계 결정 사항. 제안은 candidate/ai로만 적재(중복·기존 FK 제거, 재실행 멱등) → 검증 큐 직행, **confirm에 검증 선행 가드 추가**(candidate 확정 시 400 — §5.2 금지 이행). 자연어 탐색(프론트 `?` 프리픽스), 요약은 qname 캐시 → 그래프 노드 ai_summary → 노드 툴팁+AI 배지. ai_suggested 엣지는 검증 전에도 노출하되 명확히 구분(§5.3).
- **정지점 14: 탐색 스캔(T3) — Phase 4 완료** — 202+폴링(승인 응답 규약), 2-pass 구조(1차 리콜→상위 K 풀 재검증 — 샘플 값은 확정에 미사용, 별도 컬럼 기록), 임계(0.9) 이상만 관계·이력 영구 기록. 동시 실행 수 제한 + 야간 실행은 not_before + 폴링 기동(별도 스케줄러 데몬 없이 — 운영에선 n8n/cron 폴링). 관측 기록 로직은 T2와 공용 서비스로 추출. 백그라운드 실패는 잡 error에 격리 + 구조화 로그.
- **정지점 13: 미리보기·감사·확정 — Phase 3(Fake) 완료** — preview는 TOP 20 서버 고정·무캐시·컬럼 단위 마스킹(●●●)·감사 로그 필수. confirm은 검증 선행 강제(관계 없으면 404), 재검증에도 confirmed 유지, 그래프에 ✓ confirmed 엣지. 프론트 ColumnPanel: 컬럼 클릭 → 후보(신호 배지) → T2 검증(containment·cardinality·confidence·패턴 라벨) → 미리보기 → 확정, N:M은 교차 관계 배지.
- **정지점 12: 히스토리 + confidence** — 설계 결정: 이력·관계는 계획 DDL의 column_id FK 대신 **텍스트 식별자**로 저장(스냅샷 삭제 cascade에 이력이 소실되고 스냅샷 간 연속성이 끊기는 문제). confidence = 마지막 containment × 관측수 가중(1회 0.6→3회 1.0) × 규모 가중(5천 행 기준), 패턴 4종(stable_confirmed/stable_with_orphans/drop_alert/small_sample_only) — §3.4 표 그대로 단위 테스트. T2 관측이 columns.distinct_count를 채워 이후 저카디널리티 필터가 실동작. 검증·확정 관계는 그래프에 inferred/confirmed 엣지로 노출(confidence·last_verified_at 포함). confirmed는 재검증으로 강등되지 않음.
- **정지점 11: 후보 스코어링 + 저카디널리티 필터** — §3.1 가중치(뷰 JOIN 100 > 명명 40/32 > PK 20, 타입·길이는 필터). 키 보너스는 단독 신호 금지(전 PK가 후보로 뜨는 노이즈 방지). 제외 사유(blacklist/low_distinct/computed/not_a_table)를 API로 노출해 UI 배지 대응. UQ 컬럼 멤버십은 계획 DDL에 저장처가 없어 "인덱스 존재" 근사는 is_pk만 사용. 픽스처 회귀: 뷰 JOIN에 등장한 real_no_fk 관계가 최상위 후보로 부상.
- **정지점 10: JoinValidator 추상화 + Fake** — 계획 §4.3 Protocol 그대로. ColumnRef는 텍스트 식별자(스냅샷 독립), cardinality는 "타깃 distinct==row_count(유니크)" 판정. Fake는 픽스처 값 집합으로 실 연산하며 기대 관계 전수와 일치 검증. live 모드는 팩토리에서 차단(보안 승인 게이트, 정지점 18 해제). 픽스처 수정: 부모 키 값 집합 row_count를 유니크 의미(=distinct)로 — 아니면 실제 FK가 N:M으로 오판.
- **정지점 8·9: sqlglot 파서 + JOIN 추출 — Phase 2 완료** — 파서는 실패를 상태로 격리(ok/partial/unsupported/parse_failed — sqlglot의 조용한 Command 폴백도 parse_failed로 잡음). PIVOT은 컬럼 매핑 부재로 unsupported 고정, APPLY는 해석 가능한 범위만 추출. 중첩 뷰는 컬럼 lineage를 재귀 조립해 direct/derived 승격, 카탈로그 set 행은 보존(보강 원칙 — 회귀로 강제). view_joins 적재는 픽스처 기대와 전체 일치. 파싱 지표는 `/api/snapshots/{id}/parse-stats` + 프론트 `/parsing` 관리 화면. 픽스처 수정 1건: JOIN 뷰 출력 컬럼명 중복은 불법 T-SQL이라 parent 측 비충돌 선택으로 변경.
- **정지점 7: n8n W1 워크플로 — Phase 1 완료** — `tools/build_n8n_workflow.py`가 n8n/sql/*.sql을 단일 소스로 워크플로 JSON 생성(테스트가 커밋본==재생성본 강제 → 쿼리 드리프트 차단). 07 쿼리는 n8n MSSQL 노드의 단일 rowset 제약에 맞춰 kind 판별 컬럼으로 통합. Code 노드는 기계적 그룹핑만(가공·판단은 ingest 담당 — 계획 §2 경계 준수). deps 조립 규칙: 07 컬럼 단위 우선, 06은 미해석 참조·DMV 실패 뷰만 보강(중복 적재 방지).
- **정지점 6(프론트): ERD 뷰어** — Next.js 15 + React Flow + elkjs(bundled — 메인 엔트리는 web-worker require로 번들 깨짐) + Tailwind v4. 앵커 검색→1-hop 확장, 뷰 기본 접힘(펼치면 lineage 점선 노출), 40노드 임계치 확인 모달(이미 초과 상태에선 재확인 안 함), design-app.md 토큰 CSS 변수화. 순수 로직(엣지 스타일·병합·크기 추정·ELK 결정성) vitest 12건 + 빌드·lint 통과. 스모크: 픽스처 시드 후 프록시 경유 검색 확인(브라우저 확장 미연결로 시각 확인은 보류).
- **정지점 6(백엔드): 조회 API** — objects 검색 / 앵커 N-hop 그래프(depth≤3 강제, 전체 그래프 반환 없음) / 뷰 lineage / 스냅샷 목록·diff. FK diff는 이름이 아니라 (src, tgt, 컬럼페어) 시그니처로 매칭(자동 생성 이름 변동 대비). 그래프 응답에 lineage_flag·unresolved_dep_count 포함해 UI 배지 데이터 제공. 레이아웃 엔진은 ELK 선정(결정적 배치·계층 표현·가변 노드 크기 — d3-force는 비결정성으로 기각).
- **정지점 5: lineage 재귀 엔진** — 순수 도메인 함수(경로 기반 순환 감지, 깊이 상한, 메모이제이션, 플래그 전파 — cycle이 depth_exceeded보다 우선). view-deps ingest 완료 시 동기 실행해 `view_lineage_flat` 적재. 픽스처 기대치와 전체 집합 동일성 검증 통과(중첩 뷰의 부모 전체 집합 상속 포함). 혼합 케이스(테이블 직접 참조 + 순환 참조)는 해석 행 유지 + 플래그 행 추가로 결정. `LINEAGE_DEPTH_LIMIT` 튜닝 설정 추가.
- **정지점 4: 수집 SQL + ingest** — FastAPI 앱 + `/api/ingest/catalog`·`/api/ingest/view-deps` (픽스처 포맷 = 계약). is_pk는 key_constraints에서 파생, 미해석 참조는 텍스트 식별자 보존, 에러는 승인된 `{"error": {code, message, context}}` 규약. 스키마 추가 2건(0002): `objects.definition`(Phase 2 파싱 입력 영속화 — raw POST는 재조회 불가), `objects.dmv_unresolved`. 수집 T-SQL 7종은 `n8n/sql/` — 07은 커서 + TRY/CATCH로 객체별 개별 호출(일괄 CROSS APPLY 전체 실패 방지).
- **정지점 3: 픽스처 생성기** — `tools/fixture_gen.py` (stdlib only, 시드 결정론). 409 테이블 / 9,000 컬럼 + 필수 케이스 전부(12단 체인, 순환 뷰, 크로스 DB, definition NULL, PIVOT/APPLY/힌트, DMV 실패, 함정 컬럼). 기대치는 두 벌: lineage_full(컬럼 정밀, Phase 2용) / lineage_phase1(set-level — 중첩 뷰는 부모의 전체 참조 집합 상속, deps만으로는 좁힐 수 없음). 값 집합은 관계 DAG 위상 순서로 생성하고 containment를 최종 집합에서 재계산해 데이터·기대치 불일치를 원천 차단.
- **정지점 2: 서비스 DB 스키마** — PostgreSQL 16 + SQLAlchemy 2.0 + Alembic 확정. 계획 DDL에 3개 필드 추가: `view_deps.referenced_database/name`(미해석·크로스 DB 참조의 텍스트 식별자 보존 — 없으면 Phase 2 재해석 불가), `view_lineage_flat.flag`(cycle/depth_exceeded), `columns.masking_policy`(§3.5 선반영). 테스트는 SQLite로 실행(dialect 중립), drift는 alembic compare_metadata로 검증.
- **Step 0 개편안 승인** — 신뢰도 4색 팔레트를 dataviz validator로 검증해 확정(`#00926a`는 deep-green 램프 확장 — 원본은 선 색으로 명도·채도 미달). staleness는 투명도 대신 배지(confidence 인코딩과 충돌), 다크모드 v1 제외. 확장 토큰·ERD 시각 언어는 `rules/frontend/design-app.md` 신설로 분리(원본 DESIGN-cohere.md 동결).
- **프로젝트 초기화** — claude-code-template 기반 셋업. 스택은 FastAPI(Python) + Next.js(TypeScript)로 결정, 백엔드/프론트엔드/양 언어 룰 모두 유지. 템플릿 메타 문서(docs/template/)와 템플릿 개발 이력(PROGRESS) 제거. 상세 설계·스캐폴딩은 후속 계획으로 진행 예정.
- **Task 5: n8n W2 sample_distinct kind** — W2 query executor에 `sample_distinct` 쿼리 kind 추가. TOP-N 표본 수·distinct 집계만 반환하고 원본 값은 노출하지 않음(gate-only). BUILD_QUERY_JS에 분기 추가·notes 업데이트·워크플로 JSON 재생성, 테스트 통과.
- **Task 14: 딥링크 재편** — `/erd?anchor=` → `/erd?focus=` 치환 + 컬럼 클릭 `/verify` 프리필 재배선. `page.tsx`의 handleOpenErd·handleOpenColumn 교체(후자는 /verify?src·srcLabel·srcCol·tgtLabel·tgtCol 계약으로 전환, columnName 미사용 제거), `ChatPanel.tsx`의 anchor→focus, `TableDetail.tsx`의 onOpenColumn 타입·호출부 정리. TypeScript/ESLint/vitest 전 통과. Task 15: 구 검증 UI 삭제.

- **Task 6: 전체 게이트 + README 영향 확인** — 프론트엔드 게이트(tsc ✓·eslint ✓·vitest 12 files 72 tests ✓·build ✓), 백엔드 게이트(pytest 331 tests ✓). README `## 화면` 절의 ERD 설명 수정: "앵커 검색·1-hop 확장·조인 빌더는 없다" → "테이블 검색과 호버 컬럼 내비게이션을 제공하며, 1-hop 확장·조인 빌더는 없다"(이번 변경 Task 4·5 반영).
